# Передача разработки FitFlow

**Это первый файл, который читает новый агент.** Он написан так, чтобы продолжить
работу без истории диалога: что за проект, где мы сейчас, что делать дальше и на
каких граблях уже потоптались.

Актуально на **01.09.2026**, версия **0.9.50**, ветка `arena/01a0580b-lifeflow-fitness`.

---

## 0. Открытые вопросы (прочитать до начала работы)

`OPEN-ISSUES.md` — реестр найденных, но **сознательно не исправленных**
дефектов и долгов. Заведён 28.08.2026 по просьбе владельца: пройтись по
всем пунктам разом и решить, что корректировать.

Главный из них — **O1: `sqlite-bundle.js` не копируется в APK**, поэтому
весь этап 0.6.0 (SQLite) на телефоне тихо откатывается на localStorage.
В браузере работает, в APK нет. Данные при этом не теряются.

Ничего из `OPEN-ISSUES.md` не чинить без явного решения владельца.

---

## 1. С чего начать (10 минут)

1. Прочитать этот файл целиком, затем `AI_GUIDE.md` (правила работы с владельцем)
   и `PROJECT.md` (устройство кода).
2. `git status` и `git log --oneline -5` — убедиться, что дерево чистое.
3. Прогнать все три теста, ожидаются `ALL TESTS PASSED` / `UI INIT CHECK PASSED` /
   `FOOD DB CHECK PASSED`:
   ```
   node test-parser.js && node test-ui-init.js && node test-food-db.js
   ```
4. Не выполнять Merge PR и не переключаться на `main` — перенос в `main`
   это самый последний шаг всего проекта.

**Стек.** Ванильные HTML/CSS/JS (без сборщика) + Capacitor + нативный Android
(Java/Kotlin). Основной код — `app.js` (~900 КБ, один файл), разметка —
`index.html`, стили — `style.css`, натив — `android-native/`.
Сборка APK — GitHub Actions по тегу; в песочнице **нет JDK и Kotlin**,
нативный код проверяется только структурно и собирается в CI.

---

## 2. Что это за приложение

FitFlow — офлайн-трекер здоровья на русском: вода, питание (ручной ввод, умный
разбор фразы, сканер штрих-кода, фото), тренировки и силовой дневник, шаги и сон
из Health Connect, вес, витамины-курсы, виджет на домашнем экране, локальная
нейросеть (опционально).

**Ядро продукта — вода и ручной учёт калорий.** Всё остальное вторично: если
выбор стоит между новой функцией и стабильностью ядра, выбирается стабильность.

**Главный принцип владельца:** «Простое приложение: один раз настроил и
пользуемся». Не городить кнопок, приложение само разбирается. Любое решение,
добавляющее пользователю работу, — плохое решение по умолчанию.

**Никаких видимых заглушек.** Нерабочий элемент либо не добавляется вовсе, либо
добавляется полностью скрытым (`hidden`). Полурабочих кнопок в UI быть не должно.

---

## 3. Как работать с владельцем

Подробно — в `AI_GUIDE.md`, здесь самое важное:

- **Принцип обратной связи.** Замечания владельца — это предложения к
  обсуждению, а не приказы. Отвечать **по каждому пункту** развёрнуто и спорить
  аргументированно, если есть решение лучше. Финальное слово — за владельцем.
- **Ничего не выдумывать.** Работать строго с содержимым репозитория; не
  описывать функции, которых нет в коде.
- **Владелец читает с телефона.** Инструкции — короткими пронумерованными
  шагами. Ссылки — кликабельными полными URL; ссылки на правку файлов GitHub —
  в режиме `/edit/<ветка>/<путь>`.
- **Одна версия = одна небольшая законченная задача.**

---

## 4. Где мы сейчас

Выпущены 0.9.1–0.9.23. Замечания владельца идут блоками. Блоки 1–3 закрыты
(0.9.1–0.9.4, 0.9.5, 0.9.12), витамины — 0.9.13. **Блок 4 закрыт полностью**:
пункты 1–3 в 0.9.14–0.9.16, пункт 4 (облачное распознавание фото, вариант A
«чинить облако») — в 0.9.20. **Блок 5** закрыт в 0.9.18 (прогресс силовых за
период и два показателя). **Блок 6** закрыт в 0.9.19 (кнопка записи воды на
умных часах). **П.5 дизайн виджета** — в APK с 0.9.24 (ждёт полевой проверки).

Из блоков владельца остаются: **п.1 «Питание»** (разделение Комбо / Мои блюда /
Мои продукты), **п.3 микроанимации**.

Ожидается полевая проверка на телефоне владельца (Android 16): (а) прогресс
упражнения за 7 дней / месяц / всё время и два показателя сразу (0.9.18);
(б) кнопка «+ 250 мл» на часах — **только если часы на Wear OS**; на связке
«Honor + Zepp» кнопок не будет по причине из раздела ниже (0.9.19).

**0.9.50 (текущая):** полевая правка силовой по скрину владельца. У нового
упражнения убрана пустая строка подхода-двойник (`addStrengthExercise` теперь
`sets: []`); кнопка «Добавить» вынесена на свою строку (`flex:1 1 100%`), пилюли
«вес × повторения × подходы» получают всю ширину. Новый геометрический сторож
`tools/check-strength-bulk-fit.py` (Pillow, широкий DejaVu): 360/412dp влезают.
ВАЖНО: тест 0.9.12 требует плейсхолдер именно «Повторения» (не «Повторы»),
поэтому обрезка лечится раскладкой, а не переименованием. Только app.js/style.css/
тест/доки — workflow не тронут.

**0.9.49:** пункты 6 и 8 решений владельца. `PROJECT.md` переписан
одним файлом (533 КБ → 20 КБ): живые разделы 1–13, 16 актуализированы,
раздел 14 «История версий» (468 КБ) заменён ссылкой на `CHANGELOG.md`, план —
ссылками на `ROADMAP.md`/`POSTPONED.md`. Строка «Текущая версия: **0.9.49**»
(читается `test-ui-init.js:5470`) сохранена. `CHANGELOG.md:4` больше не
отсылает к разделу 14; O5/O6 в `OPEN-ISSUES.md` закрыты. Изменения только в
документах + версия в version.txt/package.json/app.js/index.html/README/ROADMAP.

**0.9.48:** пункт 7 решений владельца — чистка `design/`. Удалены мёртвые
макеты «капли» (`widget-p5-*.png` ~5,1 МБ, `WIDGET-P5.md`, генератор
`tools/widget-preview-p5.py`), ссылки исправлены. `design/` 7,5 → 2,6 МБ.

**0.9.47:** пункт 9 решений владельца — виджет «капля» убран,
у «списка» появился предпросмотр.
- Удалены: `FitFlowWidgetDropProvider.java`, разметка `fitflow_widget_p5`,
  три кнопки `fitflow_widget_btn_p5_*`, строки в `canvas_widgets` и
  `canvas_infos` зеркала. `layoutRes()` в `FitFlowWidgetCanvasProvider`
  стал `abstract` (дефолт существовал только ради капли). Силуэт капли
  (`dropPath`/`dropAspect`/`paintDropTiles`/`drop-shape.png`) ОСТАЛСЯ —
  его юзают «плитки». Убраны только `drawDrop`/`paintDrop`.
- Новое: `tools/widget-list-preview.py` — зеркало `widget_layout_xml()`;
  пишет `assets/widget-previews/fitflow_preview_list.png`; подключён в
  `make-widget-previews.py`. Классический `fitflow_widget_info.xml` получил
  `android:label="FitFlow · список"` и `android:previewImage`.
- **ТРЕБУЕТСЯ замена активного `.github/workflows/build.yml`** (агент не
  может пушить workflows): пока активный файл содержит `FitFlowWidgetDropProvider`,
  сборка упадёт на несуществующем классе. Две ссылки для замены — в ответе
  владельцу.
- Гейты 0.9.24/0.9.32/0.9.34/0.9.6 обновлены: ищут по коду генерации
  (`.write_text`, записи в canvas_widgets), а не по подстрокам — комментарии
  в yml честно описывают удаление и содержат старые имена.

**0.9.46:** пункт 10 решений владельца + его полевое замечание
от 01.09.2026 про клавиатуру.
- В блоке упражнения появились три пилюли «вес × повторения × подходы»
  и кнопка «Добавить»; кнопка «＋ подход» сохранена (владелец просил не
  убирать). Для упражнений со своим весом поле веса не обязательное —
  тогда строка из двух чисел читается как подходы × повторения, ровно так,
  как это уже трактует `parseQuickSets`.
- **Полевое замечание владельца:** поле «всё сразу» с `inputmode="decimal"`
  открывало на Android цифровую панель без буквы «x» — строку 5x10x12
  нельзя было набрать вовсе. Исправлено на `inputmode="text"`; разделитель
  по-прежнему любой (`x`, русская «х», пробел, запятая).
- Подтверждение очистки заполнения — окном приложения
  (`#strength-cancel-dialog`) вместо системного `window.confirm`
  с кнопками OK/Cancel, который выбивался из оформления.
- Логика пилюль вынесена в чистую `buildBulkSetsLine(values, isBw)`
  и экспортирована для тестов: 10 случаев + гейт «результат пилюль
  разбирается тем же `parseQuickSets`», чтобы два способа ввода не
  разъехались при следующей правке.
- **Владелец сам заменил активный workflow (коммит `23c0b7a`)** — теперь
  `.github/workflows/build.yml` совпадает с зеркалом `tools/github-workflows/build.yml`.
  Сборка 0.9.46 (`v0.9.46-build420`) — **первая с SQLite внутри APK**:
  APK вырос с 59 515 022 до 60 002 586 байт (+487 564).
- **Новый документ `PHOTO_AI_ANALYSIS.md`** — разбор «еда по фото» по
  запросу владельца от 01.09.2026. **Первая версия ошибалась** и выдавала за
  новые рекомендации то, что уже есть: владелец поправил, и документ переписан.
  Проверено чтением `app.js`: штрихкод+Open Food Facts (`lookupOffByBarcode`,
  0.9.2/0.8.28), ввод словами/голосом (`startRealVoiceInput`, 0.3.31/0.7.16) и
  локальное фото (`recognizeFoodPhotoLocal`, 0.4.1) — **уже реализованы**.
  Тезис «никто не делает бесплатно и точно» заменён: ZEPP (Amazfit) делает, но
  за свои серверы и в закрытой экосистеме; открытого бесплатного API нет.
  Новые (отсутствующие) варианты: GigaChat как провайдер (облако без VPN и
  зарубежной карты), встроенная нейросеть телефона (ML Kit GenAI, только новые
  флагманы), OCR этикетки через Tesseract (Google кириллицу не читает, запрос
  открыт с 04.2024). Свой сервер «как у ZEPP» — тупик.

**0.9.45:** закрыты O1, O2, O8, O13 — решения владельца от 31.08.2026.
- **O1 закрыт: `cp sqlite-bundle.js www/` добавлен в ОБА workflow.**
  Подтверждение дефекта пришло с телефона: строка состояния из 0.9.44
  показала «💾 Хранилище: localStorage (Dual-Write резерв)». Цена — +906 КБ
  к APK, владелец согласился. **Хранилищ два намеренно и оба нужны:**
  SQLite — основное (таблицы, IndexedDB, квота на порядки больше),
  localStorage — постоянный Dual-Write-резерв и источник для первой
  миграции (снимок `fitflow:pre-sqlite-backup` пишется ДО переноса). Убирать
  резерв нельзя: сбой WASM означал бы потерю дневников.
- **O8 закрыт вариантом 1:** `stripSecretsForBackup()` вычищает
  `aiSettings.cloudKey` из копии, провайдер/модель/адрес остаются. Тест —
  в `test-parser.js`, а не текстовый сторож: проверяется поведение функции.
- **O2:** `assets/js/sql-wasm.js` + `assets/wasm/sql-wasm.wasm` удалены
  (−696 КБ), README поправлен, сторож не даст вернуться.
- **O13:** строки `cp food-db.js` / `cp manifest.json` убраны.
- **ГРАБЛЯ СЕАНСА: агент НЕ МОЖЕТ менять `.github/workflows/`.** Push
  отклоняется: «refusing to allow a GitHub App to create or update workflow
  … without `workflows` permission». Обычные файлы пушатся нормально, поэтому
  ошибка неочевидна. Правило AI_GUIDE «замена workflow — всегда две ссылки
  владельцу» осталось в силе именно поэтому. Правится только зеркало
  `tools/github-workflows/build.yml`, активный файл заменяет владелец;
  сторож в `test-ui-init.js` строго проверяет зеркало, а про активный пишет
  напоминание и не краснеет. **Пока владелец не заменил workflow, SQLite в
  APK не попадёт** — версия соберётся, но строка состояния по-прежнему
  покажет localStorage.
- **Владелец спросил про «работать через личный аккаунт DeepSeek/Алисы/
  GigaChat вместо API».** Ответ: технически невозможно и не нужно. Веб-чат
  провайдера — закрытое приложение без открытого интерфейса; автоматизация
  это обход правил провайдера, а приложению пришлось бы хранить логин,
  пароль и сессионные куки пользователя — это хуже ключа на порядок. Ключ
  API — единственная честная связка.
- **Очередь (решено владельцем, не сделано):** пункт 6 — переписать
  `PROJECT.md` (из 533 КБ **468 КБ — раздел 14 «История версий»**, остальное
  ~65 КБ); пункт 7 — почистить `design/` (7,5 МБ); пункт 8 — сквозная
  ревизия документов; пункт 9 — убрать виджет «FitFlow · капля» и сделать
  предпросмотр виджету «Список»; пункт 10 — поля «вес × повторения ×
  подходы» в шаблоне тренировки + своё окно подтверждения вместо
  системного `confirm()` (единственный вызов — `app.js` ~11015).

**0.9.44:** полный аудит + живые прогоны приложения.
- **Что делали.** Прочитали репозиторий и прогнали приложение в jsdom
  (браузера в песочнице по-прежнему нет): инициализация, вода, еда текстом,
  сбой хранилища, 364 кнопки подряд, старт на 10 вариантах испорченного
  localStorage. Падений нет; найдены дефекты «тихой» природы — их текстовые
  сторожа не ловят по определению.
- **`#storage-engine-status` НЕ СУЩЕСТВОВАЛ в `index.html`.** Строку состояния
  хранилища описывали OPEN-ISSUES (O1: «Настройки → строка состояния
  хранилища») и разбор 0.9.43 (O7: «счётчик сбоев виден всегда»), а узла не
  было: `renderStorageEngineStatus()` уходил по `if (!statusEl) return`. То
  есть O1 нельзя было проверить на телефоне ВООБЩЕ, а из 0.9.43 работал
  только одноразовый тост. Узел добавлен в «Настройки → Данные», плюс
  `renderStorageEngineStatus()` зовётся в catch у `saveState()` — раньше
  строку перерисовывал только удачный SQLite-синк.
- **Грабля: текстовый сторож не видит отсутствия узла.** Проверка 0.9.43
  искала в app.js строку «Неудачных сохранений за сеанс» и была зелёной при
  неработающей функции. Заведён сторож «все id-селекторы app.js находят узел
  в разметке» (сверяет `$('#id')`, `getElementById`, `bindEvent` с index.html
  и с разметкой, которую app.js рисует сам). **Правило: сторож на текст в
  app.js доказывает наличие текста, а не работу функции.**
- **Мёртвые привязки.** `#manual-food-add`, `#food-combo-star`,
  `#manual-food-favorite`, `#quick-open-manual` — узлов нет с переезда ручной
  формы в «⚡ Быстрые записи»; на каждом запуске четыре warning'а. За ними
  стояли недостижимые `readManualMeal` / `addManualFood` / `saveFavoriteMeal`
  (упали бы на `null.value`, если бы их снова привязали) — удалены, живой
  аналог `#quick-meal-*` / `#quick-combo-*` на месте. Сторож 0.5.6 «⭐/🍽/✍️
  открывают свои вкладки» требовал ровно эти мёртвые вызовы — переписан на
  сверку вкладок разметки с вызовами.
- **CHANGELOG.md обрывался на 0.9.38** — пять версий без записей в файле,
  который README называет «историей изменений». Восстановлено по разборам
  версий + сторож «в CHANGELOG есть текущая версия».
- **`test-ui-init.js` падал ЦЕЛИКОМ без Pillow.** `execFileSync` бросал
  исключение на проверке формы капли, и прогон обрывался без итоговой строки:
  800+ проверок терялись из-за зависимости предпросмотров. Заменён на
  `spawnSync` — краснеет одна проверка. Pillow по-прежнему нужен для
  предпросмотров: `pip install pillow --break-system-packages`.
- **Три сторожа версии были зашиты строкой '0.9.43'** — при каждом подъёме
  версии краснели, хотя `VERSION` из version.txt в файле уже был. Сверяют
  `VERSION`.
- **O8 (НОВЫЙ, ждёт решения владельца): ключ облачного ИИ уезжает в
  резервную копию.** `cloneForBackup(state)` копирует состояние целиком,
  включая `aiSettings.cloudKey`; PRIVACY.md при этом говорит «ключи — только
  на устройстве» и предлагает «переслать копию себе любым способом».
- **Что проверено и в порядке:** 364 кнопки без необработанных исключений;
  10 сценариев испорченного localStorage стартуют и нормализуют данные;
  парсер (318), UI-аудит, база продуктов — зелёные; workflow и его зеркало
  идентичны; `tools/check-*.py` — три из трёх зелёные; мёртвых функций
  верхнего уровня 8 из 820; дублей id в разметке нет.

**0.9.43:** кнопка «Спорта», честный сбой сохранения, версии.
- **Кнопка `#ai-stats-run-btn` вылезала за экран.** Причина НЕ в заголовке
  (его починили в 0.9.42) и не в теме как таковой: `flex: 0 0 auto` +
  `height: 38px` + капслок с `letter-spacing: .05em` у `html[data-palette=
  "sport"] .btn`. Стало `flex: 1 1 auto`, `min-height`, `white-space: normal`;
  плюс общая страховка `.btn { max-width: 100%; overflow-wrap: anywhere; }`.
- **`tools/check-button-overflow.py`** — считает ширину подписи реальным
  Manrope с учётом капслока и разрядки на 360 dp. **Важно: комментарии CSS
  снимаются до разбора** — иначе пояснение «height: 38px …» читается как
  объявление и даёт ложный вызов (поймано на себе).
- **`saveState()` теперь громкий:** `isQuotaError()`, `toast` один раз за
  сеанс (`saveFailureNotified`), счётчик `saveFailureCount` в строке
  состояния хранилища. `toast` обёрнут в try — зовётся и до готовности UI.
- **O3/O4 из OPEN-ISSUES закрыты:** README/PROJECT/package.json/ROADMAP
  синхронны с `version.txt`, сторож в `test-ui-init.js`.
- **O1 (sqlite-bundle.js не копируется в APK) НЕ трогал** — правило файла
  «ничего не чинится без решения владельца», и это +906 КБ к APK. Вопрос
  задан владельцу.

**0.9.42:** кайма капли, начертание плиток, вёрстка тем.
- **Кайма капли: причина в ОТРИСОВКЕ, не в файле.** `saveLayer` с дробными
  координатами округляется наружу; волна идёт до `(w,h)`, попадает в лишнюю
  полоску, `DST_IN` её не гасит (за краем bitmap множителя нет). Сверху каймы
  нет — туда вода не доходит. Чинено тремя мерами: `saveLayer(-1,-1,w+1,h+1)`,
  `clipRect(0,0,w,h)`, `Math.round` для dx/dy/dw/dh.
- **Плитки: значения снова `font`, не `fontBold`** — жирный был подпоркой под
  сверхтонкий вариативный шрифт (0.9.37), причина убрана в 0.9.41. Жирными
  остаются процент в капле и подпись кнопки.
- **Предпросмотры грузят `assets/fonts/manrope-*.ttf`**, а не системный DejaVu:
  иначе врут о начертании (именно это скрывало проблему).
- **`✓` в Manrope НЕТ** — подставляется системным шрифтом, ширина
  непредсказуема, подбор кегля промахивается. В узких плитах галочка убирается.
- **Бенто, половинка:** `lineNarrow` срезает «принято », хвост «· след. …»,
  «нет на сегодня»→«нет»; кегль значения подбирает `fitSp()` (оценка
  0.55×кегль на символ, ширина плиты из `OPTION_APPWIDGET_MIN_WIDTH`).
- **Вёрстка:** корень «Анализ периода вылезает в теме Спорт» — `min-width:auto`
  у флекс-детей, а не сама тема. Добавлены `min-width:0` + `overflow-wrap`
  для `.card-header` и ещё 16 флекс-строк; сторож —
  `tools/check-flex-overflow.py` (браузера в песочнице нет, playwright не
  ставится).
- **Грабли:** сторожа 0.9.30/0.9.37 фиксировали жирность и кегль как эталон —
  при смене требований их надо править, а не ослаблять.

**0.9.41:** шрифт, капля, деление блока бенто.
- **КОРЕНЬ саги с жирным найден: `assets/fonts/manrope.ttf` ВАРИАТИВНЫЙ**
  (`fvar`, ось wght 200…800, дефолт 200/ExtraLight). `createFromAsset` берёт
  дефолт и ось не двигает → весь текст был ExtraLight, `create(t, BOLD)` почти
  не менял вид. Теперь `tools/make-widget-fonts.py` печёт
  `manrope-regular.ttf` (500) и `manrope-bold.ttf` (800), `ensureFont` грузит
  оба, откат на вариативный сохранён. Проверка веса — счётом «чернил» в Pillow.
- **Процент в капле** — два прохода: `STROKE` (обводка) + заливка. Поля темы
  `dropPct`/`dropPctHalo`. `dropEmpty` тёмной темы осветлён 0xFF374151 →
  0xFF4A5666.
- **Кайма капли** — силуэт упирался в кромку файла. Пересобран: обрезка по
  альфе ≥ 24 + поле 3 px. Теперь **306×443, AR = 0.691** (было 301×439/0.686).
- **Нижний ряд бенто делится надвое** (`MAX_SLOTS = 4`, слот D, `BARS_D`,
  `widget_bento_split_gap`). Фон нижних плит переехал из подложки в
  `android-res/drawable/fitflow_bento_card.xml` — подложка одна на все составы.
  В половинках кнопок нет (`allowBtn`), кегль 14/11/10, подписи и единицы
  сокращаются (`labelNarrow`/`goalNarrow`/`lineNarrow`). Слоту C провайдер
  возвращает 17/12/11 и отступ 46 dp, когда он занимает строку целиком
  (`setViewPadding`/`setTextViewTextSize`, оба доступны с API 16).
- **Грабли:** `replace('@', 'd')` в патч-скрипте испортил `@+id` → `d+id`;
  подставлять только уникальные маркеры вроде `SLOT`. Четыре сторожа считали
  элементы разметки точными числами и покраснели от четвёртой плиты — числа
  пересчитаны (6/8, 5/7, 11/15, полосы 2→3, кнопки 4→6).

**0.9.40:** три полевых бага виджетов.
- **Вода с виджета воскрешала вчерашние ккал.** Обработчик `ADD_WATER_250`
  писал `date = сегодня`, а `FitFlowWidgetData.load` считает по этому полю
  свежими ВСЕ дневные значения. До первого запуска приложения в новый день
  вчерашние `foodTotal`/`activityMinutes`/строки оживали. Теперь при `!fresh`
  они обнуляются в той же транзакции. **Правило: кто двигает `date` — обнуляет
  всё дневное, что не считает сам.**
- **Бенто ждал 0.9 с и прыгал.** Итог ему давали в последнем кадре анимации.
  Перенесено в начало `animateWater`, ДО первого кадра (prefs уже содержат
  итог). Не анимируется — обновляется мгновенно.
- **Бенто игнорировал строковые показатели** (сон, вес, план дня, настрой,
  тренировка, витамины) → пустой блок. Добавлены `SUPPORTED_LINES`, `isLine`,
  `lineOf`, `lineTail`; шкала гасится через `showBar(..., -1, 0)`; без данных
  плита не занимается. Зеркало — `tools/widget-bento-preview.py` (2 новых
  варианта в листе).
- Своих PNG у бенто ровно шесть (новые требуют правки сборки) → сон получил
  часы, прочие строковые — карандаш.
- **Грабли тестов:** `isLine(slot)` встречается дважды (озвучка и отрисовка);
  сторож на одно вхождение мутацию пропускал — считать через `match(...).length`.

**0.9.39:** починка сборки + Phosphor на плитках.
- **0.9.38 не компилировалась.** `catch (Throwable t)` внутри метода, где `t` —
  доля времени анимации: javac «variable t is already defined». Переименовано в
  `err`. Заведён `tools/check-java-scopes.py` (разбор по глубине скобок) и
  сторож, требующий нулевой код возврата. **JDK в песочнице нет и поставить
  нельзя — apt недоступен**, так что подобное ловим только своими проверками.
- **Логи Actions из песочницы не скачиваются**: `gh run view --log-failed` и
  `gh api .../logs` отдают EOF (редирект в blob-хранилище). Причину падения
  искать чтением диффа, а не логом.
- **Набор значков — на оформление.** `iconAsset(ctx, id, family)`, у плиток
  `TILES_ICONS = "tiles"`, файлы `assets/widget-icons/tiles-*.png` (Phosphor
  fill, `tools/fetch-phosphor-icons.py`). Откат на общий значок обязателен.
  `tiles-weight.png` НЕ кладём: у Phosphor только чаши-балансир ⚖️.
- Зеркало `tools/widget-tiles-preview.py` знает тот же порядок поиска.

**0.9.38:** капля по макету владельца, честный жирный, бенто без анимации.
- **Шаблон присылают в двух видах.** Скриншот макета непрозрачен (альфа 255
  везде), силуэт из него берётся только порогом по яркости и выходит
  ступенчатым — это и была жалоба «пиксели вместо скруглений». Новый файл
  владельца с настоящей альфой: капля вырезана по разрыву колонок, записана
  белым по альфе, 301 × 439, соотношение 0.686. Проверять тип цвета в IHDR.
- **`face == fontBold` не работает.** Manrope одновесный, `Typeface.create`
  возвращает тот же объект, условие всегда ложно — потому жирный не появился и
  в 0.9.37. Введён `FitFlowWidgetPaint.textBold()` с безусловным
  `setFakeBoldText(true)`; в `fitPaint` жирность едет отдельным флагом.
- **Каскад в кадре анимации.** `FitFlowWidgetProvider.updateAll()` перерисовывал
  соседние виджеты дважды и перевзводил `AlarmManager` на каждом кадре. Добавлен
  `updateListOnly()` без каскада и будильников; бенто из анимации исключён и
  обновляется один раз по `if (last && sHasBento)`.
- **Лист сравнения значков** — `tools/compare-icon-packs.py` →
  `design/icon-packs-compare.png`, четыре свободных набора на наших показателях.
- `tools/github-workflows/build.yml` не менялся, совпадает с активным —
  владельцу заменять не нужно.

**0.9.37:** капля владельца, переходы в разделы, плавность.
- **Жирный на Manrope делает ТОЛЬКО `setFakeBoldText(true)`.**
  `Typeface.create(font, BOLD)` на одновесном шрифте не даёт ничего —
  владелец не увидел разницы в 0.9.36. Включено в `text()` по `face == fontBold`.
- **Рывки анимации = лишняя работа на кадр.** Убрано: обход всех трёх
  семейств (теперь `detectFamilies()` один раз) и аллокация bitmap на
  каждый кадр (два буфера по очереди — отданную лаунчеру картинку
  править нельзя). Время 900 мс.
- **Шаблон капли**: владелец кладёт скриншот, а не силуэт. Из него
  извлекается маска (разница с размытием → флуд-заливка → сглаживание).
  Пропорции берутся у файла: `dropAspect()` ↔ `drop_aspect()`.
- **Тап по плитке**: прозрачные Button по сетке поверх картинки,
  `open_<id>` → `openWidgetTarget()` в app.js. Слоты объявлены ДО кнопки
  воды и шестерёнки (в FrameLayout ловит объявленный ПОЗЖЕ).
  requestCode `base+10+index` — свой у каждого слота.
- **Резерв под шестерёнку — 3.5 dp, а не 11**: считается от её центра и
  радиуса. Из-за завышенного резерва подписи резались зря.
- Узкая плитка не вмещает «Питание»/«Витамины» ни при каком кегле —
  добавлены короткие синонимы (`tilesLabel` ↔ `tiles_label`).

**0.9.36:** значки, шестерёнка на плитках, плавность.
- **Анимация виджета — ТОЛЬКО по часам.** Два антипаттерна, оба владелец
  поймал глазами: (1) `updateAll()` перед стартом = скачок на финал и
  откат назад; (2) кадры, поставленные пачкой по номеру, копятся и идут
  рывками. Правильно: следующий кадр из предыдущего + `uptimeMillis()`.
- **Значки лежат в репозитории**: `tools/fetch-widget-icons.py` тянет
  Material Symbols через `gh api` (сети наружу нет), а
  `tools/svg-to-widget-icon.py` — САМОДЕЛЬНЫЙ растеризатор SVG (в
  песочнице нет ни cairosvg, ни rsvg, ни inkscape). Заливка по
  even-odd, поэтому «дырки» в значках получаются сами.
- **Шаблон капли**: `drop-shape.png` → маска через `PorterDuff.DST_IN`
  в `saveLayer`. CLEAR по-прежнему нельзя — будет дыра до обоев.
- **`fontBold`** — только подписи кнопок. Manrope одного веса, жирность
  синтезирует система.
- У `TilesTheme` появилось поле `gear` (тише `muted`).
- Предпросмотры теперь берут НАСТОЯЩИЕ png значков — видно то же, что на
  устройстве.

**0.9.35:** третий виджет «плитки» + сон + свои значки.
- **Анимация — через подмену данных, а не параметр отрисовки.**
  `FitFlowWidgetData.sWaterOverride` + `FitFlowWidgetAnimator`: на время
  кадров подставляется промежуточное значение воды и перерисовываются
  ВСЕ семейства. Так анимацию получают все оформления, не трогая
  рисовалки. Последний кадр обязан сбрасывать override в -1.
- **Превью в системном списке** — `android:previewImage` в info-xml,
  PNG в `res/drawable-nodpi/`. Генератор: `tools/make-widget-previews.py`
  (рисует из тех же зеркал, что и виджеты). Без превью лаунчер показывает
  иконку приложения.
- **Форма капли:** острая вершина = управляющие точки прижаты к оси
  (0.545/0.455), иначе выходит яйцо. Пропорции пуза 0.80.
- **Грабля:** многострочные записи в `canvas_infos` уронили два старых
  сторожа, искавших их одной строкой — в регэкспах теперь `\s*`.
- **Грабля (тест-пустышка):** `A || B && C` — из-за приоритета `&&`
  проверка обесценивалась. Скобки обязательны.
- **Имя виджета в системном списке — `android:label` НА РЕСИВЕРЕ**
  (в манифесте), а не в `appwidget-provider` xml. Без него лаунчер
  подставляет имя приложения — владелец видел шесть одинаковых пунктов.
- **Анимация в виджете возможна только «пачкой перерисовок».** RemoteViews
  не анимируются: `animateWater()` перерисовывает картинку 12 раз по 40 мс
  через Handler. Только по нажатию! Постоянной анимации быть не должно —
  каждый кадр это `updateAppWidget`, это дорого. Ограничение стережёт тест.
- **Свои PNG-значки:** `assets/widget-icons/<id>.png` → APK →
  `FitFlowWidgetPaint.iconAsset()`. Приоритет: PNG → вектор (весы) →
  эмодзи. Суффикс `-color` запрещает перекраску. Есть кэш на процесс:
  без него декодирование убило бы анимацию.
- **Грабля:** `showWaterOnly()` трогает кнопки «Еда»/«Витамины», которых в
  разметке плиток НЕТ. Для плиток видимость выставляется вручную.
- **Грабля:** имя `FitFlowWidgetTilesProvider` было в чёрном списке теста
  0.9.24 (старый удалённый виджет). Список поправлен.
- **Сон** — только общее время (`widgetSleepLine`), цели у показателя нет.
- Предпросмотр плиток: `python3 tools/widget-tiles-preview.py`, зеркало
  сверяется тестом по шести числам.

**0.9.34:** четыре замечания владельца по 0.9.33.
- **Значок показателя ОДИН** для списка и кнопки — общая точка
  `FitFlowWidgetPaint.neonIcon(id, ...)`. Не хардкодьте эмодзи в кнопках:
  «вода — капля и там, и там» (требование владельца).
- **Вес рисуется вектором** `iconScale()` — порт `WEIGHT_SCALE_SVG_SM` из
  `app.js`. ⚖️ забраковано дважды (0.4.14 и 0.9.34): это весы правосудия.
- **Кнопка витаминов:** подпись постоянная, состояние — `iconCheckCircle()`:
  зелёный залитый = всё принято, жёлтый контурный (`DOSE_AMBER`) = часть.
- **Светлая тема** — `NeonTheme`/`NEON_LIGHT` + `FitFlowWidgetNeonLightProvider`
  (`requestCodeBase` 760). Геометрия ОБЩАЯ с тёмной, разметка тоже общая:
  второй `drawNeon` заводить нельзя, правки разъедутся. На светлом фоне
  свечение выключено флагом `glow` — неон на белом выглядит грязно.
- **Грабля среды:** песочницу могут пересоздать между сессиями — локальные
  коммиты пропадают, а файлы остаются. История цела на remote: `git fetch`
  + `git reset --soft FETCH_HEAD`. Pillow после пересоздания ставится заново
  (`pip install pillow --break-system-packages`).

**0.9.33:** шесть замечаний владельца после установки 0.9.32 на
экран телефона. Все закрыты.
- **Значки — системные эмодзи** (💧 🔥 👟 🏃 🏋️ 💊 ⚖️), рисуются `drawText`.
  Свои контуры из линий и дуг в 11 dp владелец забраковал: «не читаются».
  Не возвращайтесь к рисованным иконкам — на это есть тест. PNG в проект
  класть не нужно, эмодзи есть в любом Android.
- Список справа отодвинут от кольца (10 → 16 dp) и укрупнён (10 → 11,5 dp).
- **Колец максимум три**, остальные выбранные показатели идут строками,
  пока хватает высоты: `maxRows` считается по реальной высоте ячейки
  (4×2 ≈ 4 строки, 4×3 ≈ 5–6). Шрифт при этом НЕ уменьшается.
- **Набор кнопок следует составу** (`showActionsFor`): выключили «Питание» —
  пропала и дуга, и кнопка «Еда». Прозрачные ловушки скрываются через
  `View.GONE` синхронно с рисунком, иначе палец попадёт мимо пилюли.
- Кнопка витаминов показывает «1/2» или зелёную галочку «готово».
  Счётчик берётся из `FitFlowCourses.dosesForDate()` в `FitFlowWidgetData`.
- **Шестерёнка** 14 dp в правом верхнем углу → `widget_settings` (тот же
  обработчик, что у бенто). Середина сделана обводкой: `PorterDuff.CLEAR`
  прорезал бы в стеклянной карточке настоящую дыру до обоев.
- **Грабля:** полосу списка надо считать от границы ПОД шестерёнкой, иначе
  последняя строка залезает под значок и обрывается многоточием.
- В песочнице нет шрифта эмодзи, поэтому предпросмотр подставляет старые
  контуры — на устройстве значки цветные.
- **Нужна замена workflow:** в разметку добавлена шестерёнка.
  Владелец просит **всегда давать две ссылки**: raw (откуда копировать) и
  `/edit/` (куда вставлять) — см. AI_GUIDE.md, «Формат ответов».

**0.9.32:**
- «Кольца» (`FitFlowWidgetNeonProvider` + `FitFlowWidgetPaint.drawNeon`)
  переписаны по референсу владельца: стекло, неон, значки, пилюли.
- **Кнопки-пилюли рисует Canvas**, а в разметке лежат ПРОЗРАЧНЫЕ Button
  без фона и текста — только ловят нажатие. Иначе поверх нарисованной
  пилюли легла бы вторая, системная.
- **Грабля:** «капля» и «кольца» делили один layout `fitflow_widget_p5`.
  Прозрачные кнопки сломали бы «каплю», поэтому у «колец» теперь своя
  разметка `fitflow_widget_neon`, а `layoutRes()` в
  `FitFlowWidgetCanvasProvider` стал переопределяемым.
- В центре — дата и день недели, НЕ средний процент (владелец: такого
  показателя нет, он плыл бы при смене состава). Подбор варианта:
  полная дата → короткая → только число.
- Кольцо только у показателей с парой «значение / цель» (`NEON_RINGED`).
  «Тренировка» — строка-статус из `d.workoutShort()`; саму строку считает
  `app.js` (`widgetWorkoutLine`) и кладёт в prefs `workoutLine`.
- `FitFlowWidgetData` получил `ordered`/`order()`: `HashSet` терял порядок
  пользователя, а «кольцам» он нужен (первый выбранный — внешнее кольцо).
- Предпросмотр без Android SDK: `python3 tools/widget-glass-preview.py`.
  Он объявлен ЗЕРКАЛОМ `drawNeon()`, и тест сверяет девять чисел
  (радиус, шаг и толщины колец, шаг строк, высоту и отступ кнопок,
  отступ списка, верх списка, размер шестерёнки).
- **Нужна замена workflow:** новая разметка `fitflow_widget_neon.xml`.

**0.9.31:**
- Кольцо в крупной плите стояло выше середины. **Грабля:** при
  `layout_gravity="center"` отступ `marginBottom` сдвигает вьюху вверх на
  ВСЮ величину, а не на половину. Было 26 dp → уехало на 26.
- Правильный расчёт (он же в комментарии разметки): взять свободное поле
  между подписью и блоком «значение + цель», найти его середину и вычесть
  из центра плиты. Для 4×2: (142.2/2) − 59.1 = 12 dp, кольцо 70 dp.
- Сторож: тест «0.9.31 кольцо по центру между подписью и числом».
- Замена workflow не нужна — правка только в `android-res/layout/`.

**0.9.30:**
- **Бенто ужат до 4×2** без единого изменения кегля (владелец запретил
  трогать шрифты). Источники высоты, в порядке вклада: кольцо 104→72 dp,
  круглые кнопки из центра плиты в угол `top|end` (~14 dp), поля подложки
  36→14 px, промежуток плит 30→20 px. Кнопки 46→38 dp.
- Полоса прогресса — **отдельный слой** `layout_gravity="bottom"`, а не
  последняя строка текстовой колонки. Растягивается вместо неё строка
  цели (`weight=1`): если лаунчер выдаст плиту ниже расчётной, лишнее
  съест текст, а полоса сохранит свои 10 dp и не срежется.
- Кнопка питания приглушена `#FF8A5C`→`#D9714A`, размер явно равен кнопке
  воды.
- **Шестерёнка** в углу крупной плиты → `widget_action=widget_settings` →
  `switchView('settings-general')` + `openWidgetLayoutDialog()`. Новый
  ассет `design/widget_bento_ic_gear.png` (генерит `prepare-bento-bg.py`).
- **Нужна замена workflow:** `minHeight` 180→150, `targetCellHeight` 3→2,
  копирование `widget_bento_ic_gear`.
- Полный разбор «как ужать виджет по высоте» — `design/WIDGET-HOWTO.md`
  § 4 (таблица источников + арифметика) и § 8 (где ставить кнопки).
  § 13 — заготовка следующего, «стеклянного» виджета: перетекающая
  раскладка без дыр при скрытом слоте и слот-«статус» для тренировки.

**0.9.29:**
- **Бенто не добавлялся на рабочий стол** («Не удалось добавить виджет»,
  чёрный прямоугольник). Причина — `<View>` в разметке: он НЕ входит в
  белый список вьюх RemoteViews, и лаунчер не может раздуть layout. Было
  восемь штук — все распорки. Заменены на пустые `FrameLayout`.
- **Грабля на будущее (стоила полевой сборки).** Ошибка немая: сборка
  зелёная, `aapt` молчит, другие виджеты работают. Видно только с
  телефона. Белый список: FrameLayout, LinearLayout, RelativeLayout,
  GridLayout, TextView, ImageView, ImageButton, Button, ProgressBar,
  Chronometer, AnalogClock, ViewFlipper, ViewStub, ListView, GridView,
  StackView, AdapterViewFlipper. Всё остальное, включая `<View>` и
  `<Space>`, — мгновенный отказ.
- Сторож: тест «0.9.29 … белого списка RemoteViews» сканирует
  `android-res/layout/` и генерируемые CI разметки. Комментарии из
  проверки исключены. Замена workflow для этой версии не нужна.

**0.9.28:**
- **Разбор полевого скрина 0.9.27 — одна причина на пять замечаний.**
  Подложка несла кроссовок и подписи «Вода»/«Калории». PNG растягивается
  `fitXY`, overlay живёт в dp — совпасть они не могут. Лечение: подложка
  теперь только оболочка и три плиты, всё живое рисует разметка.
- Кроссовок и кольцо — в одном центрированном контейнере (мысок больше не
  наступает на дугу). Шрифт один на все плиты. «0 из ххх» единым видом
  (две строки: одной не даёт ширина рядом с кнопкой).
- **Срезанные пилюли — это была высота, а не стиль.** На 4×2 плите
  доставалось 62 dp при нужных 71 dp. Виджет стал 4×3 (`minHeight` 180),
  шкала дополнительно прижата распоркой `layout_weight=1` к низу.
- Слоты настраиваемые: состав берётся из `d.shows(...)` (те же галочки
  настроек виджета). Пустой слот скрывается целиком.
- У питания круглая кнопка с карандашом → `smart_entry`.
- **Грабли:** RemoteViews не умеет `setProgressDrawable`. Цвет шкалы обязан
  следовать показателю, поэтому шкалы и кольца наложены по одному на
  показатель и переключаются `setViewVisibility`.
- **Новое:** `design/WIDGET-HOWTO.md` — как создаётся виджет (сетка, бюджет
  высоты, ограничения RemoteViews, слоты, кнопки, CI, чек-лист).
  `tools/widget-bento-preview.py` — предпросмотр вёрстки без телефона.
- Нужна замена workflow. Ожидается полевая проверка 0.9.28.

**0.9.27:**
- **Бенто по полевому скрину 0.9.26.** Светлая кромка убрана, кроссовок меньше,
  кольцо шагов, цифры в две строки, полосы в пилюлях, круглая «+250 мл» в dp.
  Подложка готовится `tools/prepare-bento-bg.py`. Нужна замена workflow.
- **0.9.22 по полю закрыт:** вечерний вопрос не пришёл (часы уже отчитались).

**0.9.25:**
- Бенто на XML RemoteViews (карточки, кольцо, полосы). Полевой вид не совпал
  с макетом → 0.9.26.

**0.9.24:**
- Три оформления в APK. Классическая строка осталась. Ring / Rings / Dial /
  Tiles убраны. Полевой вид canvas-бенто не совпал с задумкой → 0.9.25.

**0.9.23:**
- **Свёрнутый список «Тренировки с часов» снова одна пластина.**
  На теме «Лес» свёрнутый блок читался как зелёная карточка с серой
  полосой-заголовком и ещё одним зелёным полем сверху. Причина — каскад
  CSS: одноклассные `.watch-workouts-block` / `.watch-workouts-toggle`
  (специфичность 0,1,0) проигрывали общим `.collapsible-block` /
  `.collapsible-toggle` ниже по файлу (`margin-top:12px` и серый фон).
  Голый `margin-top:0` ту же войну проиграл бы. Лечение — две класса
  (0,2,0): внутренний блок без своего отступа и рамки, заголовок
  прозрачный на зелёной пластине. Общие сворачиваемые блоки («Своё блюдо»,
  шаблоны) не затронуты.
- **Ожидается полевая проверка 0.9.23** на теме «Лес»: свёрнутый список
  с часов — одна зелёная карточка, без серой полосы и второго поля сверху.

**0.9.22 (`v0.9.22-build372`):**
- **Вечерний вопрос учитывает неподтверждённые минуты с часов.**
  `hasWorkoutToday()` смотрел только подтверждённый журнал — сессии из
  `state.healthSync.watchWorkouts` в расчёт не попадали, и вопрос «была сегодня
  активность?» приходил, хотя часы уже передали минуты. Автоимпорт «сегодня»
  ломал бы сценарий владельца (он намеренно не подтверждает записи, чтобы они
  сами записались на следующий день) — чинили условие напоминания, не импорт.
- `pendingWatchMinutesForDate` суммирует неподтверждённые минуты без дублей;
  `activityMinutesTodayWithWatch` используется **только** для напоминания
  (статистика и виджет по-прежнему показывают подтверждённые данные). Пересчёт
  зовут и приход сессии, и «Игнорировать» / «Отменить». Порог 15 минут совпадает
  с `HealthSyncReceiver`.
- **Ожидается полевая проверка 0.9.22.** Если вопрос всё ещё приходит при уже
  переданных с часов минутах — смотреть, доехала ли сессия в `watchWorkouts`
  к моменту планирования.

**Хвост 0.9.21 закрыт.** Владелец подтвердил: фактические минуты за 24-е
после установки — 89 (ожидали ~90). Формула `statsActivityMinutes` и журнал
как источник правды работают. Окно чтения — 30 дней (`WORKOUTS_HISTORY_DAYS`).

**П.5 — в APK с 0.9.24, ждёт полевой проверки.** Три JPEG владельца были брифом
визуала. Классическая строка осталась; Ring / Rings / Dial / Tiles убраны.
Новые провайдеры: `FitFlowWidgetBentoProvider`, `FitFlowWidgetNeonProvider`
+ `FitFlowWidgetPaint` (капля убрана в 0.9.47). Честный потолок — полупрозрачная
карточка; wallpaper-blur только если лаунчер рисует сам.

**0.9.21:**
- **Список «⌚ Тренировки с часов» сворачивается.** Часы пишут «тренировкой» и
  обычную ходьбу — за пару дней набегало ~15 карточек. Свёрнут по умолчанию,
  заголовок считает: «12 ждёт решения · 3 добавлено само». Состояние —
  `watchSuggestExpanded` (в памяти вкладки, не в резервной копии); переживает
  перерисовку, иначе список схлопывался бы после каждого «Добавить».
- **Грабли:** кнопки строк обязаны лежать внутри `.collapsible-content`.
  Клик по `.collapsible-toggle` сворачивает блок — кнопка внутри тогла мертва.
- **Минуты с часов не доходили до графика «Статистики».** `getStatsDays`
  доверял `dailyHistory`: сводка дня пишется в момент глотка воды/приёма пищи и
  фиксирует минуты, известные на тот момент, а сессии Health Connect доезжают
  позже. День со сводкой считался «посчитанным» — за 24-е висело 15 минут
  вместо 90. Теперь `statsActivityMinutes(date, summary)` = `Math.max(журнал,
  сводка)`; журнал `state.workouts` полный и не обрезается — он источник
  правды. Применено и в `renderStatsCompare` (сравнение периодов).
- Урок повторно подтверждён: **если число «застряло» — ищи, кто его закэшировал**,
  а не тот код, что его показывает.

**0.9.20:**
- **Дефект, а не ограничение провайдеров.** `cloudCallOpenAiCompat` игнорировал
  `options.image`: фото не уходило вообще, модель отвечала по одному тексту
  вопроса. Флаг `vision:false` у OpenRouter/GigaChat лечил симптом.
- Теперь два способа приложить снимок: OpenAI-совместимые — массив частей
  `content` с `image_url` (data-URL); GigaChat — `gigachatUploadImage`:
  `POST /files` (multipart, обязательный `purpose="general"`) → `file_id` в
  `attachments` сообщения. `Content-Type` для multipart не задавать вручную —
  boundary выставляет `FormData`.
- **Без фото `content` обязан остаться строкой** — массив частей понимают не все
  простые OpenAI-совместимые серверы. Тест это стережёт.
- `visionModels` у провайдера — запасные зрячие модели; первой пробуется модель
  пользователя. Перебор только на 400/404 (модель убрали/не видит картинки);
  401/402/429 осмысленны сами по себе — выходим сразу. `modelLabel` показывает
  ту модель, что реально ответила.
- Бесплатный список моделей OpenRouter меняется постоянно — если все кандидаты
  отпали, показывается объяснение, а не сырое «404: no endpoints».

**0.9.19:**
- `WearableExtender` в `WaterReminderReceiver.postWaterNotification` и в
  `CourseReminderReceiver.postDose`: на часах Wear OS у уведомления появляется
  «+ 250 мл» (у курса — «✓ Принял»). Действие переиспользует тот же
  `PendingIntent.getBroadcast`, поэтому приложение не открывается — вода уходит
  в prefs `fitflow_widget` и доезжает в дневник при следующем запуске.
- **Ловушка протокола:** как только список `WearableExtender.addAction()` не
  пуст, на часах видны ТОЛЬКО его действия. Нужную кнопку обязательно дублировать
  в оба списка, иначе часы останутся вообще без кнопок. Тест это стережёт.
- `setDismissalId` синхронизирует смахивание телефон ↔ часы; у курса id включает
  `notifId` (иначе смахивание одного приёма гасило бы напоминание о другом).
- **Чего сделать нельзя.** Кнопки на часах — протокол Wear OS. Amazfit/Zepp,
  Honor Band, Mi Band пересылают на часы только текст (Notification Listener),
  действия Android туда не пробрасываются, настройки для этого у них нет.
  Проверенные обходные пути и почему отклонены: сторонний компаньон «Notify for
  Amazfit & Zepp» (кнопки есть, но требует отключить Zepp — это рекомендация,
  а не функция приложения); Health Connect `HydrationRecord` (Zepp воду туда не
  пишет); мини-приложение Zepp OS с Side Service (отдельный проект вне объёма).
  Вместо обещаний — 18-й вопрос FAQ с честным объяснением и виджетом как
  рабочим путём.

**0.9.18:**
- Прогресс упражнения считается за выбранный период (7 дней / месяц / всё время)
  и сравнивается с предыдущим равным окном, а не «две последние тренировки»:
  `computeExerciseProgress`, переключатель `[data-strength-period]`.
- Показателей два сразу: лучший подход по внешнему весу и объём за тренировку
  (`formatStrengthProgress` / `formatVolumeProgress`, порог шума ±5%). Случай
  «вес больше, повторов меньше» помечается классом `mixed`, а не «ростом».
- Масса тела на дату (`bodyWeightOnDate`) участвует в объёме упражнений с
  собственным весом, но не переписывает прошлые тренировки — похудение не
  выглядит откатом. Объяснение простыми словами — в справке `«?»`
  (`HELP_TOPICS['strength-progress']`).

**0.9.15 (текущая):**
- Фоновое чтение Health Connect: разрешение
  `android.permission.health.READ_HEALTH_DATA_IN_BACKGROUND` (Android 15+),
  проверка поддержки по `SDK_INT >= 35` (не `getFeatureStatus`: его нет в
  закреплённой `connect-client:1.1.0-alpha07`), проверка выданности через
  `getGrantedPermissions`, единая строка состояния `backgroundReadStatus()` →
  `"unavailable" | "granted" | "denied"`. Мосты `getHealthBackgroundReadStatus`
  и `requestHealthBackgroundRead` в `MainActivity.java`. Блок в настройках
  скрыт, пока разрешение нельзя выдать.
- Вечерний вопрос «была сегодня активность?»: закрыты три дыры — импорт с часов
  не звал пересчёт; расписание на 14 дней вперёд не пересматривалось
  (`refreshTrainingReminderOnResume`); фоновый синк не снимал показанное
  уведомление (`readTodayExerciseMinutes` + `cancelTrainingReminderNotification`).
- Порог «день засчитан» — сумма минут за день `ACTIVITY_REMINDER_MIN_MINUTES = 15`
  вместо «есть хоть одна запись»: часы заводят «тренировкой» двухминутную ходьбу.
  Порог и алгоритм id уведомления продублированы в нативе и сверяются тестом.
- Подсказка в настройках синхронизации: задержки обычно из-за экономии батареи
  приложения часов (Zepp и аналоги отдают данные, только когда работают сами).

**0.9.14:**
- Обратная заливка тренировок с часов за 30 дней: `readWorkoutsHistory` в Kotlin
  (постранично, как шаги и сон), мост `syncHealthWorkoutsHistory`, колбэк
  `onHealthWorkoutsHistoryReceived`, запрос раз в сутки в `refreshHealthDataOnResume`.
  До этого тренировки читались только за 3 суток — аналога дозаливки для них не
  написали вовсе (разбор симметрии слоёв — в `AI_GUIDE.md`, урок 0.9.14).
- Слияние сессий вынесено в чистую функцию `mergeWatchWorkoutsBackfill` (без
  `state` и DOM), на неё переведён и ежедневный синк `onHealthWorkoutsReceived`.
  Она же покрыта поведенческими тестами — `test-ui-init.js`, блок 0.9.14.
- `syncPastDaySummary` вызывается при авто- и ручном импорте и при отмене: иначе
  тренировка за прошлый день видна в дневнике, но не в графике активности.
- Блок сравнения периодов отвязан от игрового режима, заголовок стал динамическим,
  прошлый период считает минуты так же, как текущий (`activityMinutesForDate`).
- `onHealthBodyMetricsReceived` наконец присвоен в `window` (мелочь из списка ниже
  закрыта): с 0.8.23 вес с умных весов не доезжал, натив звал несуществующий колбэк.

**0.9.13:**
- Напоминания о курсе витаминов переведены с плагина Capacitor на собственный
  `CourseReminderReceiver` + `AlarmManager` + `FitFlowCourses` (по образцу воды):
  плагин на Android **не умеет** `foreground:false`, любая его кнопка открывает
  Activity. Кнопка «✓ Принял» — броадкаст, приложение не запускается.
- Пункт `courses` в `WIDGET_ITEMS`: строку собирает натив, тап отмечает ближайший
  непринятый приём, при всех отмеченных — снимает последний. Без курса пункт
  не предлагается.
- Обратный канал `window.onCourseDosesFromNative` — идемпотентное применение
  очереди отметок (натив шлёт конечное состояние, не «переключить»).

**0.9.12 (build 355):**
- Шаги за прошлые дни: `readStepsHistory`/`readSleepHistory` читают Health
  Connect **постранично** по `pageToken`. Это и была причина, по которой в
  0.9.11 сон восстановился, а шаги нет (разбор — в разделе 7).
- Таймер отдыха: пресеты переехали в `state.strengthRest`, можно добавить своё
  время и удалить лишние долгим нажатием.
- Быстрый ввод подходов «5x10x12», термин «Повторения», отмена заполнения.

**Что дальше (согласовано с владельцем):**

| Приоритет | Задача |
|---|---|
| сейчас | П.5: полевая проверка бенто 0.9.27; капля/кольца canvas; дальше П.1 «Питание» |
| далее | П.1 «Питание» — чёткое разделение трёх сущностей (см. ниже) |
| далее | П.3 микроанимации: «набег» числа воды/ккал, подсветка изменившейся строки, мягкая галочка; 150–250 мс, уважать `prefers-reduced-motion` |
| последним | 1.0.0 и перенос в `main` |

**Три сущности «Питания»** (владелец просил развести их явно, а не объединять):
«Комбо» = фраза-сценарий; «Мои блюда» = готовые ккал; «Мои продукты» = база для
сканера. Нужна подсказка в поле создания комбо.

**Отложено отдельной задачей:** пересмотр целесообразности раздела «Мои блюда»
(`POSTPONED.md`, P36); апгрейд `connect-client` до стабильной 1.1.0 (P38 — требует
AGP 8.8.1 и compileSdk 35, у нас 8.2.1 / 34). P37 (тоннаж для упражнений с
собственным весом) выполнен в 0.9.15.

**Проверено и закрыто как не-баг:** `gap` в правилах плотности (`style.css`
4420/4425) не мёртвый — эти блоки flex, правило работает. `onHealthBodyMetricsReceived`
присвоен в `window` (`app.js`).

---

## 5. Постоянные требования владельца

Их нельзя нарушать без явной отмены владельцем:

- **Перенос в `main` — самый последний шаг проекта.** Merge PR до завершения
  всех задач не делать.
- **Офлайн по умолчанию.** Онлайн-функции — только явно включаемая опция.
  Статус онлайн/офлайн виден в верхней панели рядом со значком приложения.
- **Бета-метки** обязательны у: прогресса силовых, ИИ-рекомендаций, ИИ-статистики.
- **Функцию распознавания фото еды не удалять.**
- **Уменьшать APK только без потерь** функциональности, скорости и корректности
  данных.
- **Сканер штрих-кода — только камерой** (ручной ввод кода отвергнут), источник
  данных бесплатный и без зависимости от Google Play Services (выбран ZXing +
  Open Food Facts). Сканер пишет съеденное **сразу в текущий приём пищи**,
  подставляя полный вес продукта. Разовые покупки не засорять «Моими продуктами».
- **Тип приёма пищи можно менять в уже записанном приёме.**
- **«Тренировки» из обычной ходьбы, которые создают часы, оставлять как есть** —
  это реальная активность, пусть идёт в недельную цель.
- **Критерий «дубль по одинаковой длительности ±15 мин» отвергнут:** несколько
  тренировок в день похожей длительности — норма. Определять дубль автоматически
  по пересечению интервалов (сделано в 0.9.10).
- TalkBack — только базовые правки. Магазин — RuStore.
- В пилюлях быстрого перехода — только текст, без иконок.
- Юридическая информация — в существующий раздел лицензии в настройках.
- Правки в `build.yml` — одним разом, объединяя все изменения.

---

## 6. Технический минимум

### Версия правится в 4 местах одновременно
Иначе падает тест «версия в коде и в „О приложении“ совпадают»:
1. `version.txt` (строка 1) — workflow читает её сам;
2. `app.js` — `const FITFLOW_VERSION` (~строка 3054);
3. `index.html` — `#about-version` (~строка 1350);
4. `APP_DESCRIPTION.md` — строка 3.

### Протокол замены workflow (делает владелец, с телефона)
Пуш в `.github/workflows/` запрещён GitHub App (403), `workflow_dispatch` тоже.
Активный workflow заменяет владелец вручную по зеркалу:

1. Скопировать зеркало целиком:
   https://raw.githubusercontent.com/Help-yourself-dvp/LifeFlow-Fitness/arena/01a02da3-lifeflow-fitness/tools/github-workflows/build.yml
2. Открыть активный workflow в режиме редактирования и заменить всё содержимое:
   https://github.com/Help-yourself-dvp/LifeFlow-Fitness/edit/arena/01a02da3-lifeflow-fitness/.github/workflows/build.yml
3. Commit changes → вкладка «Actions» → дождаться сборки → APK в Releases.

Замена нужна **только если менялся нативный код или сама сборка**. Правки
JS/HTML/CSS собираются обычным релизом.

### Коммит и проверка сборки
`git add -A` захватывает `.github/workflows/build.yml` — **коммитить явным
списком файлов**. Проверка сборки:
```
gh run list --branch arena/01a02da3-lifeflow-fitness --limit 5 --json databaseId,status,conclusion,headSha
gh run watch <ID> --exit-status --interval 20
gh release list --limit 2
```
`gh run view --log` возвращает пусто, APK из песочницы не скачать — проверять
по `conclusion: success` и списку ассетов релиза.

---

## 7. Грабли, на которые уже наступали

Список короткий, но каждый пункт стоил отдельной итерации.

### 7.1. Колбэк из натива не найдётся, если функция не присвоена в `window`
Объявление `function onHealthSleepHistoryReceived(...)` внутри модульной области
недостаточно: мост зовёт `window.<name>`. Образец правильного подключения —
`app.js` рядом со строкой 8134. Каждую новую мост-функцию проверять
`grep -n "window\.onHealth.*=" app.js`.
Закрыто в 0.9.14: `onHealthBodyMetricsReceived` присвоен в `window`
(вес/рост с умных весов). Тест закрывает оба колбэка сразу. Урок тот же:
новую мост-функцию сразу класть в `window`.

### 7.2. Health Connect отдаёт данные постранично
`readRecords` возвращает до 1000 записей за страницу плюс `pageToken`. Без цикла
`do { … } while (pageToken != null)` часть данных теряется молча. Именно это
оставило шаги за 20–21 августа пустыми в 0.9.11: часы пишут шаги мелкими
кусками, лимит выбирался на старых днях. У сна записей мало, поэтому он прошёл.
**Любое новое чтение истории из HC — сразу с пагинацией.**

### 7.3. Шаги: часы и телефон нельзя складывать
`syncNow` считает их раздельно через `WEARABLE_PACKAGES` и отдаёт приоритет
часам. При правках агрегации шагов сверяться с `syncNow`
(`HealthConnectHelper.kt`, строки ~60–90).

### 7.4. Старые тесты могут кодировать неверное поведение
Падение теста — это конфликт намерений, а не «сломанный тест». Сначала выяснить,
почему правило введено, починить корень, и только потом переписывать ожидание —
с комментарием, почему прежнее было ошибочным.

### 7.5. Тест обязан падать без фикса и не падать аварийно
- Прогонять **все три** теста по exit-коду; прогон только «своего» маскирует регрессии.
- **Верификация откатом** — обязательный шаг перед коммитом:
  ```
  cp app.js /tmp/app.new
  git show HEAD:app.js > app.js
  node test-ui-init.js          # новые проверки должны показать ✗
  cp /tmp/app.new app.js
  ```
- Если функции ещё нет в `module.exports`, `require` роняет весь прогон и тест
  бесполезен. Ставить guard (`const hasApi = typeof api.foo === 'function'`).
- Жадный regex даёт ложно проходящий тест (`combo: '([^']+)'` ловил постороннее
  `combo: '⭐'`). Сначала вырезать нужный блок, потом искать внутри.
- Поведенческий тест без jsdom: вырезать тело функции срезом по `indexOf` и
  исполнить через `new Function(...deps, src + '; return fn;')`.
- В jsdom нельзя обращаться к `window.state`; стенды делать через
  `runScripts: 'dangerously'` и шим `$`.

### 7.6. Правки большого файла — только с уникальным контекстом
Замена по первому вхождению бьёт по чужому блоку. Адресоваться длинным
уникальным контекстом и проверять `assert s.count(old) == 1`.

### 7.7. Кириллица в правках
- Вставка русских комментариев иногда вносит случайные CJK-символы. После каждой
  записи проверять:
  ```
  python3 -c "import io;s=io.open('app.js',encoding='utf-8').read();print([c for c in s if '\u4e00'<=c<='\u9fff'])"
  ```
- `cat -v` и `sed` искажают кириллицу — сверять `python3` + `repr()`.
- Последовательность `*/` внутри блочного комментария закрывает его досрочно
  (`x/х/*/×` в комментарии ломала файл). Писать словами.

### 7.8. Прочее
- `app.js` грузится через `require` в чистом Node: верхнеуровневые обращения к
  `window`/`document` роняют `test-parser.js`.
- Java-методы не объявляются через `fun` — regex `/fun <name>/` по
  `MainActivity.java` даёт ложный ✗; использовать `/void <name>\(\)/`.
- Клик внутри `.collapsible-toggle` сворачивает блок — интерактивное класть в
  `.collapsible-content`.
- Видимость карточек — `state.homeLayout.visible[id]`, не `state.trackers`.
  Онлайн-флаги — `state.onlineFeatures`.
- `fetch` из WebView не задаёт `User-Agent` — для Open Food Facts использовать
  query-параметры `app_name` / `app_version` / `app_uuid`.
- `/tmp` между сессиями очищается — все проверки переносить в `test-ui-init.js`.
- Не писать имена функций по памяти — сначала `grep`.
- **Локальный HEAD дважды самопроизвольно откатывался** при сохранном рабочем
  дереве. Лечение без потери работы:
  ```
  tar czf /tmp/bk-tree.tgz --exclude=.git .
  git fetch origin arena/01a02da3-lifeflow-fitness
  git reset FETCH_HEAD          # без --hard
  ```

### 7.9. Кнопки уведомлений Capacitor всегда открывают приложение
`@capacitor/local-notifications` на Android игнорирует `foreground: false`:
в `LocalNotificationManager.createActionIntents` и contentIntent, и каждое
action-`PendingIntent` создаются через `PendingIntent.getActivity()` с
`ACTION_MAIN` + `CATEGORY_LAUNCHER`. Если нужно действие «в фоне» (подтвердить
приём, добавить воду) — только свой `BroadcastReceiver` + `AlarmManager`,
образцы: `WaterReminderReceiver.java`, `CourseReminderReceiver.java` (0.9.13).
Отметка, сделанная нативом без приложения, кладётся в очередь и доезжает в
app.js на `onResume` — доставку обязательно повторять до `window.__fitflowReady`,
иначе на холодном старте она потеряется до следующего запуска.

### 7.10. Новый нативный класс = правка workflow
До 0.9.13 шаг «Копировать нативные исходники» перечислял файлы поимённо.
Класс, добавленный в `android-native/` без правки списка, просто не попадал
в проект Capacitor, и сборка падала на «Build APK» с `cannot find symbol` —
именно так упали прогоны 0.9.13. В зеркале `tools/github-workflows/build.yml`
шаг теперь копирует все `*.java` и `*.kt` циклом, поимённо проверяются только
обязательные файлы. Пока владелец не перенёс зеркало в
`.github/workflows/build.yml`, активный workflow остаётся старым: пуш в
`.github/` запрещён GitHub App, замена — вручную по raw-ссылке.
Логи Actions из песочницы недоступны (`gh run view --log` пуст, скачивание
zip даёт 0 байт) — диагностировать по имени упавшего шага
(`gh run view <ID> --json jobs`) и перекрёстной сверке символов `grep`.

### 7.11. Отвергнутые подходы (не возвращаться)
ML Kit и `BarcodeDetector` для сканера (требуют Google Play Services); тинт
прогресса через RemoteViews на minSdk 26; негативные margin'ы в RemoteViews;
вырезание ABI x86 и armeabi-v7a; ручной ввод штрих-кода.

---

## 8. Карта кода

Точные номера строк живут в `PROJECT.md` (там же — история версий и разбор
подсистем). Здесь — только ориентиры, откуда начинать поиск.

| Что | Где |
|---|---|
| Версия, дефолты состояния | `app.js` ~3050 (`FITFLOW_VERSION`), дефолт `state` рядом |
| Карточки Главной | `HOME_CARDS` ~3004, `applyHomeLayout` ~6950 |
| Виджет | `WIDGET_ITEMS` ~3110, `updateNativeWidget` ~5473, `android-native/FitFlowWidget*.java` |
| Питание | `calcNutrition` ~2509, «Мои блюда» — обработчики `#quick-meal-*` ~15430, комбо ~9007–9160 |
| Сканер | `parseOffProduct` 2555 → `lookupOffByBarcode` 2636 → `startBarcodeScan` 2731 |
| Health Connect (JS) | `mergeStepsBackfill` / `mergeSleepBackfill`, колбэки ~8100–8140 |
| Health Connect (натив) | `android-native/HealthConnectHelper.kt`, мосты в `MainActivity.java` |
| Силовой дневник | `app.js` ~10400–10600: `strengthDraft`, `renderStrengthDiary`, `saveStrengthSession` |
| Таймер отдыха | `normalizeStrengthRest` / `renderStrengthRestTimer` ~10130–10250 |
| Витамины-курсы | `scheduleCourseReminders` ~4385, листенер ~11035 |
| Обработчики UI | один общий блок делегирования, `app.js` ~14080–14300 |

**Сохранение состояния:** `saveState()` → пересчёт сводки → localStorage →
`updateNativeWidget()` → `scheduleSqliteSync()`.

**Эталон записи еды** (копировать порядок финализации): `saveState()` →
`resetMealTypeAfterSave()` → `renderFood()` → `await syncMealRemindersForToday()`
→ `toast()`.

---

## 9. Документация проекта

| Файл | Назначение |
|---|---|
| `SESSION_HANDOFF.md` | этот файл — точка входа для нового агента |
| `AI_GUIDE.md` | правила работы с владельцем и с кодом |
| `PROJECT.md` | устройство подсистем + история версий |
| `ROADMAP.md` | что дальше по этапам |
| `CHANGELOG.md` | релизы человеческим языком |
| `APP_DESCRIPTION.md` | описание для магазина |
| `POSTPONED.md` | отложенные идеи и провалившиеся подходы |
| `OPEN-ISSUES.md` | найденные дефекты и долги, ждущие решения владельца |
| `ISSUES.md` | полевые случаи распознавания еды |
| `DISTRIBUTION.md` / `PRIVACY.md` / `THIRD_PARTY.md` / `DATA_SOURCES.md` | публикация и правовое |
| `COMMAND_DICTIONARY.md` | словарь команд умного ввода |
| `design/WIDGET-HOWTO.md` | как создаётся виджет: сетка, бюджет высоты, слоты, CI, чек-лист |
| ~~`design/WIDGET-P5.md`, `widget-p5-*.png`~~ | удалены в 0.9.48 вместе с виджетом «капля» (0.9.47) |
| `design/wp-dawn.jpg` / `wp-night.jpg` | живые обои макетов (JPEG; PNG-оригиналы не в git) |
| `design/ref-{light,bento,rings}.jpg` | три JPEG владельца — бриф визуала, не вклейка |
| `tools/widget-preview-p5.py` | генератор PNG п.5 (Pillow, шрифт Manrope) |
| `tools/widget-bento-preview.py` | предпросмотр вёрстки бенто без телефона |
