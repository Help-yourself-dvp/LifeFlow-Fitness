# Передача разработки FitFlow

Этот файл нужен только на случай, если текущий диалог Arena нельзя продолжить.

## Что передать новому агенту

1. Открыть и прочитать `AI_GUIDE.md`, `PROJECT.md` и этот файл.
2. Выполнить `node test-parser.js` (87 сценариев, ожидается `ALL TESTS PASSED`) и `node test-ui-init.js` (ожидается `UI INIT CHECK PASSED`).
3. Сначала проверить `git status` и `git log --oneline`.
4. Не выполнять Merge PR до завершения всех задач пользователя.
5. Номер версии приложения живёт в `version.txt` в корне репозитория; workflow сборки читает его сам (с 0.2.5). Чтобы выпустить новую версию, не нужно править build.yml — только `version.txt` и код.

## Текущее состояние

- Версия приложения: **0.2.8**.
- При выпуске версии обновлять три места: `version.txt` (версия сборки в CI), `package.json`, константу `FITFLOW_VERSION` в app.js (она показывается в «О приложении»).
- Ветка сессии: `arena/019fd0d2-lifeflow-fitness` (продолжение линии сессий `arena/019fc7ce...` → `arena/019fbe3c...`).
- Приложение offline-first: HTML/CSS/Vanilla JS + Capacitor Android.
- Данные хранятся локально, серверов нет.
- Реализованы вода, питание, парсер (база 925+ продуктов, источники в `DATA_SOURCES.md`), резервные копии, активность, шаблоны активности, вечерний вопрос об активности, утренняя мотивация, свои и избранные блюда, история итогов дня, статистика Сегодня / 7 / 30 дней, компактный Android-виджет, подробные экраны воды и питания, история веса с графиком, локальные профили (до 10), чек-лист дня с настроением 1–5, ИИ-центр с вкладками и режим «Локальный эксперт FitFlow (0 ГБ)», выбор локального файла модели, честный бенчмарк, голосовой ввод через нативный RecognizerIntent.
- 0.2.5: FAB «＋ Запись» больше не перекрывает контент (резервный отступ `#training-view`), защита инпутов от обрезания (`min-width: 0`, `flex-shrink: 0`), защита настроек от длинных строк (`overflow-wrap: anywhere` в `.settings-hint`), единый формат дат (шапка `5 авг. 2026` + полный aria-label), workflow с авто-версией из `version.txt`.
- 0.2.6: ИИ-центр больше не выходит за экран (перенос вкладок `.segmented`, `min-width: 0` у диалога); автотип приёма пищи по времени (Завтрак 06–13, Обед 13–16, Полдник 16–18, Ужин 18–22, Поздний перекус 22–06; ручной выбора до сохранения); сворачиваемые шевроном разделы Настроек (`fitflow:settings-accordion`); версия в «О приложении» из `FITFLOW_VERSION`; короткий плейсхолдер в поле Питания; 99 тестов.
- 0.2.7: самочувствие теперь с датой (не зависает на следующие дни, старые без даты очищены); микрофоны в шапках карточек Вода/Питание как SVG-кнопки; Вода — свой объём «Своя мл» (10–2000); тип приёма пищи свёрнут в компактную строку «Приём пищи: Обед ·авто» + «Изменить»; восстановлена панель «📷 Фото еды» (была потеряна из разметки; демо-состав удалён — честный режим); SVG-шевроны вместо «⌄»; фикс выглядывания текста в свёрнутых блоках и выезда полей «Своё блюдо».

## Важно про GitHub

- Рабочая ветка этой сессии: `arena/019fd0d2-lifeflow-fitness`. Не переключать ветку.
- Пуш изменений в `.github/workflows/` отклоняется GitHub App без permission `workflows` — активный workflow заменяет пользователь вручную по raw-ссылке на `tools/github-workflows/build.yml` (протокол с SHA-256 и размером описан в AI_GUIDE.md).
- После перехода на `version.txt` ручная замена workflow нужна только при нативных изменениях (Gradle, плагины, MainActivity, виджет), а не ради номера версии.

## Актуальный процесс релиза (с 0.2.5)

**Обычная версия (код/HTML/CSS/JS, а также смена номера версии):**
1. Агент правит код, обновляет `version.txt` + `package.json` + `FITFLOW_VERSION` в app.js, коммитит и пушит в рабочую ветку.
2. GitHub Actions сам читает `version.txt`, собирает `FitFlow-v<версия>-debug.apk` и публикует релиз. Пользователю ничего менять не нужно.
3. Агент проверяет успешный run через `gh` и присылает готовые ссылки:
   - APK: `https://github.com/Help-yourself-dvp/LifeFlow-Fitness/releases/download/v<версия>-build<N>/FitFlow-v<версия>-debug.apk`
   - страница релиза: `.../releases/tag/v<версия>-build<N>`

**Нативная версия (Gradle-зависимости, Capacitor-плагины, MainActivity, виджет, иконки):**
1. Агент коммитит код + обновлённую копию `tools/github-workflows/build.yml`, но активный workflow не трогает.
2. Агент присылает шаблон ответа (см. ниже); пользователь вручную заменяет `.github/workflows/build.yml` содержимым raw-файла и коммитит в рабочую ветку (Commit message любое, подходит стандартное `Update build.yml`).
3. Замена сама запускает сборку; агент проверяет SHA-256/размер и даёт ссылки на APK.

**Шаблон ответа при изменении build.yml:**

> **Финальная тестовая APK <версия>**
> Подготовленный workflow содержит все доработки версии <версия>. Активный workflow на GitHub пока содержит <предыдущая версия>, поэтому нужно вручную заменить workflow ещё один раз.
> **Готовый файл:** [raw.githubusercontent.com/.../tools/github-workflows/build.yml]
> **Редактор active workflow:** [github.com/.../edit/arena/<ветка>/.github/workflows/build.yml]
> Порядок: `В raw-файле: Ctrl + A → Ctrl + C` `В редакторе: Ctrl + A → Delete → Ctrl + V`
> Перед коммитом проверьте: `Ветка: arena/<ветка>` `Ключевые маркеры новой версии в файле`
> Не выбирайте `main` и не нажимайте **Merge pull request**.
> Контрольные данные: `SHA-256: <hash>` `Размер: <байт>`
> После ручного коммита напишите «готово» — агент проверит сборку и даст точную ссылку на APK.

## Утверждённая дорожная карта (обновлена 05.08.2026, приоритеты подтверждены пользователем)

Этап A — стабильность и UX (только CSS/JS):
1. **0.2.5** — визуальные исправления аудита + авто-версия workflow — ВЫПОЛНЕНО (build 104).
2. **0.2.6** — вылезание ИИ-центра за экран, автотип приёма пищи, шевроны-аккордеоны, живая версия в «О приложении» — ВЫПОЛНЕНО (build 105).
3. **0.2.7** — 8 замечаний пользователя: баг самочувствия, микрофоны в шапок карточек (SVG), своя порция воды, компактный тип приёма пищи, восстановление вкладки «Фото еды», SVG-шевроны — ВЫПОЛНЕНО (build 106).
4. **0.2.8** — стили `.form-input` (конец «90-х»), компактные кнопки ИИ-центра, финальный фикс свёрнутых блоков, понятная валидация «Своё блюдо», вода 2×2, пояснение самочувствия, голос во вкладке «Фото еды», актуализация документов — ВЫПОЛНЕНО.
5. **0.2.9** — реструктуризация Настроек (схема В, утверждена пользователем): экран-меню из 6 строк → подэкраны «Профиль и цели», «Уведомления», «ИИ-помощник», «Данные и копии», «О приложении» (по образцу экрана «Настроить уведомления»); «Оформление и чек-лист» остаётся на первом уровне.
6. **0.3.0** — игровая форма (запрошена пользователем, включается/выключается в Настройках): список заданий на день, отметки выполнения, медали/значки за личные рекорды, сравнение прогресса с прошлым периодом в Статистике («на этой неделе вы эффективнее на N% по параметрам…» + мотивирующая фраза). Строится на dailyHistory и чек-листе.
7. **0.3.1** — чек-ин сна: утренняя оценка в 1 тап (😫/🙂/🚀), опционально время сна, пункт чек-листа «Сон», корреляции в статистике.

Этап B — ввод и удержание:
6. **0.3.0** — мини-онбординг первого запуска (3 экрана после окна условий: офлайн/приватность, голос+парсер, профили). Обязательно: кнопка «Повторить обучение» в Настройках, чтобы пройти позже.
7. **0.3.1** — раздел «Методика расчётов» внутри «О приложении»: честное простое описание логики (парсер и база продуктов, MET-расчёт активности, как работает Локальный эксперт, справочный характер данных). Без сравнений с другими приложениями и без запугиваний.
8. **0.3.2** — уровень 2 голосового ввода (подтверждение распознанного, мульти-запись из одной фразы).
9. **0.3.3 (нативная)** — автоматическая резервная копия: пользователь один раз выбирает/создаёт файл через SAF (`ACTION_CREATE_DOCUMENT` + `takePersistableUriPermission`), далее при входе раз в N дней (3/7/14, выбор в Настройках) приложение перезаписывает тот же файл и показывает внизу экрана всплывающее уведомление (toast) «Резервная копия обновлена» — **не молча**; в Настройках рядом отображается текущее расположение файла копии с кнопкой «Изменить расположение»; при неудаче — мягкое предупреждение. Заодно правки виджета: векторная иконка кнопки «Записать» вместо эмодзи 📝 и `maxLines=1`/`ellipsize=end` в RemoteViews.
10. **0.3.4 (нативная)** — сканер штрихкодов (@capacitor/barcode-scanner) + личная база «штрихкод → своё блюдо» (100% офлайн: первый раз привязка, дальше мгновенное добавление).

Этап C — ИИ:
11. **0.4.0** — Local Expert 2.0: корреляции сна/настроения/питания, недельный дайджест.
12. **0.4.x** — нативный нейроинференс через Google MediaPipe GenAI (`com.google.mediapipe:tasks-genai`, модели Gemma/Qwen .task/.litertlm) — большая отдельная версия, только после Этапа B.

Этап D — данные:
13. **0.5.0** — миграция localStorage → SQLite строго последней; перед ней — тонкий слой абстракции хранилища в app.js, потом миграционное тестирование и решение пользователя о завершении проекта.

Health Connect, распознавание еды по фото без подтверждения, CSV/PDF-экспорт — после завершения этого списка.

## Реальная проверка APK позже

- тест уведомления через 5 секунд;
- утренняя фраза;
- вечерний вопрос при заблокированном экране;
- оба типа уведомлений после перезагрузки телефона;
- проверка настроек батареи/автозапуска на Honor, Xiaomi, Huawei, Samsung;
- FAB «＋ Запись» при прокрутке экрана Активности до конца (с 0.2.5).
