# ISSUES — журнал проблем распознавания блюд

> Назначение: собирать полевые случаи распознавания текста и фото, проверять их
> против текущего парсера и вести план доработок. Пополняется выгрузкой из
> «журнала распознаваний» (Настройки → ✨ ИИ-помощник → выгрузка в буфер).
> Статус проверяется на актуальной версии приложения (см. `version.txt`).

**Важно для нового агента.** Это НЕ общий трекер задач проекта, а узкий журнал
качества распознавания текста и фото. Текущие задачи и приоритеты — в
`SESSION_HANDOFF.md` (# 4) и `ROADMAP.md`; отложенное — в `POSTPONED.md`;
найденные технические дефекты, ждущие решения, — в `OPEN-ISSUES.md`.
Последняя массовая сверка выгрузки — 10–11.08.2026; с тех пор парсер правился
точечно, поэтому перед новой сверкой прогонять `node test-parser.js` и
`node test-food-db.js`, а не полагаться на записанные ниже оценки.

---

## Что значит «записано ✓ / не записано»

«Журнал распознаваний» пишет **каждый** разбор умного ввода и фото — даже тот,
который пользователь только посмотрел в превью и не сохранил:

- **«записано ✓»** — разбор подтверждён и внесён в дневник (нажата «Сохранить записи»).
- **«не записано»** — разбор был только показан в превью, но НЕ внесён в дневник.

Это не ошибка и не потеря данных: записи 21–29 ниже — тестовые пробы одной и той же
фразы за пару минут (10.08 12:19–13:35), их просто не сохраняли. Пометка честно
фиксирует факт «показано, но не записано». Если формулировка смущает — можно
заменить на «превью · не сохранено» (см. план, п.8).

---

## Выгрузка 10–11.08.2026 — разбор каждого пункта

Проверено на текущем парсере (прогон `parseSmartEntry`). «Было» — как в выгрузке,
«Сейчас» — как ведёт себя актуальный код.

### 16. «2 тарталетки с черной икрой и творожным сыром, бутерброд с красной рыбой и маслом, бетрброд с маслом и красной икрой»
- Сейчас: `тарталетка с творожным сыром 2 шт (170)`; `бутерброд с красной рыбой и маслом (157)`; `икра красная (249, 100 г)`; ⚠️ `бетрброд с маслом`.
- Оценка: 3 дефекта.
  1. **Чёрная икра потеряна** — ключа «тарталетка с ЧЁРНОЙ икрой и творожным сыром» нет (есть только с красной) → разбор свёл к «тарталетке с творожным сыром».
  2. **«икра красная» вылезла отдельно** — из второй половины («бетрброд с маслом и красной икрой»): когда бутерброд не распознан, икра отделилась как самостоятельные 100 г вместо начинки.
  3. **Опечатка «бетрброд»** не подсказана (честно ушла в «не разобрал», но часть фразы потеряна).
- Количество «2» теперь подхватывается корректно (в старой выгрузке было 48 г).

### 17. «100 мл воды» → вода +100 мл ✓
- Оценка: корректно, сохранено.

### 18. «Рыбная котлета одна штука, картофельное пюре с тушёнкой, бутерброд с варёной колбасой, имбирь маринованный 30 г, ира сельди 30 гр, силовая тренировка 40 минут, стакан компота»
- Сейчас: `рыбные котлеты 1 шт (170)`; `картофельное пюре (106)`; `бутерброд с вареная колбаса (119)`; `имбирь 30 г (24)`; `сельдь 30 г (48)`; активность ×1; `компот 250 г (145)`.
- Оценка: 3 дефекта.
  1. **«пюре с тушёнкой» — тушёнка потеряна** (компаньон не применяется к пюре).
  2. **«ира сельди» (опечатка) → «сельдь»** вместо «икры сельди»: база содержит «икра сельди» (проверено: «икра сельди 30 г» → 66 ккал), но опечатка «ира» отбрасывает «икру», и остаётся только рыба. Нужна терпимость к опечаткам.
  3. **Грамматика** «с варёной колбасой» → «с вареная колбаса» (падеж не согласуется) — косметика.
- «одна штука» теперь разбирается в «1 шт» (в старой выгрузке было «100 г»).

### 19. фото «выпечка 1 шт, розовый десерт 1 шт, булка 1 шт» → «булочка 0 г (295)»
- Сейчас: `булочка 1 шт (295)` ✓ — дефект «0 г» уже исправлен.
- Осталось: «выпечка», «розовый десерт» — общих имён нет в базе → честно «не разобрал». Это пополнение базы, а не баг.

### 20. «Тарелка гречки с тушёнкой, 2 котлеты, овощной салат, 1 кусок черного хлеба»
- Сейчас: `гречки 1 тарелка (783)`; `тушёнка (214)`; `котлеты 2 шт (294)`; `салат (55)`; `чёрный хлеб 1 кусок (80)`.
- Оценка: **1 существенный дефект** — «тарелка гречки» посчитана по СУХОЙ гречке (313 ккал/100 г → 250 г = 783 ккал), а должна быть варёная (~110 ккал/100 г → ~275 ккал). Завышение в ~3 раза. Сравните: «гречка с тушёнкой» → «гречка вареная (110)». Остальное корректно.

### 21–22. «Гречка с тушёнкой» → «гречка вареная (110); тушёнка (214)» ✓
- Оценка: корректно. Дубли — тестовые пробы (не записано).

### 23. «Чай с сахаром» → «чай с сахаром (40)» ✓
### 24. «Чай без сахара» → «чай (2)» ✓ (негация «без» работает)
### 25–26. «Куриные крылья в кляре [2 шт]» → «куриные крылья (300 / 420 за 2 шт)» ✓ («в кляре» учтено)
### 27. «Бутерброд с вареной колбасой» → «бутерброд с вареная колбаса (119)» — значение верно, грамматика названия косметически кривая
### 28. «Бутерброд с ва» (недописанный ввод) → «бутерброд (130)» — корректно для обрезанного ввода
### 29. «Овсяное печенье 2 шт» → «овсяное печенье 2 шт (135)» ✓

---

## Подтверждённые баги (проверено на актуальной версии)

| # | Баг | Пример | Приоритет | Статус |
|---|---|---|---|---|
| 1 | «тарелка/порция гречки» считается по СУХОЙ гречке (313) вместо варёной (110) | «тарелка гречки» → 783 ккал вместо ~275 | Высокий (завышение ×3) | ✅ исправлено в 0.7.16 |
| 2 | «пюре с тушёнкой» теряет тушёнку | → «картофельное пюре (106)» без тушёнки | Средний | ✅ исправлено в 0.7.16 |
| 3 | «тарталетка с чёрной икрой…» теряет чёрную икру (ключ есть только для красной) | → «тарталетка с творожным сыром» | Средний | ✅ исправлено в 0.7.16 |
| 4 | Опечатки в продуктах («ира»→«икра», «бетрброд») не подсказываются → часть фразы пропадает или резолвится неверно | «ира сельди» → «сельдь»; «икра красная» вылезает отдельно | Средний | ✅ исправлено в 0.7.16 |
| 5 | Грамматика названий: «с варёной колбасой» → «с вареная колбаса» (не согласуется падеж) | пункт 18, 27 | Низкий (косметика) | ✅ исправлено в 0.7.16 |
| 6 | Общие имена в базе отсутствуют («выпечка», «розовый десерт») | пункт 19 | Низкий (пополнение базы) | ⚠️ частично: «выпечка» добавлена (средняя оценка); «розовый десерт» НЕ добавлен — нет честной калорийности (принцип «правдивость — приоритет №1»), нужен реальный состав продукта |

## Уже исправлено с момента выгрузки

- «булка 1 шт» больше не даёт «0 г» → теперь «1 шт (295 ккал)».
- «Рыбная котлета одна штука» → «1 шт» (было «100 г» по умолчанию).
- Количество «2 тарталетки» подхватывается (было «48 г» без количества).
- **0.7.16**: «тарелка/порция гречки» = варёная гречка (275 ккал, не 783); «пюре с тушёнкой» не теряет тушёнку; «тарталетка с чёрной икрой и творожным сыром» — отдельный ключ (100 ккал); опечатки «ира сельди»/«бетрброд» исправляются; «выпечка» в базе; падеж начинки бутерброда («с вареной колбасой»).

## План доработок (порядок = приоритет)

1. ✅ ~~Сухая → варёная для «гречки» в порционном контексте~~ — сделано в 0.7.16.
2. ✅ ~~«пюре с тушёнкой»~~ — сделано в 0.7.16.
3. ✅ ~~«тарталетка с чёрной икрой и творожным сыром»~~ — сделано в 0.7.16.
4. ✅ ~~Терпимость к опечаткам~~ — сделано в 0.7.16 (частично: самые частые опечатки; карту расширяем по мере полевых находок).
5. ✅ ~~Согласование падежа~~ — сделано в 0.7.16.
6. ⚠️ **Пополнение базы**: «выпечка» добавлена; «розовый десерт» и похожие конкретные десерты — ждут реального состава продукта (не выдумываем калории).
7. (опционально, UX) Заменить подпись «не записано» → «превью · не сохранено», если формулировка смущает.

> Каждый пункт — отдельной небольшой версией (правило AI_GUIDE). Перед правкой —
> node-прогон фразы целиком, после — автотест-регресс в `test-parser.js`.
