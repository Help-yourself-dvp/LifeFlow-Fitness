# POSTPONED — отложенные цели и провалившиеся подходы

Файл честных «не получилось». Цель — не наступать дважды на одни грабли и не
терять накопленные факты: для каждой задачи указано, ЧЕГО хотим достичь,
КАКИЕ способы уже проверены (и почему не работают), и какие идеи остались.
Пополняется при каждом финальном «не вышло» (заведён 07.08.2026, просьба
пользователя). Смежные кейс-уроки — в SESSION_HANDOFF.md.

---

## P9. Расчистка раздела «Настройки → ИИ-помощник»

**Кейс пользователя (0.3.33):** раздел стал очень длинным — «много лишней
информации, пока можно оставить, так как решение ещё не реализовано, но потом
оставить только основные функции и убрать всё лишнее и относящееся к тестам».

**Что оставить после внедрения litert (ориентир):** переключатель ИИ,
выбор режима (Локальный эксперт / Gemma на устройстве), выбор файла модели
и её статус, одна проверка работоспособности. **Что убрать/свернуть:**
облачный тест-режим с пятью провайдерами и пояснениями (🧪), бенчмарк,
учебные абзацы — в справку «?» или вовсе. **Когда:** когда «Gemma на
устройстве» реально отвечает в чате (держим шаги LOCAL_AI → настройка →
проверка офлайн), иначе расчистка лишит способа тестировать сценарии,
которые пока проверяются через облако.

**Сделано preemptively в 0.3.33:** убрана вторая вертикальная полоса слева
(динозавр раннего дизайна) — раздел выровнен с остальными настройками.

## P6. Голосовой ввод 2.0 — доработки системного распознавателя

**Статус (07.08.2026):** функция работает (нативный RecognizerIntent-мост),
но пользователь тестировать не будет — встроенный распознаватель в GBoard
покрывает сценарий удобнее. Оставлено как запасной вариант для устройств
без клавиатуры с микрофоном.

**Претензия пользователя:** малейшая пауза — распознаватель заканчивает
слушать, говорить нужно быстро.

**Статус-обновление (07.08, 0.3.26):** пользователь попросил делать
параллельно → правки толерантности к паузам оформлены инструкцией
`tools/github-workflows/VOICE_PAUSE_PATCH.md` (применяет пользователь в
build.yml на GitHub — как IME/BACK-патчи до этого). Найдено честное
ограничение: потоковые частичные результаты через RecognizerIntent
НЕДОСТУПНЫ — для них нужен переход на SpeechRecognizer API (оставлено
как следующая ступень, если голос вернётся в приоритет).

**Путь доработки, всё нативно — правка build.yml/MainActivity:**
- `EXTRA_SPEECH_INPUT_COMPLETE_SILENCE_LENGTH_MILLIS` /
  `EXTRA_SPEECH_INPUT_POSSIBLY_COMPLETE_SILENCE_LENGTH_MILLIS` — увеличить
  толерантность к паузам (прямой ответ на претензию);
- `EXTRA_SPEECH_INPUT_MINIMUM_LENGTH_MILLIS` — держать сессию дольше;
- `EXTRA_PARTIAL_RESULTS` — потоковые частичные результаты (видно текст
  на ходу, понятно, что слышит распознаватель).
Своя модель распознавания (Vosk и т.п.) НЕ рассматривается: вес и сложность
не оправданы запасным сценарием.

---

## P8. «Мой курс» — расписание приёмов (витамины/добавки/лекарства)

**Цель (идея пользователя, 07.08):** отслеживать по расписанию то, что пьёшь
регулярно — спортивные добавки (креатин, БЦА, протеин), витамины, а при
необходимости и лекарства; напоминать и связывать с водой/питанием. Имя —
НЕ «лекарства»: аудитория — спорт и здоровье («Мой курс», «Добавки и курс»).

**Честная оценка смысла (гард не-нагромождения):** ДА — только как малый
модуль поверх ГОТОВЫХ механизмов: пункты чек-листа с типом «курс»
(количество в день, время напоминания через существующую систему
уведомлений, счётчик курса «день 12 из 30»). НЕТ — как отдельный раздел с
двойником Medisafe: это новый контур ведения, перегруз и отток от ядра.
Корреляция с водой/питанием — добавить позже в экспорт фактов (эксперт).

**Статус: РЕАЛИЗОВАНО в 0.3.30** (решение пользователя 07.08 — «давай добавим, с отдельной настройкой и напоминаниями»). Сделано малым модулем, как и оценено выше: настройка в Настройках (название, приёмов в день 1–4 с временем каждого, длительность дней, напоминания вкл/выкл), строки с отметками — в «Плане дня», напоминания — существующий планировщик (канал fitflow_course_reminders_v1, id 60000+). Схем приёма нет — FitFlow не врач. Дальше (если попросит): связь курса с водой/питанием в пакете фактов эксперта.

---

## P7. Штрих-код: полная офлайн-база и сканер камерой

**Цель:** сканировать штрих-код с упаковки камерой и получать точные
калории/БЖУ именно этого продукта (приоритет правдивости).

**Что НЕ работает (проверено проектированием 0.3.27):**
- **Полная офлайн-база штрих-кодов в APK** — нереализуема честно: Open Food
  Facts держит миллионы позиций (гигабайты), пополняется ежедневно, а RU-
  покрытие неполное даже онлайн. Паковать устаревающий срез = ввести
  пользователя в заблуждение. Так НЕ делает ни один аналог: FatSecret/
  YAZIO ходят в свои облачные базы, MFP — крауд.
- **Своя закрытая RU-база** — платная лицензионная работа, не наш масштаб.

**Что сделано (0.3.27):** ручной ввод штрих-кода → онлайн OFF API →
подстановка в форму «Мои продукты» → офлайн навсегда (личная база = честный
кэш с бэкапом). Атрибуция ODbL на месте.
**Статус (0.3.28, решение пользователя — карта контуров):** строка СКРЫТА
из диалога (не путать тесты), функции и флаг `onlineFeatures.barcodeLookup:
false` остаются готовой основой; возвращаемся только после локального ИИ.

**Путь сканера камерой (ждёт решения пользователя), нативно — build.yml:**
1) `uses-permission android.permission.CAMERA` (+ uses-feature camera,
   required=false);
2) в MainActivity `WebChromeClient.onPermissionRequest` → `request.grant(...)`
   для RESOURCE_VIDEO_CAPTURE (иначе getUserMedia в WebView молчит);
3) сканер: ZXing-browser (файл ~0.5 МБ в APK, офлайн) или нативный ML Kit
   (градл-зависимость, точнее, но тянет Google Maven).
Связать: отсканированный код → готовый lookupOffByBarcode() → форма.

---

## P1. Поля Питания и «Своя мл» — панель с кнопками над клавиатурой

**Цель:** при открытой клавиатуре поле и его кнопки («Добавить», «✓») должны
быть видны над ней, чтобы не скрывать клавиатуру для отправки.

**Провалившиеся подходы:**
- **Мгновенный IME-док** (0.3.18, build 136): перенос контейнера в
  fixed-панель в тик `focusin`. Результат: подъём клавиатуры гасится,
  нужен второй тап. Причина: IME-запрос Chromium инициируется после focusin,
  перенос ноды в этот момент его обрывает.
- **Отложенный IME-док** (0.3.19, build 137): перенос по факту просадки
  visualViewport (клавиатура открыта). Результат: клавиатура открывается и
  сразу прячется; в «Своей мл» поле «уезжает» спустя секунду. Причина:
  перенос ноды при активной IME-сессии её рвёт (фокус сохраняется,
  IME-сессия — нет).

**Вывод (0.3.20):** ноды пользовательских полей НЕ двигаем — никогда.
Возвращено стабильное поведение: поле на месте, `adjustResize` сам
докручивает его в видимую зону. Авто-док оставлен только для полей ИИ
(там фокус программный — «сжигатель мёртвой первой IME-сессии»), и во всех
текстовых вкладках ИИ это подтверждённо удобно.

**Активная проба (0.3.21):** `scrollIntoView` по факту просадки viewport
(без переноса ноды, один раз за сессию фокуса, для `#food-input` и
`#water-custom-ml`). **Вердикт пользователя (07.08, build 139): «отлично» —
проба принята.** Часть цели «не скрывать клавиатуру для отправки» остаётся
открытой мягко (кнопка «Добавить» прямо под полем); окончательная «панель
над клавиатурой» без переноса ноды — только нативным путём, отложено за
основной функционал.

---

## P2. Программный подъём клавиатуры при открытии ИИ-центра

**Цель:** вход в ИИ → клавиатура уже поднята (тап меньше).

**Провалившиеся подходы:**
- **blur/focus-ретрай через 450 мс** (0.3.18, build 135–136): неэффективен —
  Chromium принципиально не показывает клавиатуру по программному фокусу
  без пользовательского жеста над полем. Код удалён.
- **requestFocus-прогрев WebView нативно** (патч build.yml, build 135):
  симптом не снял (нужен именно жест пользователя).

**Что достигнуто вместо:** автофокус поля в пользовательском жесте «сжигает»
мёртвую первую IME-сессию → реальный тап пользователя уже работает
(клавиатура + голос с первого раза, build 136 подтверждён). Пользователь
принял такой сценарий как устроивающий. Финальный подъём появится только с
нативным вызовом `InputMethodManager.showSoftInput` по таймеру после
открытия экрана — отдельным нативным патчем (не планируется, сценарий устраивает).

---

## P3. «Мёртвая» первая IME-сессия после холодного старта (кейс GBoard)

**Цель (изначальная):** убрать «клавиатура со 2-го тапа + молчащая диктовка»
в ИИ после холодного старта.

**Провалившиеся подходы (каждый — отдельный релиз, симптом битово идентичен):**
плавные скроллы по таймеру; отключение скролл-компаньона флагом;
перевод ИИ из модального диалога в обычный экран (0.3.16);
нативный `adjustResize` + `postDelayed(requestFocus, 500)` (build 135);
прогрев layout скрытых экранов (0.3.18); blur/focus-ретрай (0.3.18).

**Вывод:** это поведение связки Chromium-WebView × GBoard, не наш код.
Финальная модель (подтверждена пробами 135–137): первая IME-сессия после
холодного старта обречена, вторая+ — живые; автофокус «сжигает» первую —
рабочая точка стабильна. Тема ЗАКРЫТА.

---

## P4. Собственный шрифт для тем оформления

**Цель:** у палитр разный визуальный характер шрифта (напр., округлый для
🍇 Винограда).

**Почему отложено:** локальный файл в APK = изменение `build.yml`
(ручные правки пользователя) + скачанные woff2/ttf; из песочницы загрузка
заблокирована. Решение задокументировано, ждёт слот после основного
функционала.

**Следующий шаг (по желанию пользователя):** инструкция
`tools/github-workflows/FONT_PATCH.md` (Comfortaa, SIL OFL 1.1, оффлайн-first
сохранён, fallback при отсутствии файла — системный шрифт).

---

## P5. Порционные дефолты вместо «100 г по умолчанию»

**Цель:** меньше «≈100 г» там, где бытовая порция очевидно другая.

**Что уже сделано:** штучные продукты — `per: 'шт'` с точным ккал/шт
(сосиска, бутерброд, глазированный сырок…); хлебное семейство — веса долей
(кусок 40 г / ломтик 30 г, 0.3.19); ёмкости — `UNIT_MAP` (стакан 250 мл,
тарелка 200 г……). Честный принцип: без веса → ≈100 г и эта оценка всегда
видна пользователю.

**Отложено:** порционные дефолты по КАТЕГОРИЯМ (тарелка супа 250–300 мл,
порция гарнира 180–200 г…) — требует курируемой таблицы и согласования;
риск «молчаливого завышения» важнее, чем удобство.
**Снимет проблему системно:** сценарий «Фото еды» (ИИ оценивает вес по
снимку) — дорожная карта.

---

## P10 — Фото-распознавание еды нейросетью на устройстве (целевой сценарий ТЗ)

**Статус: ✅ РЕАЛИЗОВАНО (0.4.0), итерация 2 по полевому тесту — 0.4.1.**
Полевой тест 0.4.0 закрыл первым же замечанием: камера у ИИ-поля жила на
тост-заглушке, этапы мелькали 2,4-с тостами, кнопка умного ввода не
поднимала модель из холода → 0.4.1: единый движок для всех трёх камер,
этапы/ошибки в панели на экране, итог — редактируемое окно подтверждения
с пометкой-провенансом (прямая просьба пользователя), фоллбэк-ловушка
«\b с кириллицей» в честном отказе закрыта. Итерация 3 — 0.4.2 (полевой тест на фото «бананы+кальмар+чупа-чупс»):
❶ галерея открыта (capture убран); ❷ «фото без еды» — промпт/температура 0.2/
умный детектор; ❸ веса — зрение считает штуки, весит база («5 бананов =
525 ккал» вместо «2000 г = 2100»); ❹ база пополнена (вяленый кальмар,
чупа-чупс). ## P11 — Бутербродная композиция: третья начинка через «и» (0.4.7, полевой кейс) — ✅ ЗАКРЫТА 0.4.9

**Статус:** решена в 0.4.9, прогонами в test-parser до релиза. Кейс пользователя (голос): «тарелка манной каши, два
бутерброда с маслом сыром и колбасой» → теперь «бутерброд с маслом и сыром и колбасой» 2 шт, 471 ккал ≈
(хлеб 30 + масло 8 + сыр 15 + колбаса 15 на шт), сыр на месте, колбаса-ломтик 15 г вместо отдельных ≈100 г.
Корни и лечение: ❶ склейщик начинок требовал ровно одно слово после «с» — заменён на проверку «весь хвост
= начинко-слова и союзы» (sandwichTailLooksFillings); ❷ перечисление без союза («маслом сыром») докраивается
splitJuxtaposedFillings с защитой от префикс-угадайки lookupProduct (составной ключ = равное число слов);
❸ побочно закрыто латентное враньё «кофе с молоком» вклеивалось в сэндвич 15-граммовой «начинкой» —
страж «собственная конструкция „с“ = не начинка». Случай «с маслом и сыром» (190 ккал) сохранён регрессией.

## P10 — (продолжение) Итерация 4 — 0.4.3 (полевой тест: связка бананов ✓ штучно, кальмар ✓ с
надписи): ❶ пункт «Камера» в пикере — манифест-патч (применение файла за
пользователем); ❷ «Бутылка воды» = 500 мл + страж «вода без объёма не
молчит»; ❸ промпт против «Тарелка 300 г»/«Пакет с чупа-чупсами 1 шт»;
❹ листовая зелень 14 ккал вместо блюда-салата 55. Тарелка из нескольких
блюд: шашлык 5 кусков модель видит как «куриные котлеты 4 шт» — счёт ±1
и род блюда плавают, граммовка через «штуки» честная; дальше — качество
самой модели (потолок E2B), ждём новых вердиктов.
**Прежний статус:** дорожная карта подтверждена (полевой вопрос пользователя, 0.3.39).
**Источник:** PROJECT.md стр. 46 — «снимок блюда → ИИ → продукты и вес → JSON
→ локальная база → запись после подтверждения»; фото — третий равноправный
способ ввода наряду с текстом и голосом; «Рецепт из холодильника» — по фото.

**Что уже есть:** фото-ЗАМЕТКА (снимок прикрепляется к записи, без разбора);
текстовая нейросеть Gemma на устройстве (LiteRT-LM 0.12.0, стриминг 0.3.39).

**Ключевое ограничение:** Gemma3-1B-IT — текст-only (слепая). Зрение есть у
gemma-4-E2B-it.litertlm (2.59 ГБ, ~3+ ГБ RAM) и E4B (3.66 ГБ, 8+ ГБ RAM).

**API готово (проверено по тегу v0.12.0):** `Content.ImageBytes(bytes)` /
`Content.ImageFile(path)`, `EngineConfig.visionBackend` — нативный метод
generate с картинкой реализуем без изменения архитектуры плагина.

**План итерации:**
1. Плагин: `generateWithImage({prompt, imagePath})`; файл из Загрузок/камеры.
2. Ввод: «📷» во вкладке ⚡ Ввод через `<input type=file accept="image/*"
   capture="environment">` (в WebView работает без нативного плагина);
   уменьшение до ~768px JPEG на canvas перед отправкой (скорость зрения
   напрямую от размера/токенов изображения).
3. Промпт: распознать продукты, оценить граммовку, вернуть СТРОГО JSON.
4. Маппинг JSON → FOOD_DB (как parseOffProduct-стиль) → экран подтверждения
   умного ввода (принцип: в дневник — только после глаз пользователя).
5. Честный таймаут-стриминг уже есть (0.3.39): процесс виден по ходу.

**Риски честно:** RAM ~3 ГБ свободной при E2B; зрение на CPU может занимать
минуты на слабом железе — полевой тест обязателен до обещаний скорости;
качество распознавания приёмов пищи у модели такого класса — «черновое»,
подтверждение пользователем — обязательная часть сценария, не опция.
