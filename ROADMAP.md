# ROADMAP — дорожная карта FitFlow

Актуальна на **29.08.2026** (версия 0.9.52). Исторические планы и зафиксированные
ранее списки — в `PROJECT.md` # 15; отложенные идеи и провалившиеся подходы — в `POSTPONED.md`.
Точка входа для нового агента — `SESSION_HANDOFF.md`.

**Где мы сейчас.** Этап 0.8.x («Дневник тренировок» и полировка) завершён.
Идёт подготовка к 1.0.0 и публикации в RuStore — шаги 0–3 плана закрыты (вынос
натива 0.8.27, рубильник онлайн-функций 0.8.28, индикатор режима и бета-метка
ИИ-анализа 0.8.29, штатное закрытие диалогов 0.8.30, бета-метка прогресса силовых
0.9.0).

Замечания владельца идут блоками, каждый закрывается своими версиями:
блок 1 — 0.9.1–0.9.4, блок 2 — 0.9.5, блок 3 — 0.9.12, витамины («Мой курс»
с подтверждением приёма из шторки без открытия приложения) — 0.9.13.
Блок 4: пункты 1–3 закрыты в 0.9.14–0.9.16, пункт 4 (облачное распознавание
фото) закрыт в 0.9.20.
Блок 5 закрыт в 0.9.18: окно сравнения силовых расширено до периодов
7 дней / месяц / всё время, введены два показателя сразу (лучший подход по
внешнему весу и объём за тренировку), масса тела разведена по показателям,
нюансы объяснены простыми словами через «?». Блок 6 закрыт в 0.9.19: кнопка
«+ 250 мл» доставлена на умные часы Wear OS (у курса — «✓ Принял»); для
браслетов-компаньонов (Amazfit/Zepp, Honor Band) это технически невозможно —
они пересылают только текст уведомления, поэтому в FAQ добавлено честное
объяснение и указан рабочий путь через виджет.

Пункт 4 блока 4 закрыт в 0.9.20: облачное распознавание фото еды заработало у
OpenRouter и GigaChat. Причиной «только Gemini» был дефект транспорта —
`cloudCallOpenAiCompat` не прикладывал изображение к запросу; теперь снимок
уходит частью `image_url` (OpenAI-совместимые) или через хранилище файлов
`POST /files` → `attachments` (GigaChat), а зрячую модель для снимка приложение
подставляет само, с запасным списком на случай отзыва бесплатных моделей.

0.9.21 закрыл два замечания по тренировкам с часов: список на Главной
сворачивается, минуты доходят до графика статистики. 0.9.22 — вечерний
вопрос «была сегодня активность?» не приходит, если часы уже передали
минуты (даже неподтверждённые: владелец не подтверждает их намеренно,
чтобы они сами записались на следующий день). 0.9.23 — свёрнутый список
с часов снова одна пластина: на теме «Лес» пропали серая полоса-заголовок
и зелёный зазор сверху (каскад CSS, две класса вместо одной). 0.9.24 —
три оформления виджета пункта 5 в APK (капля / бенто / кольца);
классическая строка осталась, Ring / Rings / Dial / Tiles убраны.
Полевая проверка 0.9.22 закрыта: вечерний вопрос не пришёл.

0.9.28 — разбор полевого скрина бенто: подложка сведена к геометрии,
кроссовок встал в центр кольца, шрифт у плит один, «0 из ххх» единым видом,
пилюли перестали срезаться (виджет 4×3 вместо 4×2), у питания появилась
кнопка быстрого ввода, а состав слотов теперь берётся из настроек виджета.
Порядок создания виджетов зафиксирован в `design/WIDGET-HOWTO.md` — следующие
оформления делаются по нему.

Сквозное требование «никаких видимых заглушек» соблюдено: нерабочих элементов
в UI нет. Дальше: разделение сущностей «Питания» (Комбо / Мои блюда / Мои
продукты), микроанимации 150–250 мс с уважением к `prefers-reduced-motion`,
затем 1.0.0 и перенос в `main` — самым последним шагом.

Финальное слово по продуктовым решениям — за командой (владелец + агент);
каждый этап стартует только после явного подтверждения владельца.

---

## ✅ Этап 0.4.x — Выполнен и закрыт (06–10.08.2026)

- Нейросеть на устройстве (Gemma, LiteRT-LM): чат, анализ, рецепты, фото еды.
- Облачный BYOK (Gemini / DeepSeek / OpenRouter / GigaChat) — опционально.
- Правдивость расчётов I: аудит базы, журнал распознаваний, «нет данных ≠ 0».
- 0.4.15 — большая ревизия по трём внешним анализам (см. CHANGELOG.md).

## ✅ Этап 0.5.0 — «Доверие данным» (выпущен 10.08.2026; ждёт полевой проверки владельцем)

Тема: пользователь должен быть уверен, что данные никогда не пропадут
и любую запись можно исправить.

1. **Версионирование состояния** — СДЕЛАНО: `STATE_SCHEMA_VERSION` (=2),
   точка миграций `migrateStateSchema`, номер схемы в бэкапе all-profiles
   (фундамент SQLite).
2. **Редактирование записей** — СДЕЛАНО: еда (название/ккал/БЖУ), активность
   (вид/время/заметка/дата), вес (✏️ в истории). Вода — через «Поправить день».
3. **Записи задним числом** — СДЕЛАНО: дата в форме активности + диалог
   «Поправить итог дня» (вода/ккал/минуты прошлого дня).
4. **Системный undo** — СДЕЛАНО для точечных операций (снекбар 6,5 с);
   массовые операции (импорт/сброс) намеренно за подтверждающими диалогами.
5. **Онбординг-lite** — СДЕЛАНО: три экрана переписаны под текущие реалии.
6. **Доступность (a11y-pass)** — БАЗОВО: :focus-visible + aria у новых
   контролов; полный TalkBack-проход остаётся в POSTPONED P19.
7. **Каркас PRO-разблокировки** (см. «Монетизация» ниже) — РЕШЕНИЕ ВЛАДЕЛЬЦА:
   «закладывай, пусть будет» → реализован в 0.5.1 (экран + офлайн-коды +
   флаг в бэкапе, БЕЗ ограничений функций).

Кандидаты из анализов, ожидающие решения: `POSTPONED.md` P13–P24.

## ✅ Этап 0.5.1 — Полировка полевого теста + PRO-каркас (выпущен 10.08.2026)

По 16 пунктам полевой проверки 0.5.0: чипы-пилюли на всю ширину, сон — эмодзи
оценки вместо серой галочки, поле питания на всю строку + описания в «?»,
«Поправить день» напротив заголовка и полями стопкой, ≈-средние у всех разделов
+ неразрывные пары «цифра+единица», подписи дат графиков не уплывают, выбранный
шрифт действует на все темы (включая Спорт), дата без «Авг. … Г.», компактная
плашка самочувствия, шапка 4→3 значка (приватность в «О приложении»),
PRO-каркас (экран/коды/бэкап/генератор, функции не закрываются), бэкап честнее
(шрифт и палитры «Лес»/«Виноград» восстанавливаются).

## ✅ Этап 0.5.2 — быстрые записи одним окном, мелкая полировка (выпущен 10.08.2026)

Второй полевой чек-лист: «⚡ Быстрые записи» собраны в один диалог с вкладками
(⭐ Комбо / 🍽 Блюда / ✍️ Своё блюдо), самочувствие — всегда одна строка,
шапка статистики без наслоений, недельная цель активности — «выполнено ИЗ цели
— осталось/сверх» (арифметика была верна, чинили однозначность подписи).

## ✅ Этап 0.5.3 — второй уровень первого запуска: мастер, плашка поддержки, «Добрые дела» (выпущен 10.08.2026)

Идеи владельца: «Быстрая настройка» — мастер из 5 вопросов при первом запуске
(пропуск = стандартные значения, повтор — Настройки → Общее), включающий
существующие настройки их штатными сеттерами; разовая плашка поддержки
(один раз, не раньше ~10 запуска); инструкция «как поддержать» в PRO;
открытые благотворительные отчёты в «О приложении» (пока каркас — записи
релизами по факту). POSTPONED P18 закрыт полностью.

## ✅ Этап 0.5.4 — «⚙️ Настроить» в уведомлении, кольцо воды, редкая плашка поддержки (выпущен 10.08.2026)

Уточнения владельца перед полевым тестом: кнопка «Настроить» во всех
напоминаниях планировщика (свой раздел + подсветка; нативное уведомление
воды — в 0.8.0 одним нативным пакетом), «Цель достигнута!» не залезает
на дугу кольца, плашка поддержки — редко-повторяемая (≤ раза в 14 дней,
PRO — никогда) + постоянный блок в «О приложении». Решение по сомнениям
о публикации: каналы роста заморожены (см. «💰 Монетизация»).

## ✅ Этап 0.5.5 — еда, полный бэкап, честный виджет, «⚙️» у воды, лицензия (выпущен 10.08.2026)

Полевой пакет владельца: база еды (рыба по сортам/приготовлению, виды икры,
бутерброды и тарталетки с икрой — с новым родом «тарталетка» в регулярках
парсера), PRO-код едет в резервную копию, настройка «пауза напоминания воды
после записи», недельная цель — короткая строка + чип-статус + «?», быстрые
записи — ряд иконок, лицензия LICENSE + блок в «О приложении». Нативный пакет
(виджет: полночь + надёжная доставка; напоминание воды: пропуск + «⚙️») —
в зеркале, ждёт 5-й замены workflow владельцем. Исследования: каналы —
DISTRIBUTION.md, облако — POSTPONED P25.

## ✅ Этап 0.5.6 — единое описание приложения, авто-пауза воды, тихая вода извне, лицензия окном (выпущен 10.08.2026)

- APP_DESCRIPTION.md — единое описание каждого элемента текстом (для ИИ и команды);
  обновляется в каждом релизе (контроль автотестом).
- «Пауза после записи воды» без переключателя: записал воду — ближайшее напоминание
  молчит, сетка не сдвигается (JS и нативное зеркало; порог = интервал сетки).
- Тихая вода с виджета/из уведомления без отложенных окон при входе.
- Живые значки 🍽/✍️ быстрых записей; самочувствие — компактные две строки.
- Лицензия полным текстом окном из «О приложении» + упоминание в условиях первого входа.
- Нулевая нативная нагрузка на владельца: единственный нативный кусок (пропуск в
  ресивере) доедет той же 5-й заменой workflow, что и пакет 0.5.5.

## ✅ Этап 0.5.7 — окно «Быстрый ввод» чинится, уведомление воды короче (выпущен 11.08.2026)

- «Быстрый ввод» (с виджета): «Разобрать» больше не обрезается — действия иконками,
  основная кнопка всегда на виду; «Отмена» строкой; safe-area у подложки диалогов.
- Уведомление воды — короткий текст «Сегодня: X из Y мл» (натив + JS-фолбэк);
  нативная правка доезжает 6-й заменой workflow (ссылки теперь кликабельные).
- Статус натива: 5-я замена workflow владельцем ВЫПОЛНЕНА 11.08.2026 — виджет
  (полночь, доставка «+250», «⚙️ Настроить», авто-пропуск) живёт на устройстве.

## ✅ Этап 0.5.8 — Лицензия внутри приложения, лаконичное «О приложении», окно воды, быстрое комбо (выпущен 11.08.2026)

- Лицензия с вкладками `🇷🇺 Русский`, `🇬🇧 English`, `📦 Сторонние библиотеки` прямо в диалоге,
  без внешних недоступных md-ссылок.
- Экран «О приложении» стал компактным списком кнопок для чтения, без простыней текста.
- «Добрые дела» — открытые отчёты вынесены в отдельный модальный диалог по кнопке.
- Самочувствие на Главной — 2 аккуратные строки (строка 1: «🌗 Самочувствие» + знак «?» со справкой;
  строка 2: 5 кнопок-смайлов + статус оценки).
- Настройки напоминаний о воде — выбор периода «С ... до ...» прямо в интерфейсе.
- «Быстрые записи» — создание комбо прямо во вкладке «⭐ Комбо» и добавление блюд в «🍽 Блюда».
- Быстрый ввод — «☆ В комбо» сделана компактной второстепенной кнопкой рядом с «Сохранить записи».

## ✅ Этап 0.6.0 — Внедрение SQLite (выпущен 11.08.2026)

- Полнофункциональная реляционная база данных SQLite (`sql.js` 1.14.1 WASM + IndexedDB binary).
- Полная независимость: работает 100% офлайн без нативных плагинов и правок workflow.
- Архитектура Dual-Write: каждый `saveState()` сохраняет состояние в `localStorage` и асинхронно синхронизирует реляционные таблицы SQLite (`meta`, `profiles`, `states`, `daily_summaries`, `food_entries`, `workout_entries`, `weight_entries`, `custom_foods`, `combos`).
- Автоматический резервный снимок `fitflow:pre-sqlite-backup` перед миграцией.
- Автоматический откат на `localStorage` при любой ошибке.

## ✅ Этап 0.7.0 / 0.7.5 — Health Connect, шагомер телефона, плотность экрана и Explainability (выпущен 12.08.2026, build 238)

- Интеграция Android Health Connect (`READ_STEPS`, `READ_TOTAL_CALORIES_BURNED`, `READ_EXERCISE`, `READ_SLEEP`, `ACTIVITY_RECOGNITION`).
- Аппаратный шагомер телефона `TYPE_STEP_COUNTER` + `TYPE_STEP_DETECTOR` (фоновый учёт на сопроцессоре без расхода батареи).
- Алгоритм приоритета Master Priority: `MAX(Health_Connect, Шагомер_Телефона)` без задвоения с отображением источника.
- Карточка «👟 Шаги и движение» на Главном экране с надёжной регулировкой цели (8 000, 8 500 и т.д.).
- Объяснимые расчёты («Откуда это число?», `#explain-dialog`) по тапу на любое число цели и текущего значения.
- Плотность Главного экрана («Минимум», «Обычный», «Полный») в Настройки ➔ Общее.
- Сон из Health Connect (Zepp) в Статистике и окне диагностики.
- Устранение троения воды с виджета (атомарный забор в Java + дедупликация в JS).
- Компактный блок «Цель за 7 дней» в разделе «Активность».
- Донатная модель поддержки без кодов (дарение по ст. 572 ГК РФ).

## 🎯 Этап 0.8.0 — Дневник тренировок (упражнения, сеты: вес × повторы, 1RM, баланс нагрузки)

Фокус: целенаправленные спортивные тренировки (зал, бег, плавание, кроссфит, домашние тренировки).

0. **Импорт тренировок с часов (УТВЕРЖДЕНО владельцем 13.08.2026, первый пункт этапа)** —
   ✅ **закрыт**. Шаг 1 (0.8.0): чтение `ExerciseSessionRecord` из Health Connect + подсказка
   «Нашли тренировки с часов — добавить в дневник?» с подтверждением. Шаг 2 (0.8.25, по замечанию
   владельца): сессии за ПРОШЛЫЕ дни, оставшиеся без решения, добавляются автоматически
   (дата — по началу сессии) с кнопкой «Отменить»; сегодняшние по-прежнему ждут подтверждения.
   Остаётся необязательное улучшение: расширить окно чтения `readTodayWorkouts` в нативном
   зеркале, чтобы подхватывать сессии за несколько прошлых дней, а не только за сегодня.

1. **Каталог упражнений** — ✅ сделано в 0.8.4–0.8.10: базовые мышечные группы + свои упражнения; сеты «вес × повторы», тоннаж, 1RM (Эпли/Бржицки); длительность силовой засчитывается в недельную цель; прогресс, рекорды и уровни (лестница порогов); шаблоны тренировок; план по дням недели с отметкой «выполнено»; таймер отдыха между подходами; баланс нагрузки (Сила/Кардио/Растяжка).
2. **Структура подходов (Сеты)**: запись рабочих сетов `Вес (кг) × Повторы` + автоматический расчёт тоннажа тренировки.
3. **Калькулятор 1RM (одноповторный максимум)**: автоматический расчёт 1RM по формулам Эпли и Бржицки.
4. **Баланс физической нагрузки**: разделение времени и калорий «Сила / Кардио / Растяжка».
5. **Экспорт в Health Connect**: отправка завершённых спортивных тренировок как `ExerciseSessionRecord`. — ✅ **сделано в 0.8.11** (нативный пакет: `insertWorkoutSession` + `WRITE_EXERCISE` + кнопка ⤴ у записи).

## 🎯 Этап 0.8.1 – 0.8.5 — Полировка дневника тренировок

- Быстрые шаблоны тренировок («День груди», «Фулбоди», «Кардио-сессия») — ✅ 0.8.8.
- Таймер отдыха между подходами с виброоткликом — ✅ 0.8.10.
- История силовых рекордов по упражнениям — ✅ 0.8.5/0.8.6.

## 🎯 Полировка по прогону «трёх персонажей» (13.08.2026, согласовано владельцем)

Рекомендации прогона приложения тремя взглядами (новичок / опытный / критик) — взяты в работу:

1. **Шаги в Статистике за период (7/30 дней)** — опытный пользователь ждёт историю шагов, а не только
   «сегодня». Нужен дневной снапшот шагов (история) + обратная заливка прошлых дней — отдельная версия
   (см. POSTPONED P28). ✅ сделано в 0.7.13/0.7.14: карточка шагов, фоновая синхронизация.
2. **Цель шагов в мастере «Быстрая настройка»** — новичку непонятно, откуда «8 000». Добавить вопрос
   про цель шагов при первом запуске (POSTPONED P29). ✅ сделано в 0.8.18.
3. **Единый паттерн пустых состояний** («нет данных за период» + подсказка, а не голый «—») — критик.
   ✅ проверено в 0.8.19: паттерн уже единый. Дополнительно в 0.8.25 «нулевой» день в графиках
   рисуется прозрачным пунктиром (`.stats-bar.is-empty`), а не пропадает.
4. **Единый стиль подписей** (ккал, «сожжено»/«≈») — ✅ доведено в 0.8.19 (везде «≈»).
5. **Доступность (a11y)** — aria-метки на кликабельных метриках (начато в 0.7.14), дальше полный
   TalkBack-проход — POSTPONED P19.
6. **Размер APK (~58 МБ)** — за счёт нативного плагина нейросети (LiteRT-LM). Минификация/оптимизация —
   предрелизная зачистка, POSTPONED P27.

Порядок: 1 → 2 → (3+4 вместе) → 5 → 6; п.1 и п.2 — отдельными небольшими версиями, как требует AI_GUIDE.

## ✅ Этап 0.8.12 – 0.8.26 — Полировка, удобство и работа по замечаниям владельца (13–23.08.2026)

- 0.8.12–0.8.16: игровой режим и медали, компактное «О приложении», быстрый переход из фактически
  видимых карточек (единый оффсет подсветки и прокрутки).
- 0.8.17–0.8.19: перегруппировка «О приложении», цель шагов в мастере, единый стиль оценок.
- 0.8.20–0.8.23: поиск по базе продуктов, экспорт дневника в CSV, «↺ Повторить вчерашний приём пищи»,
  умные весы (вес и рост из Health Connect, нативный пакет).
- 0.8.24: вечерний вопрос «Как прошёл день?» (P13), скачивание модели Gemma по кнопке
  с прогрессом и подсказкой о совместимости телефона (P14).
- 0.8.25 (по четырём замечаниям владельца): авто-добавление тренировок с часов за прошлые дни
  с возможностью отмены; скрытая карточка исключается из всего приложения (План дня, задания,
  Статистика); кнопка «✓ Принял» в уведомлении о приёме курса; видимые столбики графиков.
- **0.8.26** — панель быстрого перехода: в пилюлях только текст (длинные названия
  больше не обрезаются), ширина по тексту с прокруткой вбок; подсветка активного
  раздела считается по видимой площади — корректна при прокрутке в обе стороны.

## ✅ 0.9.0 — Бета-метка на прогрессе силовых (выпущено)

Требование владельца: прогресс силовых и ИИ-рекомендации оставить, но с видимой
пометкой «бета, в разработке и тестировании». Метка `.beta-badge` добавлена в блок
«Прогресс силовых» и в подпись группы медалей «Сила (уровни)»; справка
`beta-strength` разделяет точные данные (вес, повторы, тоннаж) и расчётную оценку
(`1RM ≈` по Эпли/Бржицки, уровни и цели). ИИ-анализ периода помечен ранее, в 0.8.29.

## ✅ 0.9.1–0.9.4 — Пять пунктов замечаний владельца (выпущено)

- **0.9.1 (п.1)** — подсветка панели быстрого перехода. Критерий «максимум видимой
  площади» (0.8.26) врал на клике: `scrollIntoView` ставит верх нажатой карточки на
  линию порога, и высокая соседка побеждала по площади. Стало: активна карточка,
  ПЕРЕСЕКАЮЩАЯ линию порога, — она же цель прокрутки, поэтому нажатый чип
  подсвечивается всегда; `syncQuickNavTop()` пересчитывается перед прокруткой
  (высота шапки меняется от индикатора сети и поворота экрана).
- **0.9.2 (п.2)** — сканер штрих-кода камерой. Выбран ZXing
  (`journeyapps/zxing-android-embedded`, Apache-2.0): работает офлайн и без Google
  Play Services, в отличие от ML Kit и `BarcodeDetector` — они требуют GMS и потому
  отвергнуты. Источник данных — Open Food Facts (ODbL): бесплатно, включая массовое
  и коммерческое использование, при атрибуции. Рост APK ~1 МБ согласован.
- **0.9.3 (п.3)** — ссылка на страницу модели `litert-community/Gemma3-1B-IT` плюс
  пошаговая инструкция в подсказке «?». Лицензионная чистота проверена: обязанности
  Gemma Terms возникают только при распространении весов, чего приложение не делает;
  товарные знаки Google не используются. Репозиторий gated — пользователь принимает
  условия и скачивает сам.
- **0.9.4 (п.4, п.5)** — конфигуратор виджета: галочки выбора карточек, фиксированный
  бюджет строк по размеру виджета и сообщение при превышении; «Питание» больше не
  показывается на виджете при выключенной карточке.
- **Сквозное требование** «никаких видимых заглушек» действует постоянно: нерабочий
  элемент либо доводится до рабочего состояния, либо скрывается полностью (например,
  кнопка сканера показывается только при наличии нативного моста и камеры).

## ✅ 0.9.5–0.9.11 — Второй блок замечаний и работа по данным (выпущено)

- **0.9.5** — быстрый переход в самом верху, дубль строки воды на виджете, кнопка
  обновления виджета, подгонка числа слотов под высоту.
- **0.9.6–0.9.8** — полировка по итогам полевого использования.
- **0.9.9** — «Создать шаблон» прямо из пустого плана тренировок.
- **0.9.10** — тренировка не считается дважды. Дубль определяется по пересечению
  интервалов, а не по похожей длительности (критерий «±15 мин» владелец отверг:
  две тренировки одной длины в день — норма). Спорные случаи не решаются молча —
  остаются в подсказке с выбором «Всё равно добавить» / «Это дубль». Свои же
  выгруженные в Health Connect тренировки при чтении пропускаются.
- **0.9.11** — восстановление пропущенных дней: ранний снимок больше не блокирует
  итог дня, шаги после полуночи делятся между сутками, часы и телефон не
  складываются, добавлена дозаливка истории сна за 30 дней. **Итог полевой
  проверки: сон восстановился, шаги — нет** (причина найдена в 0.9.12).

## ✅ 0.9.12 — Шаги за прошлые дни, свой отдых, быстрый ввод подходов (выпущено)

Третий блок замечаний владельца после установки 0.9.11.

- **Настоящая причина непришедших шагов.** `readStepsHistory` читал Health Connect
  **одной страницей**: `readRecords` отдаёт до 1000 записей за раз плюс
  `pageToken`. Часы пишут шаги множеством мелких записей, поэтому за 30 дней лимит
  выбирался на самых старых днях и свежие не доезжали. У сна одна-две записи за
  ночь — он проходил в лимит, отсюда и расхождение «сон подтянулся, шаги нет».
  Обе функции истории переведены на цикл по `pageToken`.
- **Таймер отдыха настраивается.** Пресеты переехали из разметки в
  `state.strengthRest`; можно задать своё время («3 мин», «2:30», «180») и убрать
  лишние долгим нажатием. Последний вариант удалить нельзя — иначе таймер нечем
  запускать. По умолчанию 60/90/120/180 вместо прежних 30/60/90/120: потолок в
  120 секунд был жёстким ограничением.
- **Быстрый ввод подходов.** «5x10x12» = 5 подходов по 10 повторений с 12 кг за
  одно действие вместо десяти нажатий «＋ подход». Разделителем считается любой из
  `x`, `х`, `*`, `×` и пробела — на телефонной клавиатуре целиться в конкретный
  символ неудобно. Вес необязателен («3x8»), дробный принимается («12,5»).
  Упражнения с отягощением (отжимания + 12 кг) записываются как есть: вес
  трактуется как дополнительное отягощение.
- **Термины разведены.** Поле подхода называется «Повторения», а не «Повторы».
- **Отмена заполнения.** Кнопка «Отменить заполнение» с подтверждением очищает
  черновик; сохранение использует ту же `resetStrengthDraft()`, чтобы два пути
  очистки не разъехались. Кнопка показывается только когда есть что отменять.

## ✅ 0.9.13 — Витамины (выпущена 24.08.2026)

Перенесено из 0.9.11 по согласованию с владельцем. Технический разбор —
`PROJECT.md` # 14.

1. **Подтверждение приёма из уведомления не открывает приложение** — СДЕЛАНО.
   Плагин `@capacitor/local-notifications` на Android игнорирует `foreground:false`
   (все action-`PendingIntent` создаются через `getActivity`), поэтому напоминания
   о курсе переведены на собственные `CourseReminderReceiver` + `AlarmManager` +
   `FitFlowCourses` по образцу воды. Кнопка «✓ Принял» — броадкаст: отметка
   пишется в нативные prefs, уведомление гаснет, приложение остаётся закрытым.
2. **Витамины на виджете** — СДЕЛАНО. Пункт `courses` в `WIDGET_ITEMS`, текст
   строки собирает натив (после полуночи виджет обновится сам), тап отмечает
   ближайший непринятый приём, а когда отмечены все — снимает последний
   («забыл» / случайный тап). Без заведённого курса пункт не предлагается.
3. **Сведение отметок** — очередь натива доезжает в app.js через
   `window.onCourseDosesFromNative` и применяется идемпотентно.

## 🎯 Ближайшая очередь (ждём «делай» по макетам пункта 5)

- **Дизайн виджета — пункт 5 (макеты готовы, кода в APK нет).** Три JPEG
  владельца (`design/ref-light.jpg`, `ref-bento.jpg`, `ref-rings.jpg`) — бриф
  визуала, не вклейка. Макеты капля / бенто / кольца лежали в
  `design/WIDGET-P5.md` и `design/widget-p5-*.png`; в 0.9.47 виджет «капля»
  убран, а с ним в 0.9.48 удалены и его макеты (остались ref-*.jpg владельца). Показатели, кнопки и логика — наши
  (вода, съедено, шаги, активность, самочувствие, курсы). Сон и burned kcal
  с референсов не переносим. Постоянной анимации нет; микроанимация только
  в момент «добавить на виджеты». Если новые оформления примут — текущие
  Ring / Rings / Dial / Tiles из приложения уйдут. **Код в APK — только
  после явного «делай».**
- **«Питание» — чёткое разделение трёх сущностей** (владелец просил развести, а не
  объединять): «Комбо» = фраза-сценарий, «Мои блюда» = готовые ккал, «Мои
  продукты» = база для сканера. Нужна подсказка в поле создания комбо.
- **Микроанимации точечно:** «набег» числа воды/ккал, подсветка изменившейся
  строки, мягкая галочка. 150–250 мс, обязательно уважать
  `prefers-reduced-motion`. Заготовки в конце `style.css` (`.value-bump`,
  `.row-flash` — пока не применён, `.check-pop`).

## 🎯 Углублённая аналитика прогресса (крупный этап, после 1.0.0)

Содержательное развитие — уже за пределами дороги в магазин:

- Графики силового прогресса (динамика 1RM по ключевым движениям).
- Тренды изменения веса и состава тела за 3–6 месяцев.
- Корреляция питания (калории/белок) и результатов в тренировках.
- Калибровка порогов `STRENGTH_LADDER_RULES`: сейчас они эвристические, поэтому
  уровни и помечены бетой. Снять метку можно только после проверки на реальных данных.

Открытые задачи из `POSTPONED.md`, которые логично закрыть рядом: P17, P19 (полный
TalkBack-проход), P20, P21, P23, P25, P26, P27 (размер APK ~58 МБ), P33, P9, P6–P8.
(P22 закрыт в 0.8.30: системный «назад» закрывает диалоги их штатными
close-функциями; видимые кнопки «← Назад» решено оставить.)

---

## 📱 План по разработке под iOS (iPhone / iPad)

Благодаря архитектуре FitFlow (Offline-first HTML5 / Vanilla JS / SQLite WASM + IndexedDB) приложение на 90% кроссплатформенно. Ниже 4 пути реализации под экосистему Apple:

### 1. PWA (Progressive Web App) через Safari — пробный бесплатный формат
- **Как работает**: публикация веб-сборки на GitHub Pages. Пользователь открывает в Safari ➔ «Поделиться» ➔ «На экран "Домой"».
- **Плюсы**: $0 затрат, без аккаунта Apple Developer ($99/год), работает полностью офлайн (Service Worker + IndexedDB / SQLite WASM).
- **Ограничения iOS**:
  - Нет нативных виджетов рабочего стола iOS.
  - Локальные уведомления без сервера в iOS PWA сильно ограничены.
  - Safari может очищать кэш при нехватке памяти при редком использовании (требуется регулярный бэкап JSON).

### 2. SideStore / AltStore — нативный IPA без джейлбрейка
- **Как работает**: сборка `.ipa` через GitHub Actions (на раннере macOS). Установка на iPhone через SideStore с бесплатным личным Apple ID.
- **Преимущество SideStore**: самообновление 7-дневного бесплатного сертификата Apple прямо с телефона по Wi-Fi через локальный WireGuard VPN (компьютер нужен только один раз при первичной установке).
- **Плюсы**: полноценное нативное приложение, поддержка локальных уведомлений, фоновой работы и нативных мостов.

### 3. Интеграция с Apple Health (HealthKit)
- **Мост данных**: плагин `@capacitor-community/health-kit` или кастомный Swift-мост.
- **Метрики**:
  - Чтение шагов (`HKQuantityTypeIdentifierStepCount`) с iPhone и Apple Watch.
  - Чтение объективного сна (`HKCategoryTypeIdentifierSleepAnalysis`).
  - Чтение активной энергии (`HKQuantityTypeIdentifierActiveEnergyBurned`).
- **Алгоритм Master Priority**: `MAX(Apple_HealthKit, Шагомер_Телефона)`.

### 4. Официальная публикация в App Store / TestFlight (финальный этап)
- Требует: Apple Developer Program ($99/год), прохождение App Store Review, политика конфиденциальности.

---

## 🔮 Будущие и отложенные разработки (POSTPONED P13–P27)

1. **P13. Вечерний пуш «Оцени день»**: напоминание внести оценку самочувствия и итоги дня перед сном.
2. **P14. Установка локальной ИИ-модели по кнопке**: скачивание квантованной модели LiteRT-LM из приложения с прогресс-баром.
3. **P15. Полнотекстовый поиск по базе продуктов**: каталог 960+ продуктов с фильтрами по категориям, просмотром КБЖУ и порций.
4. **P16. Экспорт истории в CSV**: выгрузка дневника (еда, вода, вес, тренировки) в таблицу для Excel / врача.
5. **P23. Быстрый FAB «＋ Запись»**: единая кнопка быстрого добавления любой метрики в один тап.
6. **P24. «Повторить вчерашний приём пищи»**: кнопка быстрого копирования завтрака/обеда из вчерашнего дня.
7. **P19. Полный аудит доступности (a11y)**: разметка для скринридеров TalkBack / VoiceOver.
8. **P21. Релизная подпись (Release Keystore)**: подготовка подписанного APK/AAB для магазинов приложений.
9. **P27. Финальная предрелизная зачистка**: минификация и удаление служебных комментариев перед публикацией в сторах.

## 🏁 Этап 1.0.0 — Финальный релиз

- Комплексный аудит всех офлайн-сценариев и стабильности.
- Финализация справочной документации и методики расчётов.
- Предрелизная оптимизация производительности и размера APK.
5. Повторный проход по лицензионному чек-листу при появлении монетизации,
   рекламы или аналитики.

## 💰 Монетизация (статус: РЕШЕНО 10.08.2026 — бесплатно навсегда + добровольная поддержка)

Фактура, найденная владельцем: в RuStore монетизацию подключают только
юридические лица (физлицам оплата через магазин недоступна); платежи
Google Play в РФ недоступны; даже бесплатная публикация в RuStore требует
полной верификации физлица (паспорт, фото, доп. сведения) — поэтому RuStore
на текущий момент отложен, основной канал распространения — GitHub Releases.

Решение: приложение остаётся бесплатным и полноценным навсегда; раздел
«FitFlow PRO» (0.5.1) — только добровольная поддержка разработки, никакие
функции кодом не открываются.

Рабочие правовые заметки владельца (НЕ юридическая консультация; при оборотах —
налоговый консультант):
- Приём переводов только через российские банки/СБП — фильтрация по 115-ФЗ
  выполняется банком; крипту от неизвестных не принимать.
- При поступлениях — регистрация НПД (самозанятость, «Мой налог», 4–6 %):
  корректная форма для «поддержки с благодарностью (PRO-статус)»; чистое
  дарение денег между физлицами НДФЛ не облагается, но встречное предоставление
  делает перевод оплатой — НПД снимает этот риск.
- Подозрительный перевод — вернуть отправителю тем же способом; назначение
  платежа — «добровольная поддержка проекта FitFlow».

Принципы (зафиксированы ранее в PROJECT.md # 2): бесплатное ядро навсегда;
без серверов, подписок и аккаунтов; полный офлайн.

**Инженерное предложение — офлайн-PRO-каркас:**
- Владелец генерирует коды ВНЕ приложения: `HMAC(секрет, личная строка
  покупателя — имя/e-mail)` → короткий код. Секрет есть только у владельца
  (в репозитории — только публичная проверочная часть).
- Экран «Разблокировать»: ввод кода → локальная проверка подписи →
  флаг `proUnlocked` в state, **входящий в резервную копию all-profiles**.
- Вопрос владельца «что при удалении/переносе на другой телефон»:
  код привязан к ЛИЧНОЙ СТРОКЕ, а не к устройству → на новом телефоне
  вводится тот же код заново (или PRO восстанавливается вместе с бэкапом).
- Честно: это защита от случайного копирования и «добросовестного
  пользователя», а не DRM — взломать можно, цель её не в этом.
- Кандидаты в PRO (обсуждаемо, ядро не трогаем): локальные «тяжёлые» ИИ-модели,
  расширенные темы, «Мой курс»-плюс, расширенная аналитика.

Реализация каркаса (экран + флаг + генератор кодов для владельца) — выполнена
в 0.5.1 (решение владельца), детали — в записи 0.5.1 раздела # 14 PROJECT.md.

Каналы роста (вопрос владельца «а как люди узнают?», 10.08.2026): GitHub сам
по себе не воронка — он «причал», куда ведут ссылки. Воронки: ① подача
бесплатной сборки в RuStore после стор-чеклиста выше (верификация разовая;
PRO-экран правилам не противоречит — он ничего не продаёт); ② посты в
тематических Telegram-каналах и ветка приложения на 4PDA; ③ статья на Хабре
(офлайн-приватность + умный ввод — читаемая история); ④ Obtainium для
технической аудитории (сам следит за GitHub Releases). В приложении —
плашка поддержки (0.5.3, редко-повторяемая с 0.5.4) и социальный механизм
«поделись с тем, кому полезно»: рассмотреть отдельным пунктом позже.

Статус каналов роста (решение владельца 10.08.2026): **ЗАМОРОЖЕНО** до
отдельного решения. Аргументы: объём персональных данных KYC-верификации
RuStore неприемлем для бесплатной публикации; 4PDA — вероятные ограничения
для новых аккаунтов (создание тем после истории/репутации) и узкая
«целевая» аудитория; публикация — не спринт, сначала продукт для себя
и близких. Практический канал по-прежнему один — GitHub Releases.
