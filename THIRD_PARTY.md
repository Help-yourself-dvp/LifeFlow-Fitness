# THIRD_PARTY — сторонние компоненты и их лицензии

Сводная таблица для экрана «Лицензии третьих сторон» и магазинной публикации.
Поддерживается вместе с `PROJECT.md` # 16 (там же чек-лист 16.4).
Правило: любой новый компонент вносится сюда ДО включения в релиз.

## Нейросетевые модели (подключает пользователь, в APK не входят)

| Компонент | Лицензия | Обязательства |
|---|---|---|
| Gemma 4 E2B / E4B (формат .litertlm) | Apache 2.0 | Текст лицензии + attribution в экране лицензий |
| Gemma 3 / 3n | Gemma Terms of Use (кастомная) | Соблюдать Prohibited Use Policy; дисклеймер «не медицинский совет» обязателен |
| Qwen3 (например, 0.6B) | Apache 2.0 | Текст лицензии + attribution |

Модели скачивает/импортирует сам пользователь; перераспространение в APK не ведётся.

**Ссылка на страницу модели (0.9.3).** В настройках локального ИИ есть кнопка «Открыть страницу модели»,
ведущая на `https://huggingface.co/litert-community/Gemma3-1B-IT` (константа `AI_MODEL_PAGE_URL` в `app.js`).
Проверка лицензионной чистоты:

- Ведём **на страницу автора**, а не на файл: веса модели мы не размещаем и не раздаём,
  то есть Distribution в смысле Gemma Terms of Use не возникает и связанные с ним
  обязанности (передача текста лицензии получателю, пометки об изменениях) к нам не относятся.
- Репозиторий gated: скачивание возможно только после входа в аккаунт Hugging Face и
  принятия условий Gemma **самим пользователем**. Обход этого механизма мы не делаем и
  прямых (временных) ссылок на файл не зашиваем.
- Согласно §3.3 Gemma Terms of Use Google не претендует на результаты работы модели (Output),
  поэтому подсказки FitFlow, полученные локально, ограничений не несут.
- Согласно §4.2 не используются товарные знаки и логотипы Google: кнопка подписана текстом,
  изображения Google/Gemma в интерфейсе отсутствуют.
- Ссылка открывается во внешнем браузере (`openExternalUrl` в `MainActivity.java`,
  только схемы http/https), поэтому форма входа Hugging Face не показывается внутри нашего WebView.

## Источники пищевых данных

| Источник | Лицензия | Что делаем |
|---|---|---|
| USDA FoodData Central / SR Legacy | Public domain | Упоминание в «О приложении» (хороший тон) |
| Open Food Facts | ODbL 1.0 | Атрибуция ниже; аудит базы перед магазином — см. ROADMAP.md |

Использование API (с 0.9.2, сканер штрих-кода): чтение не требует ключа, но
Open Food Facts обязывает идентифицировать приложение, иначе клиент банится
как бот. Заголовок `User-Agent` из `fetch` в WebView задать нельзя (forbidden
header), поэтому применяются штатные параметры запроса `app_name`,
`app_version`, `app_uuid` (случайный идентификатор, не привязан к устройству
и профилю). Ответы кэшируются локально — этого требуют правила OFF.

Атрибуция Open Food Facts (обязательная формулировка):

> This record contains information from Open Food Facts (https://en.openfoodfacts.org), which is made available here under the Open Database License (https://opendatacommons.org/licenses/odbl/1-0).

Подробности о том, что именно хранится, — в `DATA_SOURCES.md`.

## Библиотеки и фреймворки

| Компонент | Версия | Лицензия |
|---|---|---|
| sql.js (SQLite WebAssembly) / SQLite | 1.14.1 | MIT / Public Domain |
| Capacitor (core, android, plugins) | 5.7.0 | MIT |
| LiteRT-LM Android (`litertlm-android`) | 0.12.0 | Apache 2.0 |
| Kotlin stdlib / kotlinx | 2.2.21 (транзитивно) | Apache 2.0 |
| AndroidX библиотеки | по шаблону сборки | Apache 2.0 |
| Android Gradle Plugin | 8.2.1 | Apache 2.0 |
| ZXing Core (`com.google.zxing:core`) | 3.5.3 | Apache 2.0 |
| zxing-android-embedded (`com.journeyapps`) | 4.3.0 | Apache 2.0 |

Перед первой магазинной публикацией: сверить список версий с фактической
сборкой и вынести тексты лицензий офлайн в экран «О приложении → Лицензии».

## Облачные ИИ-сервисы (опционально, BYOK — ключ пользователя)

Google Gemini, DeepSeek, OpenRouter, GigaChat. Использование — по условиям
соответствующих сервисов и только по явному выбору пользователя; данные им
отправляются только в рамках текущего вопроса (см. `PRIVACY.md`).

## Шрифты и иконки

- Шрифты — системные стеки Android; файлы шрифтов в приложение не встраиваются
  по умолчанию. Файловые тематические шрифты (Russo One, PT Serif, Comfortaa,
  Manrope — свободно распространяемые, OFL/лицензия издателя) могут быть
  добавлены владельцем по инструкциям `tools/github-workflows/FONT*_PATCH.md`;
  при фактическом встраивании конкретные лицензии заносятся в эту таблицу
  и в `PROJECT.md` # 16.
- Иконки интерфейса — собственные инлайн-SVG; эмодзи — системные.

## Чек-лист перед публикацией в магазине

См. `PROJECT.md` # 16.4 (экран лицензий, дисклеймеры, описание offline-first
в карточке магазина, повторный проход при появлении монетизации/аналитики).
