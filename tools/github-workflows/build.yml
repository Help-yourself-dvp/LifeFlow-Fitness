name: Build Native Android APK

permissions:
  contents: write

on:
  push:
    branches: [ '**' ]
  workflow_dispatch:

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: Read app version from version.txt
        shell: bash
        run: |
          APP_VERSION="$(tr -d '[:space:]' < version.txt)"
          if [ -z "$APP_VERSION" ]; then
            echo "ОШИБКА: файл version.txt пуст — сборка остановлена."
            exit 1
          fi
          echo "APP_VERSION=$APP_VERSION" >> "$GITHUB_ENV"
          echo "Версия приложения из version.txt: $APP_VERSION"

      - name: Проверить, что активный workflow совпадает с зеркалом
        shell: bash
        run: |
          if cmp -s .github/workflows/build.yml tools/github-workflows/build.yml; then
            echo "✓ Активный workflow совпадает с зеркалом tools/github-workflows/build.yml — нативные правки применены."
          else
            echo "⚠️ ВНИМАНИЕ: активный .github/workflows/build.yml ОТЛИЧАЕТСЯ от зеркала tools/github-workflows/build.yml."
            echo "   Сборка идёт на СТАРОМ нативе — после установки проверьте замену workflow по протоколу."
            diff --stat .github/workflows/build.yml tools/github-workflows/build.yml || true
          fi

      - uses: actions/setup-java@v4
        with:
          distribution: temurin
          java-version: '17'

      - uses: actions/setup-node@v4
        with:
          node-version: '20'

      - name: Remove stale capacitor config (legacy PWA leftover)
        run: |
          if [ -f capacitor.config.json ]; then
            echo "Найден устаревший capacitor.config.json — удаляю, чтобы cap init создал правильный."
            rm -f capacitor.config.json
          else
            echo "Старого capacitor.config.json нет, всё чисто."
          fi

      - name: Prepare www
        run: |
          mkdir -p www
          cp index.html www/
          cp style.css www/
          cp app.js www/
          mkdir -p www/assets/fonts && cp assets/fonts/*.ttf www/assets/fonts/ 2>/dev/null || true
          cp food-db.js www/ 2>/dev/null || true
          cp manifest.json www/ 2>/dev/null || true
          cp icon.png www/ 2>/dev/null || true

      - name: Inject build number (0.7.11)
        shell: bash
        run: |
          RUN_NUMBER="${{ github.run_number }}"
          sed -i "s/const FITFLOW_BUILD = '[^']*';/const FITFLOW_BUILD = 'build ${RUN_NUMBER}';/" www/app.js
          sed -i "s/v${APP_VERSION} (build [0-9]*)/v${APP_VERSION} (build ${RUN_NUMBER})/" www/index.html
          echo "FITFLOW_BUILD = build ${RUN_NUMBER} (версия ${APP_VERSION})"

      - name: Install Capacitor
        run: |
          npm init -y
          npm install @capacitor/core@5.7.0 @capacitor/cli@5.7.0 @capacitor/android@5.7.0 @capacitor/local-notifications@5.0.7 @capacitor/browser@5.2.0
          # LOCAL_AI_PATCH (0.4.x): нативный модуль нейросети Gemma (LiteRT-LM) из репозитория
          npm install ./plugins/fitflow-local-ai

      - name: Init Android
        run: |
          npx cap init "FitFlow" "com.fitflow.app" --web-dir www
          npx cap add android
          sed -i 's/targetSdkVersion = .*/targetSdkVersion = 34/g' android/variables.gradle
          sed -i 's/compileSdkVersion = .*/compileSdkVersion = 34/g' android/variables.gradle
          # LOCAL_AI_PATCH: LiteRT-LM требует minSdk 24 (по умолчанию шаблон ставит 22/23)
          sed -i 's/minSdkVersion = .*/minSdkVersion = 26/g' android/variables.gradle
          # CAMERA_CHOOSER_PATCH (0.4.3): пункт «Камера» в системном выборе фото WebView
          # (input type=file accept="image/*") виден только при объявленном permission
          # CAMERA в манифесте — без него пикер сразу открывает галерею (полевой баг:
          # «готовое фото выбрать можно, сфотографировать — нет»). uses-feature не
          # обязателен: камера бывает внешней/отсутствует, приложение обязано работать.
          sed -i 's|<application|<uses-feature android:name="android.hardware.camera.any" android:required="false" />\n    <uses-permission android:name="android.permission.CAMERA" />\n\n    <application|' android/app/src/main/AndroidManifest.xml
          # LOCAL_AI_PATCH (0.3.37): D8 с поддержкой Java-21 байткода.
          # litertlm-android:0.12.0 собран под Java 21 (class file major 65) —
          # D8 внутри шаблонного AGP 8.2.1 его не читает (сбой 0.3.36: «Unsupported
          # class file major version 65» на :app:mergeExtDexDebug; то же у
          # error_prone_annotations-2.41.0). Официальный механизм Google — оверрайд
          # R8/D8 новее встроенного через buildscript-classpath корневого
          # android/build.gradle; сам AGP не трогаем (минимальный риск).
          python3 - <<'PYEOF'
          from pathlib import Path as _P
          _p = _P('android/build.gradle')
          _t = _p.read_text(encoding='utf-8')
          if 'com.android.tools:r8:' not in _t:
              _anchor = "classpath 'com.android.tools.build:gradle:"
              _out = []
              _done = False
              for _ln in _t.split('\n'):
                  _out.append(_ln)
                  if (not _done) and _anchor in _ln:
                      _out.append("        classpath 'com.android.tools:r8:8.5.35' // LOCAL_AI_PATCH: D8 с чтением Java-21 байткода (litertlm)")
                      _done = True
              if not _done:
                  raise SystemExit('ОШИБКА: якорь AGP-classpath не найден в android/build.gradle — R8 не внедрён, сборка остановлена')
              _p.write_text('\n'.join(_out), encoding='utf-8')
              print('OK: R8 8.5.35 внедрён в buildscript корневого android/build.gradle')
          else:
              print('classpath com.android.tools:r8 уже есть, пропуск')
          PYEOF
          sed -i "s/versionCode 1/versionCode ${{ github.run_number }}/g" android/app/build.gradle
          sed -i "s/versionName \"1.0\"/versionName \"${APP_VERSION}\"/g" android/app/build.gradle
          echo 'android.suppressUnsupportedCompileSdk=34' >> android/gradle.properties
          # Health Connect dependency — чтение шагов/сна из Zepp, Garmin, Samsung
          sed -i '/dependencies {/a\    implementation "androidx.health.connect:connect-client:1.1.0-alpha07"' android/app/build.gradle
          sed -i '/dependencies {/a\    implementation "org.jetbrains.kotlinx:kotlinx-coroutines-android:1.7.3"' android/app/build.gradle
                              # Kotlin plugin for HealthConnectHelper.kt
          python3 - <<'PYEOF2'
          from pathlib import Path
          _p = Path('android/build.gradle')
          _t = _p.read_text(encoding='utf-8')
          if 'kotlin-gradle-plugin' not in _t:
              _anchor = "classpath 'com.android.tools:r8:"
              _out = []
              _done = False
              for _ln in _t.split('\n'):
                  _out.append(_ln)
                  if (not _done) and _anchor in _ln:
                      _out.append("        classpath 'org.jetbrains.kotlin:kotlin-gradle-plugin:2.2.21'")
                      _done = True
              if _done:
                  _p.write_text('\n'.join(_out), encoding='utf-8')
                  print('OK: Kotlin plugin added to root build.gradle')
              else:
                  print('WARNING: R8 anchor not found, Kotlin plugin NOT added')
          else:
              print('Kotlin plugin already in root build.gradle')
          PYEOF2
          # Apply Kotlin plugin to app module
          sed -i "/apply plugin: 'com.android.application'/a\\apply plugin: 'kotlin-android'" android/app/build.gradle
          npx cap sync android

      - name: Verify webDir is correct (safety check)
        run: |
          echo "Итоговый capacitor.config.json:"
          cat capacitor.config.json || true
          echo "---"
          echo "Содержимое android/app/src/main/assets/public:"
          ls -la android/app/src/main/assets/public 2>/dev/null | head -30 || echo "путь не найден, пропускаем проверку"

      - name: Patch MainActivity (alarms, battery, backup save bridge)
        run: |
          mkdir -p android/app/src/main/java/com/fitflow/app
          cat > android/app/src/main/java/com/fitflow/app/MainActivity.java << 'EOF'
          package com.fitflow.app;

          import android.app.AlarmManager;
          import android.content.Intent;
          import android.content.SharedPreferences;
          import android.net.Uri;
          import android.speech.RecognizerIntent;
          import android.os.Build;
          import android.os.Bundle;
          import android.os.PowerManager;
          import android.provider.Settings;
          import android.webkit.JavascriptInterface;
          import android.webkit.WebView;
          import android.hardware.Sensor;
          import android.hardware.SensorEvent;
          import android.hardware.SensorEventListener;
          import android.hardware.SensorManager;

          import androidx.core.app.NotificationManagerCompat;
          import androidx.core.content.ContextCompat;

          import com.getcapacitor.BridgeActivity;

          import org.json.JSONObject;

          // Health Connect API — чтение шагов и сна из Zepp/Garmin/Samsung
                                                                                                              import java.util.List;
          import java.util.Set;
          import androidx.health.connect.client.HealthConnectClient;
          import androidx.health.connect.client.records.StepsRecord;
          import androidx.health.connect.client.records.SleepSessionRecord;
          import androidx.health.connect.client.request.ReadRecordsRequest;
          import androidx.health.connect.client.response.ReadRecordsResponse;
          import androidx.health.connect.client.time.TimeRangeFilter;

          import java.io.OutputStream;
          import java.util.ArrayList;

          public class MainActivity extends BridgeActivity implements SensorEventListener {

              private static final int REQ_SAVE_BACKUP = 7301;
              private static final int REQ_VOICE_INPUT = 7302;
              private String pendingBackupJson = null;
              private String pendingWidgetAction = null;
              private SensorManager sensorManager;
              private Sensor stepCounterSensor;
              private Sensor stepDetectorSensor;

              @Override
              protected void onCreate(Bundle savedInstanceState) {
                  super.onCreate(savedInstanceState);
                // IME «прогрев» (0.3.17): у части WebView первая сессия клавиатуры
                // после холодного старта теряется, если у корневого WebView ещё
                // не запрашивали фокус. requestFocus() после загрузки страницы
                // лечит «клавиатуру со второго тапа» и молчащий набор GBoard.
                try {
                    final WebView wv = getBridge() != null ? getBridge().getWebView() : null;
                    if (wv != null) {
                        wv.postDelayed(new Runnable() {
                            @Override public void run() { wv.requestFocus(); }
                        }, 500);
                    }
                } catch (Exception e) { }
                  requestExactAlarmPermission();
                  requestIgnoreBatteryOptimizations();
                  installBackupBridge();
                  initStepSensors();
                  try { HealthSyncReceiver.schedulePeriodicSync(this); } catch (Exception e) { }
                  checkPendingWidgetWaterAdd();
                  Intent startIntent = getIntent();
                  if (startIntent != null && startIntent.hasExtra("widget_action")) {
                      pendingWidgetAction = startIntent.getStringExtra("widget_action");
                  }
              }
              // Кнопка/жест «назад»: пока есть история в WebView — шаг назад
              // внутри приложения, из корня истории — обычный выход.
              @Override
              public void onBackPressed() {
                  WebView webView = getBridge() != null ? getBridge().getWebView() : null;
                  if (webView != null && webView.canGoBack()) {
                      webView.goBack();
                      return;
                  }
                  super.onBackPressed();
              }
              @Override
              protected void onNewIntent(Intent intent) {
                  super.onNewIntent(intent);
                  checkPendingWidgetWaterAdd();
                  if (intent != null && intent.hasExtra("widget_action")) {
                      pendingWidgetAction = intent.getStringExtra("widget_action");
                      processWidgetAction();
                  }
              }

              @Override
              public void onResume() {
                  super.onResume();
                  try {
                      if (sensorManager != null) {
                          if (stepCounterSensor != null) sensorManager.registerListener(this, stepCounterSensor, SensorManager.SENSOR_DELAY_NORMAL);
                          if (stepDetectorSensor != null) sensorManager.registerListener(this, stepDetectorSensor, SensorManager.SENSOR_DELAY_NORMAL);
                      }
                  } catch (Exception e) { }
                  checkPendingWidgetWaterAdd();
                  if (pendingWidgetAction != null) processWidgetAction();
              }

              private boolean isDeliveringWater = false;

              private synchronized void checkPendingWidgetWaterAdd() {
                  try {
                      if (isDeliveringWater) return;
                      SharedPreferences prefs = getSharedPreferences("fitflow_widget", MODE_PRIVATE);
                      final int pending = prefs.getInt("pendingWaterAdd", 0);
                      if (pending > 0) {
                          isDeliveringWater = true;
                          prefs.edit().putInt("pendingWaterAdd", 0).apply();
                          tryProcessWidgetAction("add_water_" + pending, 0);
                      }
                  } catch (Exception e) { }
              }

              private void processWidgetAction() {
                  final String action = pendingWidgetAction;
                  pendingWidgetAction = null;
                  if (action == null) return;
                  tryProcessWidgetAction(action, 0);
              }

              private void tryProcessWidgetAction(final String action, final int attempt) {
                  runOnUiThread(new Runnable() {
                      @Override
                      public void run() {
                          try {
                              WebView wv = getBridge().getWebView();
                              if (wv != null) {
                                  // 0.5.5: ждём реальную готовность app.js. Раньше evaluateJavascript
                                  // на холодном старте тихо «съедал» действие (window.onWidgetAction
                                  // ещё не существовала), а pendingAdd уже был обнулён.
                                  wv.evaluateJavascript("(window.__fitflowReady === true)", new android.webkit.ValueCallback<String>() {
                                      @Override public void onReceiveValue(String value) {
                                          if ("true".equals(value)) deliverWidgetAction(action);
                                          else retryWidgetAction(action, attempt);
                                      }
                                  });
                                  return;
                              }
                          } catch (Exception e) { }
                          retryWidgetAction(action, attempt);
                      }
                  });
              }

              private void deliverWidgetAction(String action) {
                  try {
                      isDeliveringWater = false;
                      WebView wv = getBridge().getWebView();
                      if (wv != null) {
                          wv.evaluateJavascript("window.onWidgetAction && window.onWidgetAction('" + action + "');", null);
                          return;
                      }
                  } catch (Exception e) { }
                  isDeliveringWater = false;
                  retryWidgetAction(action, 0);
              }

              private void retryWidgetAction(final String action, final int attempt) {
                  if (action == null) { isDeliveringWater = false; return; }
                  if (attempt < 40) {
                      new android.os.Handler().postDelayed(new Runnable() {
                          @Override
                          public void run() { tryProcessWidgetAction(action, attempt + 1); }
                      }, 300);
                  } else {
                      isDeliveringWater = false;
                  }
                  // Если и за ~12 секунд не дождались — действие остаётся в prefs
                  // (add_water) или теряется безвредно (smart_entry/notif_settings_*).
              }

              /* ===== Сохранение резервной копии через системное окно Android ===== */

              private void initStepSensors() {
                  try {
                      sensorManager = (SensorManager) getSystemService(SENSOR_SERVICE);
                      if (sensorManager != null) {
                          stepCounterSensor = sensorManager.getDefaultSensor(Sensor.TYPE_STEP_COUNTER);
                          if (stepCounterSensor != null) {
                              sensorManager.registerListener(this, stepCounterSensor, SensorManager.SENSOR_DELAY_NORMAL);
                          }
                          stepDetectorSensor = sensorManager.getDefaultSensor(Sensor.TYPE_STEP_DETECTOR);
                          if (stepDetectorSensor != null) {
                              sensorManager.registerListener(this, stepDetectorSensor, SensorManager.SENSOR_DELAY_NORMAL);
                          }
                      }
                  } catch (Exception e) { }
              }

              @Override
              public void onSensorChanged(SensorEvent event) {
                  try {
                      if (event.sensor.getType() == Sensor.TYPE_STEP_COUNTER) {
                          int totalSteps = (int) event.values[0];
                          updatePhoneSteps(totalSteps);
                      } else if (event.sensor.getType() == Sensor.TYPE_STEP_DETECTOR) {
                          incrementStepDetector();
                      }
                  } catch (Exception e) { }
              }

              private void updatePhoneSteps(int rawSteps) {
                  try {
                      SharedPreferences prefs = getSharedPreferences("fitflow_sensor_prefs", MODE_PRIVATE);
                      String today = new java.text.SimpleDateFormat("yyyy-MM-dd", java.util.Locale.US).format(new java.util.Date());
                      String baseDate = prefs.getString("steps_base_date", "");
                      int baseSteps = prefs.getInt("steps_base", -1);
                      // Перебазирование: новый день, нет базы или счётчик сброшен (перезагрузка).
                      // Отдельный ключ steps_base_date хранит ДЕНЬ установки базы: ночной сброс
                      // HealthSyncReceiver меняет steps_date, но не базу — раньше утром счётчик
                      // продолжал считать от вчерашней базы и прибавлял вчерашние шаги к сегодняшним.
                      if (!today.equals(baseDate) || baseSteps == -1 || rawSteps < baseSteps) {
                          baseSteps = rawSteps;
                          prefs.edit()
                              .putString("steps_date", today)
                              .putString("steps_base_date", today)
                              .putInt("steps_base", baseSteps)
                              .putInt("steps_today", 0)
                              .apply();
                      } else {
                          int stepsToday = rawSteps - baseSteps;
                          prefs.edit().putInt("steps_today", Math.max(0, stepsToday)).apply();
                      }
                  } catch (Exception e) { }
              }

              private void incrementStepDetector() {
                  try {
                      SharedPreferences prefs = getSharedPreferences("fitflow_sensor_prefs", MODE_PRIVATE);
                      String today = new java.text.SimpleDateFormat("yyyy-MM-dd", java.util.Locale.US).format(new java.util.Date());
                      String savedDate = prefs.getString("steps_date", "");
                      if (!today.equals(savedDate)) {
                          prefs.edit().putString("steps_date", today).putInt("steps_today", 1).putInt("steps_base", 0).apply();
                      } else {
                          int current = prefs.getInt("steps_today", 0);
                          prefs.edit().putInt("steps_today", current + 1).apply();
                      }
                  } catch (Exception e) { }
              }

              @Override
              public void onAccuracyChanged(Sensor sensor, int accuracy) { }

              private void installBackupBridge() {
                  try {
                      WebView wv = getBridge().getWebView();
                      if (wv != null) {
                          BackupBridge bridge = new BackupBridge();
                          wv.addJavascriptInterface(bridge, "FitFlowExport");
                          wv.addJavascriptInterface(bridge, "AquaExport");
                      }
                  } catch (Exception e) {
                  }
              }

              public class BackupBridge {
                  /* Производитель устройства — для подсказки по автозапуску */
                  @JavascriptInterface
                  public String getManufacturer() {
                      try {
                          return Build.MANUFACTURER == null ? "" : Build.MANUFACTURER;
                      } catch (Exception e) {
                          return "";
                      }
                  }

                  /* Разрешены ли уведомления */
                  @JavascriptInterface
                  public boolean areNotificationsEnabled() {
                      try {
                          return NotificationManagerCompat.from(MainActivity.this).areNotificationsEnabled();
                      } catch (Exception e) {
                          return true;
                      }
                  }

                  /* Выключены ли оптимизации батареи для приложения */
                  @JavascriptInterface
                  public boolean isBatteryOptimizationIgnored() {
                      try {
                          PowerManager pm = (PowerManager) getSystemService(POWER_SERVICE);
                          return pm != null && pm.isIgnoringBatteryOptimizations(getPackageName());
                      } catch (Exception e) {
                          return true;
                      }
                  }

                  /* Снимок фоновой синхронизации: шаги и калории из Health Connect и телефона */
                  @JavascriptInterface
                  public String getHealthSyncSnapshot() {
                      try {
                          SharedPreferences prefs = getSharedPreferences("fitflow_sensor_prefs", MODE_PRIVATE);
                          String today = new java.text.SimpleDateFormat("yyyy-MM-dd", java.util.Locale.US).format(new java.util.Date());
                          String savedDate = prefs.getString("steps_date", "");
                          if (today.equals(savedDate)) {
                              int hcSteps = prefs.getInt("hc_steps_today", 0);   // шаги с часов
                              int hcTotalSteps = prefs.getInt("hc_total_steps_today", hcSteps); // всего в HC
                              float hcKcal = prefs.getFloat("hc_kcal_today", 0.0f);
                              int phoneSteps = prefs.getInt("steps_today", 0);
                              long lastSync = prefs.getLong("hc_last_sync_ts", 0);
                              long watchLastTs = prefs.getLong("hc_watch_last_ts", 0);
                              return String.format(java.util.Locale.US,
                                  "{\"date\":\"%s\",\"hcSteps\":%d,\"watchSteps\":%d,\"hcTotalSteps\":%d,\"hcKcal\":%.1f,\"phoneSteps\":%d,\"watchLastTs\":%d,\"lastSync\":%d}",
                                  today, hcSteps, hcSteps, hcTotalSteps, hcKcal, phoneSteps, watchLastTs, lastSync
                              );
                          }
                      } catch (Exception e) { }
                      return "{}";
                  }

                  /* Полная диагностика шагомера, датчиков и Health Connect */
                  @JavascriptInterface
                  public String getHealthDiagnosticsJson() {
                      try {
                          JSONObject diag = new JSONObject();
                          diag.put("androidApi", Build.VERSION.SDK_INT);
                          diag.put("manufacturer", Build.MANUFACTURER != null ? Build.MANUFACTURER : "");
                          diag.put("model", Build.MODEL != null ? Build.MODEL : "");

                          boolean actPerm = true;
                          if (Build.VERSION.SDK_INT >= 29) {
                              actPerm = checkSelfPermission(android.Manifest.permission.ACTIVITY_RECOGNITION) == android.content.pm.PackageManager.PERMISSION_GRANTED;
                          }
                          diag.put("activityRecognitionGranted", actPerm);

                          diag.put("hasStepCounterSensor", stepCounterSensor != null);
                          diag.put("hasStepDetectorSensor", stepDetectorSensor != null);

                          SharedPreferences prefs = getSharedPreferences("fitflow_sensor_prefs", MODE_PRIVATE);
                          String today = new java.text.SimpleDateFormat("yyyy-MM-dd", java.util.Locale.US).format(new java.util.Date());
                          String savedDate = prefs.getString("steps_date", "");
                          int stepsToday = today.equals(savedDate) ? prefs.getInt("steps_today", 0) : 0;
                          int baseSteps = prefs.getInt("steps_base", -1);
                          int hcSteps = today.equals(savedDate) ? prefs.getInt("hc_steps_today", 0) : 0;
                          int hcTotalSteps = today.equals(savedDate) ? prefs.getInt("hc_total_steps_today", hcSteps) : 0;
                          float hcKcal = today.equals(savedDate) ? prefs.getFloat("hc_kcal_today", 0.0f) : 0.0f;
                          long lastSync = prefs.getLong("hc_last_sync_ts", 0);

                          diag.put("todayDate", today);
                          diag.put("savedDate", savedDate);
                          diag.put("phoneStepsToday", stepsToday);
                          diag.put("phoneBaseSteps", baseSteps);
                          diag.put("hcStepsToday", hcSteps);
                          diag.put("hcWatchStepsToday", hcSteps);
                          diag.put("hcTotalStepsToday", hcTotalSteps);
                          diag.put("hcKcalToday", (double) hcKcal);
                          diag.put("hcSleepMin", prefs.getInt("hc_sleep_min", 0));
                          diag.put("hcSleepBedTime", prefs.getString("hc_sleep_bed", ""));
                          diag.put("hcSleepWakeTime", prefs.getString("hc_sleep_wake", ""));
                          diag.put("hcWatchLastTs", prefs.getLong("hc_watch_last_ts", 0));
                          diag.put("lastSyncTs", lastSync);

                          boolean hasHcIntent = false;
                          String[] hcIntents = new String[] {
                              "android.health.connect.action.HEALTH_HOME_SETTINGS",
                              "androidx.health.ACTION_HEALTH_CONNECT_SETTINGS"
                          };
                          for (String act : hcIntents) {
                              try {
                                  Intent testIntent = new Intent(act);
                                  if (getPackageManager().resolveActivity(testIntent, 0) != null) {
                                      hasHcIntent = true;
                                      break;
                                  }
                              } catch (Exception e) { }
                          }
                          if (Build.VERSION.SDK_INT >= 34) {
                              hasHcIntent = true;
                          }
                          diag.put("hasHealthConnectApp", hasHcIntent);
                          String currErr = HealthConnectHelper.getLastError();
                          if (currErr.isEmpty()) {
                              currErr = prefs.getString("hc_last_error", "");
                          }
                          diag.put("hcLastError", currErr);
                          boolean hcPerm = true;
                          try {
                              if (Build.VERSION.SDK_INT >= 34) {
                                  hcPerm = checkSelfPermission("android.permission.health.READ_STEPS") == android.content.pm.PackageManager.PERMISSION_GRANTED;
                              }
                          } catch (Exception e) { }
                          diag.put("healthConnectPermissionGranted", hcPerm);

                          return diag.toString();
                      } catch (Exception e) {
                          return "{\"error\":\"" + e.getMessage() + "\"}";
                      }
                  }

                  /* Шагомер: получить количество шагов телефона за сегодня */
                  @JavascriptInterface
                  public int getPhoneStepsToday() {
                      try {
                          SharedPreferences prefs = getSharedPreferences("fitflow_sensor_prefs", MODE_PRIVATE);
                          String today = new java.text.SimpleDateFormat("yyyy-MM-dd", java.util.Locale.US).format(new java.util.Date());
                          String savedDate = prefs.getString("steps_date", "");
                          if (today.equals(savedDate)) {
                              return prefs.getInt("steps_today", 0);
                          }
                          return 0;
                      } catch (Exception e) {
                          return 0;
                      }
                  }

                  /* Принудительный запрос к Health Connect через Kotlin-хелпер */
                  @JavascriptInterface
                  public void syncHealthConnectNow() {
                      new Thread(new Runnable() {
                          @Override
                          public void run() {
                              try {
                                  SharedPreferences prefs = getSharedPreferences("fitflow_sensor_prefs", MODE_PRIVATE);
                                  final int[] result = HealthConnectHelper.syncNow(getApplicationContext());
                                  // result = [шаги с часов, всего в HC, сон мин]
                                  final int watchSteps = result[0];
                                  final int totalSteps = result[1];
                                  final int sleepMin = result[2];
                                  final double kcal = watchSteps * 0.04;
                                  final long watchLastTs = HealthConnectHelper.getLastWatchEndMs();
                                  prefs.edit()
                                      .putInt("hc_steps_today", watchSteps)
                                      .putInt("hc_total_steps_today", totalSteps)
                                      .putFloat("hc_kcal_today", (float) kcal)
                                      .putInt("hc_sleep_min", sleepMin)
                                      .putString("hc_sleep_bed", HealthConnectHelper.getLastBedTime())
                                      .putString("hc_sleep_wake", HealthConnectHelper.getLastWakeTime())
                                      .putLong("hc_watch_last_ts", watchLastTs)
                                      .putLong("hc_last_sync_ts", System.currentTimeMillis())
                                      .putString("hc_last_error", HealthConnectHelper.getLastError())
                                      .apply();
                                  runOnUiThread(new Runnable() {
                                      @Override public void run() {
                                          try {
                                              WebView wv = getBridge() != null ? getBridge().getWebView() : null;
                                              if (wv != null) {
                                                  String js = String.format(java.util.Locale.US,
                                                      "window.onHealthConnectDataReceived && window.onHealthConnectDataReceived(%d, %.1f, %d, '%s', '%s', %d);",
                                                      watchSteps, (float) kcal, sleepMin,
                                                      HealthConnectHelper.getLastBedTime(), HealthConnectHelper.getLastWakeTime(), watchLastTs);
                                                  wv.evaluateJavascript(js, null);
                                              }
                                          } catch (Exception e) {}
                                      }
                                  });
                              } catch (Exception e) {}
                          }
                      }).start();
                  }

                  @JavascriptInterface
                  public String getHealthConnectLastError() {
                      String curr = HealthConnectHelper.getLastError();
                      if (!curr.isEmpty()) return curr;
                      SharedPreferences prefs = getSharedPreferences("fitflow_sensor_prefs", MODE_PRIVATE);
                      return prefs.getString("hc_last_error", "");
                  }

                  /* 0.8.0: сессии тренировок с часов из Health Connect → JS-подсказка */
                  @JavascriptInterface
                  public void syncHealthWorkoutsNow() {
                      new Thread(new Runnable() {
                          @Override public void run() {
                              try {
                                  final String json = HealthConnectHelper.readTodayWorkouts(getApplicationContext());
                                  runOnUiThread(new Runnable() {
                                      @Override public void run() {
                                          try {
                                              WebView wv = getBridge() != null ? getBridge().getWebView() : null;
                                              if (wv != null) {
                                                  wv.evaluateJavascript(
                                                      "window.onHealthWorkoutsReceived && window.onHealthWorkoutsReceived(" + JSONObject.quote(json) + ");", null);
                                              }
                                          } catch (Exception e) {}
                                      }
                                  });
                              } catch (Exception e) {}
                          }
                      }).start();
                  }

                  /* Запросить рантайм-разрешения Health Connect на Android 14+ */
                  @JavascriptInterface
                  public void requestHealthConnectPermissions() {
                      runOnUiThread(new Runnable() {
                          @Override
                          public void run() {
                              try {
                                  Intent intent = new Intent("android.health.connect.action.MANAGE_HEALTH_PERMISSIONS");
                                  intent.putExtra(Intent.EXTRA_PACKAGE_NAME, getPackageName());
                                  intent.addFlags(Intent.FLAG_ACTIVITY_NEW_TASK);
                                  startActivity(intent);
                                  return;
                              } catch (Exception e) { }
                              try {
                                  Intent intent = new Intent("android.health.connect.action.HEALTH_HOME_SETTINGS");
                                  intent.addFlags(Intent.FLAG_ACTIVITY_NEW_TASK);
                                  startActivity(intent);
                                  return;
                              } catch (Exception e2) { }
                              openHealthConnectSettings();
                          }
                      });
                  }

                  /* Открыть системные настройки разрешений Health Connect */
                  @JavascriptInterface
                  public void openHealthConnectSettings() {
                      runOnUiThread(new Runnable() {
                          @Override
                          public void run() {
                              String[] actions = new String[] {
                                  "android.health.connect.action.HEALTH_HOME_SETTINGS",
                                  "androidx.health.ACTION_HEALTH_CONNECT_SETTINGS"
                              };
                              for (String act : actions) {
                                  try {
                                      Intent intent = new Intent(act);
                                      intent.addFlags(Intent.FLAG_ACTIVITY_NEW_TASK);
                                      startActivity(intent);
                                      return;
                                  } catch (Exception e) { }
                              }
                              try {
                                  Intent intent = new Intent(android.provider.Settings.ACTION_APPLICATION_DETAILS_SETTINGS);
                                  intent.setData(Uri.fromParts("package", getPackageName(), null));
                                  intent.addFlags(Intent.FLAG_ACTIVITY_NEW_TASK);
                                  startActivity(intent);
                              } catch (Exception e2) { }
                          }
                      });
                  }

                  /* Запросить разрешение на физическую активность (шагомер) */
                  @JavascriptInterface
                  public void requestActivityRecognition() {
                      runOnUiThread(new Runnable() {
                          @Override
                          public void run() {
                              try {
                                  if (Build.VERSION.SDK_INT >= 29) {
                                      if (checkSelfPermission(android.Manifest.permission.ACTIVITY_RECOGNITION) != android.content.pm.PackageManager.PERMISSION_GRANTED) {
                                          requestPermissions(new String[]{android.Manifest.permission.ACTIVITY_RECOGNITION}, 1002);
                                      }
                                  }
                              } catch (Exception e) { }
                          }
                      });
                  }

                  /* Открыть нужный экран системных настроек */
                  @JavascriptInterface
                  public void openSettingsScreen(final String which) {
                      runOnUiThread(new Runnable() {
                          @Override
                          public void run() {
                              openSettings(which);
                          }
                      });
                  }

                  /* Офлайн-предпочтительный системный голосовой ввод Android */
                  @JavascriptInterface
                  public void startVoiceInput() {
                      runOnUiThread(new Runnable() {
                          @Override
                          public void run() {
                              try {
                                  Intent intent = new Intent(RecognizerIntent.ACTION_RECOGNIZE_SPEECH);
                                  intent.putExtra(RecognizerIntent.EXTRA_LANGUAGE_MODEL, RecognizerIntent.LANGUAGE_MODEL_FREE_FORM);
                                  intent.putExtra(RecognizerIntent.EXTRA_LANGUAGE, "ru-RU");
                                  intent.putExtra(RecognizerIntent.EXTRA_PREFER_OFFLINE, true);
                                  // VOICE_PAUSE_PATCH (0.3.39): «6 с» — НИЖНЯЯ граница сессии, не лимит!
                                  // Короткая фраза просто дождётся 6-й секунды (это floor). «Отключается
                                  // посреди ввода» — срабатывание по паузе на обдумывание: тишину подняли
                                  // 2.5 → 4.0/4.5 с — списки можно диктовать не торопясь.
                                  intent.putExtra(RecognizerIntent.EXTRA_SPEECH_INPUT_MINIMUM_LENGTH_MILLIS, 6000L);
                                  intent.putExtra(RecognizerIntent.EXTRA_SPEECH_INPUT_COMPLETE_SILENCE_LENGTH_MILLIS, 4500L);
                                  intent.putExtra(RecognizerIntent.EXTRA_SPEECH_INPUT_POSSIBLY_COMPLETE_SILENCE_LENGTH_MILLIS, 4000L);
                                  intent.putExtra(RecognizerIntent.EXTRA_PROMPT, "Расскажите о воде, еде и активности");
                                  startActivityForResult(intent, REQ_VOICE_INPUT);
                              } catch (Exception e) {
                                  notifyVoiceResult("");
                              }
                          }
                      });
                  }

                  /* Закрыть приложение, если пользователь не принял условия */
                  @JavascriptInterface
                  public void closeApp() {
                      runOnUiThread(new Runnable() {
                          @Override
                          public void run() {
                              try { finishAndRemoveTask(); } catch (Exception e) { finish(); }
                          }
                      });
                  }

                  /* Обновить компактный Android-виджет из WebView */
                  @JavascriptInterface
                  public void updateWidget(final String json) {
                      runOnUiThread(new Runnable() {
                          @Override
                          public void run() {
                              try {
                                  JSONObject data = new JSONObject(json == null ? "{}" : json);
                                  SharedPreferences prefs = getSharedPreferences("fitflow_widget", MODE_PRIVATE);
                                  prefs.edit()
                                      .putInt("waterTotal", data.optInt("waterTotal", 0))
                                      .putInt("waterGoal", data.optInt("waterGoal", 2500))
                                      .putInt("foodTotal", data.optInt("foodTotal", 0))
                                      .putInt("foodGoal", data.optInt("foodGoal", 2000))
                                      .putInt("activityMinutes", data.optInt("activityMinutes", 0))
                                      .putInt("stepsToday", data.optInt("stepsToday", 0))
                                      .putString("profileName", data.optString("profileName", "Мой профиль"))
                                      .putString("date", data.optString("date", ""))
                                      // 0.5.5/0.5.6: момент последней записи воды — ресивер сам
                                      // пропускает ближайшее напоминание (настройки «пауза» больше нет)
                                      .putLong("lastWaterAt", data.optLong("lastWaterAt", prefs.getLong("lastWaterAt", 0)))
                                      .apply();
                                  // 0.7.13: приоритет источников шагов — в сенсорные prefs,
                                  // чтобы фоновый HealthSyncReceiver мог разрешать шаги без WebView.
                                  getSharedPreferences("fitflow_sensor_prefs", MODE_PRIVATE).edit()
                                      .putString("health_priority", data.optString("priority", "auto"))
                                      .apply();
                                  FitFlowWidgetProvider.updateAll(MainActivity.this);
                              } catch (Exception e) {
                              }
                          }
                      });
                  }

                  @JavascriptInterface
                  public void saveBackup(final String json, final String fileName) {
                      pendingBackupJson = json;
                      final String name = (fileName == null || fileName.length() == 0)
                              ? "fitflow-backup.json"
                              : fileName;
                      runOnUiThread(new Runnable() {
                          @Override
                          public void run() {
                              try {
                                  Intent intent = new Intent(Intent.ACTION_CREATE_DOCUMENT);
                                  intent.addCategory(Intent.CATEGORY_OPENABLE);
                                  intent.setType("application/json");
                                  intent.putExtra(Intent.EXTRA_TITLE, name);
                                  startActivityForResult(intent, REQ_SAVE_BACKUP);
                              } catch (Exception e) {
                                  pendingBackupJson = null;
                                  notifyBackupResult(false, "Системное окно сохранения недоступно");
                              }
                          }
                      });
                  }

                  /* Нативные напоминания о воде: кнопка «+250 мл» прямо в шторке,
                     без открытия приложения; виджет обновляется автоматически */
                  @JavascriptInterface
                  public void scheduleWaterRemindersNative(final String timesCsv) {
                      runOnUiThread(new Runnable() {
                          @Override
                          public void run() {
                              try { WaterReminderReceiver.setSchedule(MainActivity.this, timesCsv); } catch (Exception e) { }
                          }
                      });
                  }

                  @JavascriptInterface
                  public void cancelWaterRemindersNative() {
                      runOnUiThread(new Runnable() {
                          @Override
                          public void run() {
                              try { WaterReminderReceiver.clearSchedule(MainActivity.this); } catch (Exception e) { }
                          }
                      });
                  }
              }

              @Override
              protected void onActivityResult(int requestCode, int resultCode, Intent data) {
                  if (requestCode == REQ_VOICE_INPUT) {
                      String text = "";
                      try {
                          if (resultCode == RESULT_OK && data != null) {
                              ArrayList<String> results = data.getStringArrayListExtra(RecognizerIntent.EXTRA_RESULTS);
                              if (results != null && !results.isEmpty()) text = results.get(0);
                          }
                      } catch (Exception e) {
                      }
                      notifyVoiceResult(text);
                      return;
                  }
                  if (requestCode == REQ_SAVE_BACKUP) {
                      String json = pendingBackupJson;
                      pendingBackupJson = null;
                      Uri uri = (data == null) ? null : data.getData();
                      if (resultCode == RESULT_OK && uri != null && json != null) {
                          OutputStream os = null;
                          try {
                              os = getContentResolver().openOutputStream(uri, "w");
                              os.write(json.getBytes("UTF-8"));
                              os.flush();
                              notifyBackupResult(true, "Резервная копия сохранена");
                          } catch (Exception e) {
                              notifyBackupResult(false, "Не удалось записать файл");
                          } finally {
                              try { if (os != null) os.close(); } catch (Exception e2) { }
                          }
                      } else {
                          notifyBackupResult(false, "Сохранение отменено");
                      }
                      return;
                  }
                  super.onActivityResult(requestCode, resultCode, data);
              }

              private void notifyVoiceResult(final String text) {
                  runOnUiThread(new Runnable() {
                      @Override
                      public void run() {
                          try {
                              WebView wv = getBridge().getWebView();
                              if (wv == null) return;
                              String js = "window.onVoiceInputResult && window.onVoiceInputResult(" + JSONObject.quote(text) + ");";
                              wv.evaluateJavascript(js, null);
                          } catch (Exception e) {
                          }
                      }
                  });
              }

              private void notifyBackupResult(final boolean ok, final String message) {
                  runOnUiThread(new Runnable() {
                      @Override
                      public void run() {
                          try {
                              WebView wv = getBridge().getWebView();
                              if (wv == null) return;
                              String js = "window.onBackupSaveResult && window.onBackupSaveResult("
                                      + ok + "," + JSONObject.quote(message) + ");";
                              wv.evaluateJavascript(js, null);
                          } catch (Exception e) {
                          }
                      }
                  });
              }

              /* ===== Экраны системных настроек ===== */

              private void openSettings(String which) {
                  if ("battery".equals(which)) {
                      boolean granted = false;
                      try {
                          PowerManager pm = (PowerManager) getSystemService(POWER_SERVICE);
                          granted = pm != null && pm.isIgnoringBatteryOptimizations(getPackageName());
                      } catch (Exception e) {
                      }

                      if (!granted) {
                          if (tryStart(new Intent(
                                  Settings.ACTION_REQUEST_IGNORE_BATTERY_OPTIMIZATIONS,
                                  Uri.parse("package:" + getPackageName())))) return;
                      }

                      if (tryStart(new Intent(
                              Settings.ACTION_IGNORE_BATTERY_OPTIMIZATION_SETTINGS))) return;
                      openAppDetails();
                      return;
                  }

                  if ("notifications".equals(which)) {
                      if (Build.VERSION.SDK_INT >= Build.VERSION_CODES.O) {
                          Intent i = new Intent(Settings.ACTION_APP_NOTIFICATION_SETTINGS);
                          i.putExtra(Settings.EXTRA_APP_PACKAGE, getPackageName());
                          if (tryStart(i)) return;
                      }
                      openAppDetails();
                      return;
                  }

                  if ("autostart".equals(which)) {
                      if (tryAutostart()) return;
                      openAppDetails();
                      return;
                  }
                  openAppDetails();
              }

              private boolean tryAutostart() {
                  String[][] targets = new String[][] {
                      { "com.miui.securitycenter", "com.miui.permcenter.autostart.AutoStartManagementActivity" },
                      { "com.huawei.systemmanager", "com.huawei.systemmanager.startupmgr.ui.StartupNormalAppListActivity" },
                      { "com.huawei.systemmanager", "com.huawei.systemmanager.optimize.process.ProtectActivity" },
                      { "com.coloros.safecenter", "com.coloros.safecenter.permission.startup.StartupAppListActivity" },
                      { "com.coloros.safecenter", "com.coloros.safecenter.startupapp.StartupAppListActivity" },
                      { "com.oppo.safe", "com.oppo.safe.permission.startup.StartupAppListActivity" },
                      { "com.vivo.permissionmanager", "com.vivo.permissionmanager.activity.BgStartUpManagerActivity" },
                      { "com.iqoo.secure", "com.iqoo.secure.ui.phoneoptimize.AddWhiteListActivity" },
                      { "com.samsung.android.lool", "com.samsung.android.sm.ui.battery.BatteryActivity" },
                      { "com.letv.android.letvsafe", "com.letv.android.letvsafe.AutobootManageActivity" },
                      { "com.asus.mobilemanager", "com.asus.mobilemanager.entry.FunctionActivity" }
                  };
                  for (String[] t : targets) {
                      Intent i = new Intent();
                      i.setClassName(t[0], t[1]);
                      if (tryStart(i)) return true;
                  }
                  return false;
              }

              private void openAppDetails() {
                  tryStart(new Intent(
                          Settings.ACTION_APPLICATION_DETAILS_SETTINGS,
                          Uri.parse("package:" + getPackageName())));
              }

              private boolean tryStart(Intent intent) {
                  try {
                      intent.addFlags(Intent.FLAG_ACTIVITY_NEW_TASK);
                      startActivity(intent);
                      return true;
                  } catch (Exception e) {
                      return false;
                  }
              }
              
              /* ===== Разрешения ===== */

              private void requestExactAlarmPermission() {
                  if (Build.VERSION.SDK_INT >= Build.VERSION_CODES.S) {
                      try {
                          AlarmManager am = (AlarmManager) getSystemService(ALARM_SERVICE);
                          if (am != null && !am.canScheduleExactAlarms()) {
                              Intent intent = new Intent(Settings.ACTION_REQUEST_SCHEDULE_EXACT_ALARM);
                              intent.setData(Uri.parse("package:" + getPackageName()));
                              startActivity(intent);
                          }
                      } catch (Exception e) {
                      }
                  }
              }

              private void requestIgnoreBatteryOptimizations() {
                  try {
                      PowerManager pm = (PowerManager) getSystemService(POWER_SERVICE);
                      if (pm != null && !pm.isIgnoringBatteryOptimizations(getPackageName())) {
                          Intent intent = new Intent(Settings.ACTION_REQUEST_IGNORE_BATTERY_OPTIMIZATIONS);
                          intent.setData(Uri.parse("package:" + getPackageName()));
                          startActivity(intent);
                      }
                  } catch (Exception e) {
                  }
              }
          }
          EOF

          cat > android/app/src/main/java/com/fitflow/app/FitFlowWidgetProvider.java << 'EOF'
          package com.fitflow.app;

          import android.app.PendingIntent;
          import android.appwidget.AppWidgetManager;
          import android.appwidget.AppWidgetProvider;
          import android.content.ComponentName;
          import android.content.Context;
          import android.content.Intent;
          import android.content.SharedPreferences;
          import android.os.Bundle;
          import android.widget.RemoteViews;

          public class FitFlowWidgetProvider extends AppWidgetProvider {
              private static final String ACTION_MIDNIGHT = "com.fitflow.app.WIDGET_MIDNIGHT_REFRESH";

              public static void updateAll(Context context) {
                  AppWidgetManager manager = AppWidgetManager.getInstance(context);
                  ComponentName component = new ComponentName(context, FitFlowWidgetProvider.class);
                  int[] ids = manager.getAppWidgetIds(component);
                  for (int id : ids) updateWidget(context, manager, id);
                  scheduleMidnightRefresh(context);
              }

              /* 0.5.5 (полевой баг: утром виджет показывал вчерашний день):
                 самоподдерживающийся будильник на ближайшую полночь — updateWidget
                 сам обнулит чужую дату (страж 0.4.12) и перевооружит следующий. */
              private static void scheduleMidnightRefresh(Context context) {
                  try {
                      java.util.Calendar cal = java.util.Calendar.getInstance();
                      cal.add(java.util.Calendar.DAY_OF_YEAR, 1);
                      cal.set(java.util.Calendar.HOUR_OF_DAY, 0);
                      cal.set(java.util.Calendar.MINUTE, 0);
                      cal.set(java.util.Calendar.SECOND, 5);
                      cal.set(java.util.Calendar.MILLISECOND, 0);
                      Intent i = new Intent(context, FitFlowWidgetProvider.class);
                      i.setAction(ACTION_MIDNIGHT);
                      android.app.PendingIntent pi = android.app.PendingIntent.getBroadcast(context, 9, i,
                          android.app.PendingIntent.FLAG_UPDATE_CURRENT | android.app.PendingIntent.FLAG_IMMUTABLE);
                      android.app.AlarmManager am = (android.app.AlarmManager) context.getSystemService(Context.ALARM_SERVICE);
                      if (am == null) return;
                      if (android.os.Build.VERSION.SDK_INT >= 31 && !am.canScheduleExactAlarms()) {
                          am.setAndAllowWhileIdle(android.app.AlarmManager.RTC_WAKEUP, cal.getTimeInMillis(), pi);
                      } else {
                          am.setExactAndAllowWhileIdle(android.app.AlarmManager.RTC_WAKEUP, cal.getTimeInMillis(), pi);
                      }
                  } catch (Exception e) { }
              }

              @Override
              public void onUpdate(Context context, AppWidgetManager manager, int[] ids) {
                  for (int id : ids) updateWidget(context, manager, id);
              }

              @Override
              public void onAppWidgetOptionsChanged(Context context, AppWidgetManager manager, int id, Bundle options) {
                  updateWidget(context, manager, id);
              }

              private static void updateWidget(Context context, AppWidgetManager manager, int id) {
                  SharedPreferences prefs = context.getSharedPreferences("fitflow_widget", Context.MODE_PRIVATE);
                  int water = prefs.getInt("waterTotal", 0);
                  int waterGoal = prefs.getInt("waterGoal", 2500);
                  int food = prefs.getInt("foodTotal", 0);
                  int foodGoal = prefs.getInt("foodGoal", 2000);
                  int activity = prefs.getInt("activityMinutes", 0);
                  // 0.4.12 (полевой баг: утром виджет показывал вчерашние цифры):
                  // прогресс дня валиден только для своей даты; новый день —
                  // честные нули, пока приложение не пришлёт свежие значения.
                  String savedDate = prefs.getString("date", "");
                  String today = new java.text.SimpleDateFormat("yyyy-MM-dd").format(new java.util.Date());
                  if (savedDate == null || !savedDate.equals(today)) { water = 0; food = 0; activity = 0; }
                  String profileName = prefs.getString("profileName", "Мой профиль");
                  if (profileName == null || profileName.trim().length() == 0) profileName = "Мой профиль";

                  Bundle options = manager.getAppWidgetOptions(id);
                  int maxWidth = options.getInt(AppWidgetManager.OPTION_APPWIDGET_MAX_WIDTH, 180);
                  int maxHeight = options.getInt(AppWidgetManager.OPTION_APPWIDGET_MAX_HEIGHT, 110);
                  boolean large = maxWidth >= 250 || maxHeight >= 180;
                  RemoteViews views = new RemoteViews(context.getPackageName(), large ? R.layout.fitflow_widget_large : R.layout.fitflow_widget);
                  views.setTextViewText(R.id.widget_profile, "Профиль: " + profileName);
                  views.setTextViewText(R.id.widget_water, "Вода: " + water + " / " + waterGoal + " мл");
                  views.setTextViewText(R.id.widget_food, "Питание: " + food + " / " + foodGoal + " ккал");
                  views.setProgressBar(R.id.widget_water_bar, 100, Math.min(100, Math.round(waterGoal > 0 ? water * 100f / waterGoal : 0)), false);
                  views.setProgressBar(R.id.widget_food_bar, 100, Math.min(100, Math.round(foodGoal > 0 ? food * 100f / foodGoal : 0)), false);
                  int steps = resolveWidgetSteps(context);
                  views.setTextViewText(R.id.widget_steps, "Шаги: " + steps);
                  if (large) {
                      views.setTextViewText(R.id.widget_water_pct, Math.min(100, Math.round(waterGoal > 0 ? water * 100f / waterGoal : 0)) + "% цели");
                      views.setTextViewText(R.id.widget_food_pct, Math.min(100, Math.round(foodGoal > 0 ? food * 100f / foodGoal : 0)) + "% цели");
                      String actStr = "Активность: " + formatActivity(activity);
                      if (steps > 0) actStr += " · " + steps + " ш.";
                      views.setTextViewText(R.id.widget_activity, actStr);
                  }

                  Intent launch = context.getPackageManager().getLaunchIntentForPackage(context.getPackageName());
                  if (launch == null) launch = new Intent(context, MainActivity.class);
                  launch.setFlags(Intent.FLAG_ACTIVITY_NEW_TASK | Intent.FLAG_ACTIVITY_CLEAR_TOP);
                  PendingIntent pending = PendingIntent.getActivity(context, 0, launch,
                      PendingIntent.FLAG_UPDATE_CURRENT | PendingIntent.FLAG_IMMUTABLE);
                  views.setOnClickPendingIntent(R.id.widget_root, pending);

                  Intent waterBtn = new Intent(context, FitFlowWidgetProvider.class);
                  waterBtn.setAction("com.fitflow.app.ADD_WATER_250");
                  PendingIntent waterPi = PendingIntent.getBroadcast(context, 1, waterBtn,
                      PendingIntent.FLAG_UPDATE_CURRENT | PendingIntent.FLAG_IMMUTABLE);
                  views.setOnClickPendingIntent(R.id.widget_water_btn, waterPi);

                  Intent recordBtn = new Intent(context, MainActivity.class);
                  recordBtn.putExtra("widget_action", "smart_entry");
                  recordBtn.setFlags(Intent.FLAG_ACTIVITY_NEW_TASK | Intent.FLAG_ACTIVITY_CLEAR_TOP);
                  PendingIntent recordPi = PendingIntent.getActivity(context, 2, recordBtn,
                      PendingIntent.FLAG_UPDATE_CURRENT | PendingIntent.FLAG_IMMUTABLE);
                  views.setOnClickPendingIntent(R.id.widget_record_btn, recordPi);
                  manager.updateAppWidget(id, views);
              }

              private static String formatActivity(int minutes) {
                  if (minutes <= 0) return "0 мин";
                  int hours = minutes / 60;
                  int rest = minutes % 60;
                  if (hours == 0) return minutes + " мин";
                  if (rest == 0) return hours + " ч";
                  return hours + " ч " + rest + " мин";
              }

              /* 0.7.13: шаги виджета по приоритету пользователя (часы vs телефон) —
                 без суммы источников и без устаревшего значения из JS-кэша.
                 phone_only → аппаратный шагомер; health_connect_only → часы;
                 auto → часы при наличии, иначе телефон. */
              private static int resolveWidgetSteps(Context context) {
                  SharedPreferences s = context.getSharedPreferences("fitflow_sensor_prefs", Context.MODE_PRIVATE);
                  String today = new java.text.SimpleDateFormat("yyyy-MM-dd", java.util.Locale.US).format(new java.util.Date());
                  String savedDate = s.getString("steps_date", "");
                  int phone = today.equals(savedDate) ? s.getInt("steps_today", 0) : 0;
                  int watch = today.equals(savedDate) ? s.getInt("hc_steps_today", 0) : 0;
                  String priority = s.getString("health_priority", "auto");
                  if ("phone_only".equals(priority)) return phone;
                  if ("health_connect_only".equals(priority)) return watch;
                  return watch > 0 ? watch : phone;
              }

              @Override
              public void onReceive(Context context, Intent intent) {
                  super.onReceive(context, intent);
                  // 0.5.5: полночь — перерисовать (дата-страж обнулит вчерашнее)
                  // и перевооружить будильник на следующую полночь.
                  if (intent != null && ACTION_MIDNIGHT.equals(intent.getAction())) {
                      updateAll(context);
                      return;
                  }
                  if (intent != null && "com.fitflow.app.ADD_WATER_250".equals(intent.getAction())) {
                      SharedPreferences prefs = context.getSharedPreferences("fitflow_widget", Context.MODE_PRIVATE);
                      // 0.4.12: вчерашний остаток не наследуем — за полночь база
                      // начинается с нуля и дата в prefs поднимается до сегодня.
                      String savedDate = prefs.getString("date", "");
                      String today = new java.text.SimpleDateFormat("yyyy-MM-dd").format(new java.util.Date());
                      boolean fresh = savedDate != null && savedDate.equals(today);
                      int waterTotal = (fresh ? prefs.getInt("waterTotal", 0) : 0) + 250;
                      int pendingAdd = (fresh ? prefs.getInt("pendingWaterAdd", 0) : 0) + 250;
                      prefs.edit()
                          .putInt("waterTotal", waterTotal)
                          .putInt("pendingWaterAdd", pendingAdd)
                          .putString("date", today)
                          .putLong("lastWaterAt", System.currentTimeMillis()) // 0.5.5
                          .apply();
                      updateAll(context);
                      // Если на экране висит напоминание о воде — обновить его текст вживую
                      try { WaterReminderReceiver.onWaterAdded(context); } catch (Exception e) { }
                  }
              }
          }
          EOF

          cat > android/app/src/main/java/com/fitflow/app/WaterReminderReceiver.java << 'EOF'
          package com.fitflow.app;

          import android.app.AlarmManager;
          import android.app.NotificationChannel;
          import android.app.NotificationManager;
          import android.app.PendingIntent;
          import android.content.BroadcastReceiver;
          import android.content.Context;
          import android.content.Intent;
          import android.content.SharedPreferences;
          import android.os.Build;

          import androidx.core.app.NotificationCompat;
          import androidx.core.app.NotificationManagerCompat;
          import androidx.core.content.ContextCompat;

          import java.util.ArrayList;
          import java.util.Calendar;
          import java.util.Collections;

          /* Напоминания о воде с кнопкой «+250 мл» прямо в шторке уведомлений.
             Расписание приходит из приложения (CSV минут от полуночи) и хранится
             в SharedPreferences, поэтому после перезагрузки телефона будильник
             перевооружается через BOOT_COMPLETED без открытия приложения. */
          public class WaterReminderReceiver extends BroadcastReceiver {
              public static final int WATER_NOTIFICATION_ID = 4250;
              private static final int ALARM_REQUEST_CODE = 4250;
              private static final String PREFS = "fitflow_reminders";
              private static final String KEY_TIMES = "waterTimes";
              private static final String CHANNEL_ID = "fitflow_water_reminders_native";
              private static final String ACTION_ALARM = "com.fitflow.app.WATER_REMINDER_ALARM";
              // 0.4.13 (полевой баг: «смахиваю уведомление — приходит повторно»):
              // смахивание = «увидел», даём 45 минут тишины вместо новых сигналов.
              private static final String ACTION_DISMISS = "com.fitflow.app.WATER_REMINDER_DISMISSED";
              private static final String KEY_MUTE_UNTIL = "muteUntilMs";
              private static final long DISMISS_MUTE_MS = 45L * 60L * 1000L;

              @Override
              public void onReceive(Context context, Intent intent) {
                  String action = (intent == null) ? "" : intent.getAction();
                  if (Intent.ACTION_BOOT_COMPLETED.equals(action)) {
                      rescheduleFromPrefs(context);
                      return;
                  }
                  // 0.4.13: смахнутое напоминание не возвращается минимум 45 минут.
                  if (ACTION_DISMISS.equals(action)) {
                      prefs(context).edit().putLong(KEY_MUTE_UNTIL, System.currentTimeMillis() + DISMISS_MUTE_MS).apply();
                      rescheduleFromPrefs(context);
                      return;
                  }
                  // 0.5.6 (решение владельца вместо настройки «пауза»): вода записана
                  // недавно — ближайший сигнал пропускаем; сетка НЕ сдвигается, следующее
                  // напоминание придёт в свой обычный час. Порог — сам интервал сетки:
                  // пьёшь не реже, чем напоминает FitFlow, — значит, не донимаем.
                  SharedPreferences wprefs = context.getSharedPreferences("fitflow_widget", Context.MODE_PRIVATE);
                  long lastWaterAt = wprefs.getLong("lastWaterAt", 0);
                  if (lastWaterAt > 0
                          && System.currentTimeMillis() - lastWaterAt < scheduleIntervalMs(context)) {
                      rescheduleFromPrefs(context);
                      return;
                  }
                  // Время пришло: показать напоминание и перевооружить на следующее
                  postWaterNotification(context, false);
                  rescheduleFromPrefs(context);
              }

              /* Установить расписание (вызывается из JS-моста MainActivity) */
              public static void setSchedule(Context context, String csv) {
                  prefs(context).edit().putString(KEY_TIMES, csv == null ? "" : csv).apply();
                  rescheduleFromPrefs(context);
              }

              /* Полностью выключить напоминания */
              public static void clearSchedule(Context context) {
                  prefs(context).edit().remove(KEY_TIMES).apply();
                  AlarmManager am = (AlarmManager) context.getSystemService(Context.ALARM_SERVICE);
                  if (am != null) am.cancel(alarmPi(context));
                  try { NotificationManagerCompat.from(context).cancel(WATER_NOTIFICATION_ID); } catch (Exception e) { }
              }

              /* Кнопка «+250 мл» нажата (или вода добавлена с виджета):
                 обновить текст уже показанного напоминания. Если напоминания
                 на экране нет — новое не создаём. */
              public static void onWaterAdded(Context context) {
                  if (Build.VERSION.SDK_INT >= 23) {
                      boolean active = false;
                      try {
                          NotificationManager nm = (NotificationManager) context.getSystemService(Context.NOTIFICATION_SERVICE);
                          if (nm != null) {
                              for (android.service.notification.StatusBarNotification n : nm.getActiveNotifications()) {
                                  if (n.getId() == WATER_NOTIFICATION_ID) { active = true; break; }
                              }
                          }
                      } catch (Exception e) { }
                      if (!active) return;
                  }
                  postWaterNotification(context, true);
              }

              private static SharedPreferences prefs(Context c) {
                  return c.getSharedPreferences(PREFS, Context.MODE_PRIVATE);
              }

              private static PendingIntent alarmPi(Context c) {
                  Intent i = new Intent(c, WaterReminderReceiver.class);
                  i.setAction(ACTION_ALARM);
                  return PendingIntent.getBroadcast(c, ALARM_REQUEST_CODE, i,
                      PendingIntent.FLAG_UPDATE_CURRENT | PendingIntent.FLAG_IMMUTABLE);
              }

              private static void rescheduleFromPrefs(Context context) {
                  AlarmManager am = (AlarmManager) context.getSystemService(Context.ALARM_SERVICE);
                  if (am == null) return;
                  PendingIntent pi = alarmPi(context);
                  long next = computeNextTrigger(context, System.currentTimeMillis());
                  if (next <= 0) { am.cancel(pi); return; }
                  try {
                      if (Build.VERSION.SDK_INT >= 31 && !am.canScheduleExactAlarms()) {
                          am.setAndAllowWhileIdle(AlarmManager.RTC_WAKEUP, next, pi);
                      } else {
                          am.setExactAndAllowWhileIdle(AlarmManager.RTC_WAKEUP, next, pi);
                      }
                  } catch (SecurityException se) {
                      try { am.setAndAllowWhileIdle(AlarmManager.RTC_WAKEUP, next, pi); } catch (Exception e2) { }
                  } catch (Exception e) { }
              }

              /* 0.5.6: интервал сетки в мс — минимальный положительный зазор между
                 соседними слотами сохранённого расписания (оно в минутах от полуночи,
                 через запятую: "480,570,660,..."). Если вычислить нельзя — 90 минут. */
              private static long scheduleIntervalMs(Context context) {
                  try {
                      String csv = prefs(context).getString(KEY_TIMES, "");
                      if (csv == null || csv.trim().length() == 0) return 90L * 60000L;
                      ArrayList<Integer> mins = new ArrayList<>();
                      for (String part : csv.split(",")) {
                          try {
                              int v = Integer.parseInt(part.trim());
                              if (v >= 0 && v < 1440) mins.add(v);
                          } catch (Exception e) { }
                      }
                      Collections.sort(mins);
                      int best = 0;
                      for (int i = 1; i < mins.size(); i++) {
                          int diff = mins.get(i) - mins.get(i - 1);
                          if (diff > 0 && (best == 0 || diff < best)) best = diff;
                      }
                      if (best > 0) return best * 60000L;
                  } catch (Exception e) { }
                  return 90L * 60000L;
              }

              static long computeNextTrigger(Context context, long nowMs) {
                  String csv = prefs(context).getString(KEY_TIMES, "");
                  if (csv == null || csv.trim().length() == 0) return -1;
                  // 0.4.13: в период тишины после смахивания ближайшие слоты пропускаются.
                  long mutedUntil = prefs(context).getLong(KEY_MUTE_UNTIL, 0);
                  if (mutedUntil > nowMs) nowMs = mutedUntil;
                  ArrayList<Integer> times = new ArrayList<>();
                  for (String part : csv.split(",")) {
                      try {
                          int v = Integer.parseInt(part.trim());
                          if (v >= 0 && v < 1440) times.add(v);
                      } catch (Exception e) { }
                  }
                  if (times.isEmpty()) return -1;
                  Collections.sort(times);
                  Calendar now = Calendar.getInstance();
                  now.setTimeInMillis(nowMs);
                  int nowMin = now.get(Calendar.HOUR_OF_DAY) * 60 + now.get(Calendar.MINUTE);
                  int chosen = -1;
                  for (int t : times) { if (t > nowMin) { chosen = t; break; } }
                  int daysAdd = 0;
                  if (chosen < 0) { chosen = times.get(0); daysAdd = 1; }
                  Calendar cal = Calendar.getInstance();
                  cal.setTimeInMillis(nowMs);
                  cal.add(Calendar.DAY_OF_YEAR, daysAdd);
                  cal.set(Calendar.HOUR_OF_DAY, chosen / 60);
                  cal.set(Calendar.MINUTE, chosen % 60);
                  cal.set(Calendar.SECOND, 0);
                  cal.set(Calendar.MILLISECOND, 0);
                  return cal.getTimeInMillis();
              }

              private static void ensureChannel(Context context) {
                  if (Build.VERSION.SDK_INT >= 26) {
                      try {
                          NotificationManager nm = (NotificationManager) context.getSystemService(Context.NOTIFICATION_SERVICE);
                          if (nm == null) return;
                          if (nm.getNotificationChannel(CHANNEL_ID) == null) {
                              NotificationChannel ch = new NotificationChannel(CHANNEL_ID, "FitFlow: вода", NotificationManager.IMPORTANCE_DEFAULT);
                              ch.setDescription("Напоминания выпить воду с быстрой записью +250 мл");
                              nm.createNotificationChannel(ch);
                          }
                      } catch (Exception e) { }
                  }
              }

              private static void postWaterNotification(Context context, boolean justAdded) {
                  ensureChannel(context);
                  SharedPreferences w = context.getSharedPreferences("fitflow_widget", Context.MODE_PRIVATE);
                  int total = w.getInt("waterTotal", 0);
                  int goal = w.getInt("waterGoal", 2500);
                  // 0.4.12 (полевой баг «утреннее уведомление со вчерашними
                  // цифрами»): прогресс — только за свою дату, иначе честный 0.
                  String savedDate = w.getString("date", "");
                  String today = new java.text.SimpleDateFormat("yyyy-MM-dd").format(new java.util.Date());
                  if (savedDate == null || !savedDate.equals(today)) total = 0;

                  Intent open = context.getPackageManager().getLaunchIntentForPackage(context.getPackageName());
                  if (open == null) open = new Intent(context, MainActivity.class);
                  open.setFlags(Intent.FLAG_ACTIVITY_NEW_TASK | Intent.FLAG_ACTIVITY_CLEAR_TOP);
                  PendingIntent openPi = PendingIntent.getActivity(context, 10, open,
                      PendingIntent.FLAG_UPDATE_CURRENT | PendingIntent.FLAG_IMMUTABLE);

                  // Та же броадкаст-цепочка, что и у кнопки виджета «+250 мл»
                  Intent add = new Intent(context, FitFlowWidgetProvider.class);
                  add.setAction("com.fitflow.app.ADD_WATER_250");
                  PendingIntent addPi = PendingIntent.getBroadcast(context, 42, add,
                      PendingIntent.FLAG_UPDATE_CURRENT | PendingIntent.FLAG_IMMUTABLE);

                  // 0.5.5 (владелец): «⚙️ Настроить» — сразу в раздел напоминаний
                  // о воде с подсветкой (маршрут делает app.js: openNotificationSettings).
                  Intent nset = new Intent(context, MainActivity.class);
                  nset.putExtra("widget_action", "notif_settings_water");
                  nset.setFlags(Intent.FLAG_ACTIVITY_NEW_TASK | Intent.FLAG_ACTIVITY_CLEAR_TOP);
                  PendingIntent nsetPi = PendingIntent.getActivity(context, 44, nset,
                      PendingIntent.FLAG_UPDATE_CURRENT | PendingIntent.FLAG_IMMUTABLE);

                  String text = justAdded
                      ? "✓ Записано! Сегодня: " + total + " из " + goal + " мл."
                      // 0.5.7 (владелец: «не виден весь текст, последняя фраза —
                      // записывать можно...»): короткая строка видна целиком.
                      : "Сегодня: " + total + " из " + goal + " мл.";

                  // 0.4.13: смахивание ловим и глушим повторы (ветка ACTION_DISMISS выше).
                  Intent dismiss = new Intent(context, WaterReminderReceiver.class);
                  dismiss.setAction(ACTION_DISMISS);
                  PendingIntent dismissPi = PendingIntent.getBroadcast(context, 43, dismiss,
                      PendingIntent.FLAG_UPDATE_CURRENT | PendingIntent.FLAG_IMMUTABLE);

                  NotificationCompat.Builder b = new NotificationCompat.Builder(context, CHANNEL_ID)
                      .setSmallIcon(R.drawable.ic_stat_icon)
                      .setContentTitle("💧 Время выпить воду")
                      .setContentText(text)
                      .setColor(0xFF00696B)
                      .setContentIntent(openPi)
                      // 0.4.13 (полевой баг «после +250 мл ещё 1–2 сигнала без новых
                      // уведомлений»): обновление текста того же уведомления звучало
                      // как новое. Только первый показ будит, правки текста — молча.
                      .setOnlyAlertOnce(true)
                      .setDeleteIntent(dismissPi)
                      .setAutoCancel(true)
                      .setCategory(NotificationCompat.CATEGORY_REMINDER)
                      .setPriority(NotificationCompat.PRIORITY_DEFAULT)
                      .addAction(R.drawable.ic_stat_icon, "+ 250 мл воды", addPi)
                      .addAction(R.drawable.ic_stat_icon, "⚙️ Настроить", nsetPi);
                  try {
                      NotificationManagerCompat.from(context).notify(WATER_NOTIFICATION_ID, b.build());
                      // 0.4.15 (просьба владельца): подтверждение «+250 мл» показывает
                      // итог — и САМО уходит через 2 секунды, смахивать руками не нужно.
                      if (justAdded) {
                          final Context fctx = context.getApplicationContext();
                          new android.os.Handler(android.os.Looper.getMainLooper()).postDelayed(new Runnable() {
                              @Override public void run() {
                                  try { NotificationManagerCompat.from(fctx).cancel(WATER_NOTIFICATION_ID); } catch (Exception e) { }
                              }
                          }, 2000);
                      }
                  } catch (SecurityException se) { } catch (Exception e) { }
              }
          }
          EOF

          cat > android/app/src/main/java/com/fitflow/app/HealthSyncReceiver.java << 'EOF'
          package com.fitflow.app;

          import android.app.AlarmManager;
          import android.app.PendingIntent;
          import android.content.BroadcastReceiver;
          import android.content.Context;
          import android.content.Intent;
          import android.content.SharedPreferences;

          public class HealthSyncReceiver extends BroadcastReceiver {
              public static final String ACTION_PERIODIC_SYNC = "com.fitflow.app.ACTION_HEALTH_PERIODIC_SYNC";
              private static final long BACKGROUND_SYNC_MIN_INTERVAL = 15 * 60 * 1000L; // не чаще раза в 15 минут

              @Override
              public void onReceive(Context context, Intent intent) {
                  // 0.7.13: фоновое обновление шагов для виджета без открытия приложения.
                  // Работа в отдельном потоке (goAsync), чтобы не блокировать главный.
                  final PendingResult pending = goAsync();
                  new Thread(new Runnable() {
                      @Override public void run() {
                          try {
                              SharedPreferences prefs = context.getSharedPreferences("fitflow_sensor_prefs", Context.MODE_PRIVATE);
                              String today = new java.text.SimpleDateFormat("yyyy-MM-dd", java.util.Locale.US).format(new java.util.Date());
                              String savedDate = prefs.getString("steps_date", "");
                              if (!today.equals(savedDate)) {
                                  prefs.edit()
                                      .putString("steps_date", today)
                                      .putString("steps_base_date", "")
                                      .putInt("steps_base", -1)
                                      .putInt("steps_today", 0)
                                      .putInt("hc_steps_today", 0)
                                      .putInt("hc_total_steps_today", 0)
                                      .putInt("hc_sleep_min", 0)
                                      .putFloat("hc_kcal_today", 0.0f)
                                      .putLong("hc_last_sync_ts", System.currentTimeMillis())
                                      .apply();
                              }

                              long lastSync = prefs.getLong("hc_last_sync_ts", 0);
                              if (System.currentTimeMillis() - lastSync >= BACKGROUND_SYNC_MIN_INTERVAL) {
                                  try {
                                      int[] result = HealthConnectHelper.syncNow(context);
                                      int watchSteps = result[0];
                                      int totalSteps = result[1];
                                      int sleepMin = result[2];
                                      prefs.edit()
                                          .putInt("hc_steps_today", watchSteps)
                                          .putInt("hc_total_steps_today", totalSteps)
                                          .putInt("hc_sleep_min", sleepMin)
                                          .putString("hc_sleep_bed", HealthConnectHelper.getLastBedTime())
                                          .putString("hc_sleep_wake", HealthConnectHelper.getLastWakeTime())
                                          .putLong("hc_watch_last_ts", HealthConnectHelper.getLastWatchEndMs())
                                          .putLong("hc_last_sync_ts", System.currentTimeMillis())
                                          .putString("hc_last_error", HealthConnectHelper.getLastError())
                                          .apply();
                                  } catch (Exception e) { }
                                  // Виджет перерисовывается сам по resolveWidgetSteps (часы/телефон по приоритету).
                                  try { FitFlowWidgetProvider.updateAll(context); } catch (Exception e) { }
                              }
                          } catch (Exception e) { } finally {
                              pending.finish();
                          }
                      }
                  }).start();
              }

              public static void schedulePeriodicSync(Context context) {
                  try {
                      AlarmManager am = (AlarmManager) context.getSystemService(Context.ALARM_SERVICE);
                      Intent i = new Intent(context, HealthSyncReceiver.class);
                      i.setAction(ACTION_PERIODIC_SYNC);
                      PendingIntent pi = PendingIntent.getBroadcast(context, 8402, i, PendingIntent.FLAG_UPDATE_CURRENT | PendingIntent.FLAG_IMMUTABLE);
                      if (am != null) {
                          long interval = 60 * 60 * 1000L; // каждый 1 час в фоне
                          long next = System.currentTimeMillis() + interval;
                          am.setInexactRepeating(AlarmManager.RTC_WAKEUP, next, interval, pi);
                      }
                  } catch (Exception e) { }
              }
          }
          EOF
      - name: Create HealthConnectHelper.kt
        run: |
          cat > android/app/src/main/java/com/fitflow/app/HealthConnectHelper.kt << 'KOTLIN_EOF'
          package com.fitflow.app
          import android.content.Context
          import androidx.health.connect.client.HealthConnectClient
          import androidx.health.connect.client.records.StepsRecord
          import androidx.health.connect.client.records.SleepSessionRecord
          import androidx.health.connect.client.records.ExerciseSessionRecord
          import androidx.health.connect.client.request.ReadRecordsRequest
          import androidx.health.connect.client.time.TimeRangeFilter
          import kotlinx.coroutines.runBlocking
          import org.json.JSONArray
          import org.json.JSONObject
          import java.time.Instant
          import java.time.LocalDate
          import java.time.ZoneId
          import java.time.format.DateTimeFormatter
          object HealthConnectHelper {
              @JvmStatic
              var lastError: String = ""

              @JvmStatic
              var lastBedTime: String = ""

              @JvmStatic
              var lastWakeTime: String = ""

              // Время последней записи шагов с часов (epoch ms, 0 = нет) —
              // для определения свежести данных в режиме «Авто» (часы сняты?).
              @JvmStatic
              var lastWatchEndMs: Long = 0L

              // Приложения-спутники носимых устройств (часы/браслеты), которые пишут
              // в Health Connect данные именно с устройства, а не с телефона.
              // ВАЖНО: Google Fit (com.google.android.apps.fitness) сюда НЕ входит —
              // он пишет шаги с телефона, и мы не хотим помечать их как «часы».
              private val WEARABLE_PACKAGES = setOf(
                  "com.huami.watch.hmwatchmanager",         // Zepp / Amazfit
                  "com.garmin.android.apps.connectmobile",  // Garmin Connect
                  "fi.polar.polarflow",                     // Polar Flow
                  "com.suunto.sports",                      // Suunto
                  "com.coros.health",                       // COROS
                  "com.huawei.health",                      // Huawei Health (Huawei Watch/Band)
                  "com.hihonor.health",                     // Honor Health
                  "com.xiaomi.hm.health",                   // Zepp Life (бывш. Mi Fit) / Xiaomi Band
                  "com.xiaomi.wearable",                    // Mi Fitness (Xiaomi Wear)
                  "com.sec.android.app.shealth",            // Samsung Health (Galaxy Watch)
                  "com.fitbit.FitbitMobile",                // Fitbit
                  "com.withings.wiscale2"                   // Withings Health Mate
              )

              @JvmStatic
              fun syncNow(context: Context): IntArray {
                  return try {
                      val client = HealthConnectClient.getOrCreate(context)
                      val today = LocalDate.now()
                      val zone = ZoneId.systemDefault()
                      val startOfDay = today.atStartOfDay(zone).toInstant()
                      val endOfDay = today.plusDays(1).atStartOfDay(zone).toInstant()

                      // 1. ШАГИ: читаем сырые записи и разделяем по источнику (часы vs телефон),
                      // чтобы приложение не складывало шаги телефона и часов.
                      // Читаем ПОСТРАНИЧНО: readRecords возвращает до 1000 записей за
                      // страницу (pageSize по умолчанию) и pageToken при продолжении —
                      // без цикла часть шагов за день могла бы молча потеряться.
                      var totalSteps = 0L
                      var watchSteps = 0L
                      var watchLastEndMs = 0L
                      val origins = mutableMapOf<String, Long>()
                      var pageToken: String? = null
                      do {
                          val stepsReq = ReadRecordsRequest(
                              recordType = StepsRecord::class,
                              timeRangeFilter = TimeRangeFilter.between(startOfDay, endOfDay),
                              pageToken = pageToken
                          )
                          val stepsResponse = runBlocking { client.readRecords(stepsReq) }
                          for (record in stepsResponse.records) {
                              val pkg = record.metadata.dataOrigin?.packageName ?: ""
                              val endMs = record.endTime.toEpochMilli()
                              totalSteps += record.count
                              origins[pkg] = (origins[pkg] ?: 0L) + record.count
                              if (WEARABLE_PACKAGES.contains(pkg)) {
                                  watchSteps += record.count
                                  if (endMs > watchLastEndMs) watchLastEndMs = endMs
                              }
                          }
                          pageToken = stepsResponse.pageToken
                      } while (pageToken != null)
                      lastWatchEndMs = watchLastEndMs

                      // 2. СОН: ночная сессия пересекает полночь, поэтому окно с 18:00
                      // вчерашнего дня до 18:00 сегодняшнего накрывает её целиком.
                      var sleepMin = 0
                      try {
                          val sleepStart = today.minusDays(1).atTime(18, 0).atZone(zone).toInstant()
                          val sleepEnd = today.atTime(18, 0).atZone(zone).toInstant()
                          val sleepReq = ReadRecordsRequest(SleepSessionRecord::class, TimeRangeFilter.between(sleepStart, sleepEnd))
                          val sleepResponse = runBlocking { client.readRecords(sleepReq) }
                          var totalMs = 0L
                          var bestMs = 0L
                          var bestStart: Instant? = null
                          var bestEnd: Instant? = null
                          for (record in sleepResponse.records) {
                              val ms = record.endTime.toEpochMilli() - record.startTime.toEpochMilli()
                              if (ms > 0) {
                                  totalMs += ms
                                  if (ms > bestMs) {
                                      bestMs = ms
                                      bestStart = record.startTime
                                      bestEnd = record.endTime
                                  }
                              }
                          }
                          sleepMin = (totalMs / 60000).toInt()
                          val fmt = DateTimeFormatter.ofPattern("HH:mm")
                          lastBedTime = if (bestStart != null) fmt.format(bestStart.atZone(zone)) else ""
                          lastWakeTime = if (bestEnd != null) fmt.format(bestEnd.atZone(zone)) else ""
                      } catch (_: Exception) {
                          lastBedTime = ""
                          lastWakeTime = ""
                      }

                      val originsDesc = origins.entries.joinToString(", ") { it.key + ": " + it.value }
                      lastError = "OK (часы: " + watchSteps + ", всего в HC: " + totalSteps + ", сон: " + sleepMin + "м, источники: " + (if (originsDesc.isEmpty()) "нет" else originsDesc) + ")"
                      intArrayOf(watchSteps.toInt(), totalSteps.toInt(), sleepMin)
                  } catch (e: Exception) {
                      var grantedStr = ""
                      try {
                          val client = HealthConnectClient.getOrCreate(context)
                          val granted = runBlocking { client.permissionController.getGrantedPermissions() }
                          grantedStr = " (выдано в ОС: " + granted.size + ")"
                      } catch (_: Exception) {}
                      lastError = e.javaClass.simpleName + ": " + (e.message ?: "ошибка доступа") + grantedStr
                      intArrayOf(0, 0, 0)
                  }
              }

              /* 0.8.0: сессии тренировок за сегодня из Health Connect (ExerciseSessionRecord).
                 Возвращает JSON-массив: [{recordId, type, title, start, end, minutes}].
                 Тип мапится по надёжным константам; остальное JS доопределяет по заголовку. */
              @JvmStatic
              fun readTodayWorkouts(context: Context): String {
                  return try {
                      val client = HealthConnectClient.getOrCreate(context)
                      val today = LocalDate.now()
                      val zone = ZoneId.systemDefault()
                      val startOfDay = today.atStartOfDay(zone).toInstant()
                      val endOfDay = today.plusDays(1).atStartOfDay(zone).toInstant()
                      val req = ReadRecordsRequest(ExerciseSessionRecord::class, TimeRangeFilter.between(startOfDay, endOfDay))
                      val resp = runBlocking { client.readRecords(req) }
                      val arr = JSONArray()
                      for (rec in resp.records) {
                          val minutes = ((rec.endTime.toEpochMilli() - rec.startTime.toEpochMilli()) / 60000L).toInt()
                          if (minutes < 5) continue
                          val obj = JSONObject()
                          obj.put("recordId", rec.metadata.id)
                          obj.put("type", mapExerciseType(rec.exerciseType))
                          obj.put("title", rec.title ?: "")
                          obj.put("start", rec.startTime.toEpochMilli())
                          obj.put("end", rec.endTime.toEpochMilli())
                          obj.put("minutes", minutes)
                          arr.put(obj)
                      }
                      arr.toString()
                  } catch (e: Exception) {
                      "[]"
                  }
              }

              private fun mapExerciseType(t: Int): String = when (t) {
                  ExerciseSessionRecord.EXERCISE_TYPE_RUNNING -> "run"
                  ExerciseSessionRecord.EXERCISE_TYPE_BIKING -> "bike"
                  ExerciseSessionRecord.EXERCISE_TYPE_HIKING -> "walk"
                  ExerciseSessionRecord.EXERCISE_TYPE_OTHER_WORKOUT -> "other"
                  else -> ""
              }
          }
          KOTLIN_EOF

      - name: Patch manifest and icons
        run: |
          sudo apt-get update -qq
          sudo apt-get install -y python3-pil
          python3 << 'ENDSCRIPT'
          from pathlib import Path
          from PIL import Image

          man = Path('android/app/src/main/AndroidManifest.xml')
          t = man.read_text(encoding='utf-8')
          perms = [
              'android.permission.POST_NOTIFICATIONS',
              'android.permission.RECORD_AUDIO',
              'android.permission.SCHEDULE_EXACT_ALARM',
              'android.permission.USE_EXACT_ALARM',
              'android.permission.RECEIVE_BOOT_COMPLETED',
              'android.permission.WAKE_LOCK',
              'android.permission.REQUEST_IGNORE_BATTERY_OPTIMIZATIONS',
              'android.permission.ACTIVITY_RECOGNITION',
              'android.permission.health.READ_STEPS',
              'android.permission.health.READ_TOTAL_CALORIES_BURNED',
              'android.permission.health.READ_EXERCISE',
              'android.permission.health.READ_SLEEP',
          ]
          for p in perms:
              tag = f'    <uses-permission android:name="{p}" />\n'
              if p not in t:
                  t = t.replace('</manifest>', tag + '</manifest>')
          receiver = (
              '        <receiver android:name=".FitFlowWidgetProvider" android:exported="false">\n'
              '            <intent-filter>\n'
              '                <action android:name="android.appwidget.action.APPWIDGET_UPDATE" />\n'
              '            </intent-filter>\n'
              '            <meta-data android:name="android.appwidget.provider" android:resource="@xml/fitflow_widget_info" />\n'
              '        </receiver>\n'
          )
          if 'FitFlowWidgetProvider' not in t:
              t = t.replace('</application>', receiver + '    </application>')
          health_alias = (
              '        <activity-alias\n'
              '            android:name="ViewPermissionUsageActivity"\n'
              '            android:exported="true"\n'
              '            android:targetActivity=".MainActivity"\n'
              '            android:permission="android.permission.START_VIEW_PERMISSION_USAGE">\n'
              '            <intent-filter>\n'
              '                <action android:name="android.intent.action.VIEW_PERMISSION_USAGE" />\n'
              '                <category android:name="android.intent.category.HEALTH_PERMISSIONS" />\n'
              '            </intent-filter>\n'
              '        </activity-alias>\n'
          )
          if 'ViewPermissionUsageActivity' not in t:
              t = t.replace('</application>', health_alias + '    </application>')

          health_rationale = (
              '            <intent-filter>\n'
              '                <action android:name="androidx.health.ACTION_SHOW_PERMISSIONS_RATIONALE" />\n'
              '            </intent-filter>\n'
          )
          if 'ACTION_SHOW_PERMISSIONS_RATIONALE' not in t:
              t = t.replace('</activity>', health_rationale + '        </activity>')
          water_receiver = (
              '        <receiver android:name=".WaterReminderReceiver" android:enabled="true" android:exported="true">\n'
              '            <intent-filter>\n'
              '                <action android:name="android.intent.action.BOOT_COMPLETED" />\n'
              '            </intent-filter>\n'
              '        </receiver>\n'
          )
          if 'WaterReminderReceiver' not in t:
              t = t.replace('</application>', water_receiver + '    </application>')
          health_sync_receiver = (
              '        <receiver android:name=".HealthSyncReceiver" android:enabled="true" android:exported="false">\n'
              '            <intent-filter>\n'
              '                <action android:name="com.fitflow.app.ACTION_HEALTH_PERIODIC_SYNC" />\n'
              '                <action android:name="android.intent.action.BOOT_COMPLETED" />\n'
              '            </intent-filter>\n'
              '        </receiver>\n'
          )
          if 'HealthSyncReceiver' not in t:
              t = t.replace('</application>', health_sync_receiver + '    </application>')
          queries_block = (
              '    <queries>\n'
              '        <intent>\n'
              '            <action android:name="android.health.connect.action.HEALTH_HOME_SETTINGS" />\n'
              '        </intent>\n'
              '        <intent>\n'
              '            <action android:name="androidx.health.ACTION_HEALTH_CONNECT_SETTINGS" />\n'
              '        </intent>\n'
              '        <package android:name="com.google.android.apps.healthdata" />\n'
              '        <package android:name="com.huami.watch.hmwatchmanager" />\n'
              '    </queries>\n'
          )
          if '<queries>' not in t:
              t = t.replace('</manifest>', queries_block + '</manifest>')
          # IME/клавиатура (0.3.17): без softInputMode система не сжимает вьюпорт
          # WebView — клавиатура перекрывает поле, а первая IME-сессия после
          # холодного старта теряется. adjustResize: WebView сам сжимается,
          # Chromium скроллит к полю ввода.
          if 'windowSoftInputMode' not in t:
              t = t.replace('android:name=".MainActivity"',
                            'android:name=".MainActivity"\n            android:windowSoftInputMode="adjustResize"', 1)
          man.write_text(t, encoding='utf-8')

          res = Path('android/app/src/main/res')
          for p in res.rglob('ic_launcher*'):
              if p.is_file():
                  p.unlink()
          legacy = res / 'values' / 'ic_launcher_background.xml'
          if legacy.exists():
              legacy.unlink()

          icon = Path('icon.png')
          if not icon.exists():
              print('no icon.png')
              raise SystemExit(0)

          src = Image.open(icon).convert('RGBA')
          BG = (0, 105, 107, 255)

          def solid(size, path):
              c = Image.new('RGBA', (size, size), BG)
              fg = src.copy()
              fg.thumbnail((int(size * 0.72), int(size * 0.72)), Image.Resampling.LANCZOS)
              c.paste(fg, ((size - fg.width) // 2, (size - fg.height) // 2), fg)
              path.parent.mkdir(parents=True, exist_ok=True)
              c.convert('RGB').save(path, format='PNG')

          def foreground(size, path):
              c = Image.new('RGBA', (size, size), (0, 0, 0, 0))
              fg = src.copy()
              fg.thumbnail((int(size * 0.72), int(size * 0.72)), Image.Resampling.LANCZOS)
              c.paste(fg, ((size - fg.width) // 2, (size - fg.height) // 2), fg)
              path.parent.mkdir(parents=True, exist_ok=True)
              c.save(path, format='PNG')

          for folder, size in {
              'mipmap-mdpi': 48,
              'mipmap-hdpi': 72,
              'mipmap-xhdpi': 96,
              'mipmap-xxhdpi': 144,
              'mipmap-xxxhdpi': 192,
          }.items():
              d = res / folder
              solid(size, d / 'ic_launcher.png')
              solid(size, d / 'ic_launcher_round.png')
              foreground(size, d / 'ic_launcher_foreground.png')

          drawable = res / 'drawable'
          foreground(324, drawable / 'ic_launcher_foreground.png')
          src.resize((96, 96), Image.Resampling.LANCZOS).save(drawable / 'ic_stat_icon.png', format='PNG')

          (res / 'values').mkdir(parents=True, exist_ok=True)
          (res / 'values' / 'colors.xml').write_text(
              '<?xml version="1.0" encoding="utf-8"?>\n'
              '<resources>\n'
              '    <color name="colorPrimary">#00696B</color>\n'
              '    <color name="colorPrimaryDark">#005052</color>\n'
              '    <color name="ic_launcher_background">#00696B</color>\n'
              '</resources>\n',
              encoding='utf-8',
          )

          (drawable / 'ic_launcher_background.xml').write_text(
              '<?xml version="1.0" encoding="utf-8"?>\n'
              '<vector xmlns:android="http://schemas.android.com/apk/res/android"\n'
              '    android:width="108dp"\n'
              '    android:height="108dp"\n'
              '    android:viewportWidth="108"\n'
              '    android:viewportHeight="108">\n'
              '    <path android:fillColor="#00696B" android:pathData="M0,0h108v108h-108z"/>\n'
              '</vector>\n',
              encoding='utf-8',
          )

          (drawable / 'fitflow_widget_btn_water.xml').write_text(
              '<?xml version="1.0" encoding="utf-8"?>\n'
              '<shape xmlns:android="http://schemas.android.com/apk/res/android" android:shape="rectangle">\n'
              '    <solid android:color="#00696B"/>\n'
              '    <corners android:radius="12dp"/>\n'
              '</shape>\n', encoding='utf-8')

          (drawable / 'fitflow_widget_btn_secondary.xml').write_text(
              '<?xml version="1.0" encoding="utf-8"?>\n'
              '<shape xmlns:android="http://schemas.android.com/apk/res/android" android:shape="rectangle">\n'
              '    <solid android:color="#E0F2F1"/>\n'
              '    <corners android:radius="12dp"/>\n'
              '    <stroke android:width="1dp" android:color="#00696B"/>\n'
              '</shape>\n', encoding='utf-8')

          (drawable / 'fitflow_widget_background.xml').write_text(
              '<?xml version="1.0" encoding="utf-8"?>\n'
              '<shape xmlns:android="http://schemas.android.com/apk/res/android" android:shape="rectangle">\n'
              '    <solid android:color="#E8F5F4"/>\n'
              '    <corners android:radius="20dp"/>\n'
              '    <stroke android:width="1dp" android:color="#7AA6A6"/>\n'
              '</shape>\n', encoding='utf-8')
          layout = res / 'layout'
          layout.mkdir(parents=True, exist_ok=True)
          (layout / 'fitflow_widget.xml').write_text(
              '<?xml version="1.0" encoding="utf-8"?>\n'
              '<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"\n'
              '    android:id="@+id/widget_root" android:layout_width="match_parent" android:layout_height="match_parent"\n'
              '    android:background="@drawable/fitflow_widget_background" android:orientation="vertical" android:padding="14dp">\n'
              '    <TextView android:layout_width="wrap_content" android:layout_height="wrap_content" android:text="FITFLOW · СЕГОДНЯ" android:textColor="#00696B" android:textSize="11sp" android:fontFamily="sans-serif-medium" />\n'
              '    <TextView android:id="@+id/widget_profile" android:layout_width="match_parent" android:layout_height="wrap_content" android:layout_marginTop="1dp" android:text="Профиль: Мой профиль" android:textColor="#4A6363" android:textSize="10sp" android:ellipsize="end" android:singleLine="true" />\n'
              '    <TextView android:id="@+id/widget_water" android:layout_width="wrap_content" android:layout_height="wrap_content" android:layout_marginTop="8dp" android:text="Вода: 0 / 2500 мл" android:textColor="#002021" android:textSize="15sp" android:fontFamily="sans-serif-medium" />\n'
              '    <ProgressBar android:id="@+id/widget_water_bar" style="?android:attr/progressBarStyleHorizontal" android:layout_width="match_parent" android:layout_height="4dp" android:layout_marginTop="3dp" android:max="100" android:progress="0" android:progressTint="#00696B" />\n'
              '    <TextView android:id="@+id/widget_food" android:layout_width="wrap_content" android:layout_height="wrap_content" android:layout_marginTop="5dp" android:text="Питание: 0 / 2000 ккал" android:textColor="#003738" android:textSize="13sp" />\n'
              '    <ProgressBar android:id="@+id/widget_food_bar" style="?android:attr/progressBarStyleHorizontal" android:layout_width="match_parent" android:layout_height="4dp" android:layout_marginTop="3dp" android:max="100" android:progress="0" android:progressTint="#FF9E3D" />\n'
              '    <TextView android:id="@+id/widget_steps" android:layout_width="wrap_content" android:layout_height="wrap_content" android:layout_marginTop="4dp" android:text="Шаги: 0" android:textColor="#00696B" android:textSize="11sp" android:fontFamily="sans-serif-medium" />\n'
              '    <LinearLayout android:layout_width="match_parent" android:layout_height="wrap_content" android:layout_marginTop="7dp" android:orientation="horizontal">\n'
              '        <Button android:id="@+id/widget_water_btn" android:layout_width="0dp" android:layout_height="34dp" android:layout_weight="1" android:layout_marginEnd="4dp" android:text="+250 мл" android:textSize="11sp" android:textColor="#FFFFFF" android:background="@drawable/fitflow_widget_btn_water" android:fontFamily="sans-serif-medium" />\n'
              '        <Button android:id="@+id/widget_record_btn" android:layout_width="0dp" android:layout_height="34dp" android:layout_weight="1" android:layout_marginStart="4dp" android:text="📝 Записать" android:textSize="11sp" android:textColor="#00696B" android:background="@drawable/fitflow_widget_btn_secondary" android:fontFamily="sans-serif-medium" />\n'
              '    </LinearLayout>\n'
              '</LinearLayout>\n', encoding='utf-8')
          (layout / 'fitflow_widget_large.xml').write_text(
              '<?xml version="1.0" encoding="utf-8"?>\n'
              '<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"\n'
              '    android:id="@+id/widget_root" android:layout_width="match_parent" android:layout_height="match_parent"\n'
              '    android:background="@drawable/fitflow_widget_background" android:orientation="vertical" android:padding="16dp">\n'
              '    <TextView android:layout_width="wrap_content" android:layout_height="wrap_content" android:text="FITFLOW · СЕГОДНЯ" android:textColor="#00696B" android:textSize="12sp" android:fontFamily="sans-serif-medium" />\n'
              '    <TextView android:id="@+id/widget_profile" android:layout_width="match_parent" android:layout_height="wrap_content" android:layout_marginTop="1dp" android:text="Профиль: Мой профиль" android:textColor="#4A6363" android:textSize="11sp" android:ellipsize="end" android:singleLine="true" />\n'
              '    <TextView android:id="@+id/widget_water" android:layout_width="wrap_content" android:layout_height="wrap_content" android:layout_marginTop="9dp" android:text="Вода: 0 / 2500 мл" android:textColor="#002021" android:textSize="16sp" android:fontFamily="sans-serif-medium" />\n'
              '    <ProgressBar android:id="@+id/widget_water_bar" style="?android:attr/progressBarStyleHorizontal" android:layout_width="match_parent" android:layout_height="5dp" android:layout_marginTop="3dp" android:max="100" android:progress="0" android:progressTint="#00696B" />\n'
              '    <TextView android:id="@+id/widget_water_pct" android:layout_width="wrap_content" android:layout_height="wrap_content" android:text="0% цели" android:textColor="#00696B" android:textSize="12sp" />\n'
              '    <TextView android:id="@+id/widget_food" android:layout_width="wrap_content" android:layout_height="wrap_content" android:layout_marginTop="7dp" android:text="Питание: 0 / 2000 ккал" android:textColor="#002021" android:textSize="16sp" android:fontFamily="sans-serif-medium" />\n'
              '    <ProgressBar android:id="@+id/widget_food_bar" style="?android:attr/progressBarStyleHorizontal" android:layout_width="match_parent" android:layout_height="5dp" android:layout_marginTop="3dp" android:max="100" android:progress="0" android:progressTint="#FF9E3D" />\n'
              '    <TextView android:id="@+id/widget_food_pct" android:layout_width="wrap_content" android:layout_height="wrap_content" android:text="0% цели" android:textColor="#8F4C00" android:textSize="12sp" />\n'
              '    <TextView android:id="@+id/widget_activity" android:layout_width="wrap_content" android:layout_height="wrap_content" android:layout_marginTop="7dp" android:text="Активность: 0 мин" android:textColor="#003738" android:textSize="14sp" android:fontFamily="sans-serif-medium" />\n'
              '    <LinearLayout android:layout_width="match_parent" android:layout_height="wrap_content" android:layout_marginTop="8dp" android:orientation="horizontal">\n'
              '        <Button android:id="@+id/widget_water_btn" android:layout_width="0dp" android:layout_height="38dp" android:layout_weight="1" android:layout_marginEnd="5dp" android:text="+250 мл" android:textSize="12sp" android:textColor="#FFFFFF" android:background="@drawable/fitflow_widget_btn_water" android:fontFamily="sans-serif-medium" />\n'
              '        <Button android:id="@+id/widget_record_btn" android:layout_width="0dp" android:layout_height="38dp" android:layout_weight="1" android:layout_marginStart="5dp" android:text="📝 Записать" android:textSize="12sp" android:textColor="#00696B" android:background="@drawable/fitflow_widget_btn_secondary" android:fontFamily="sans-serif-medium" />\n'
              '    </LinearLayout>\n'
              '</LinearLayout>\n', encoding='utf-8')
          xml = res / 'xml'
          xml.mkdir(parents=True, exist_ok=True)
          (xml / 'fitflow_widget_info.xml').write_text(
              '<?xml version="1.0" encoding="utf-8"?>\n'
              '<appwidget-provider xmlns:android="http://schemas.android.com/apk/res/android"\n'
              '    android:minWidth="180dp"\n'
              '    android:minHeight="110dp"\n'
              '    android:updatePeriodMillis="0"\n'
              '    android:initialLayout="@layout/fitflow_widget"\n'
              '    android:resizeMode="horizontal|vertical"\n'
              '    android:widgetCategory="home_screen" />\n', encoding='utf-8')

          adaptive = (
              '<?xml version="1.0" encoding="utf-8"?>\n'
              '<adaptive-icon xmlns:android="http://schemas.android.com/apk/res/android">\n'
              '    <background android:drawable="@drawable/ic_launcher_background"/>\n'
              '    <foreground android:drawable="@drawable/ic_launcher_foreground"/>\n'
              '</adaptive-icon>\n'
          )
          anydpi = res / 'mipmap-anydpi-v26'
          anydpi.mkdir(parents=True, exist_ok=True)
          (anydpi / 'ic_launcher.xml').write_text(adaptive, encoding='utf-8')
          (anydpi / 'ic_launcher_round.xml').write_text(adaptive, encoding='utf-8')
          print('OK icons + manifest')
          ENDSCRIPT

      - name: Restore stable debug keystore (signing)
        run: |
          mkdir -p ~/.android
          if [ -n "${{ secrets.DEBUG_KEYSTORE_BASE64 }}" ]; then
            echo "${{ secrets.DEBUG_KEYSTORE_BASE64 }}" | base64 -d > ~/.android/debug.keystore
            echo "Стабильный debug.keystore восстановлен из секрета — подпись будет постоянной."
          else
            echo "ВНИМАНИЕ: секрет DEBUG_KEYSTORE_BASE64 не найден, будет использован автоматически создаваемый debug.keystore."
          fi

      - name: Build APK
        run: |
          cd android
          chmod +x gradlew
          ./gradlew assembleDebug --no-daemon

      - name: Rename APK
        run: |
          cp android/app/build/outputs/apk/debug/app-debug.apk \
             "android/app/build/outputs/apk/debug/FitFlow-v${APP_VERSION}-debug.apk"

      - name: Upload APK
        uses: actions/upload-artifact@v4
        with:
          name: FitFlow-APK
          path: |
            android/app/build/outputs/apk/debug/app-debug.apk
            android/app/build/outputs/apk/debug/FitFlow-v${{ env.APP_VERSION }}-debug.apk

      - name: Publish GitHub Release
        if: github.event_name == 'push' && (github.ref == 'refs/heads/main' || startsWith(github.ref, 'refs/heads/arena/'))
        uses: softprops/action-gh-release@v2
        with:
          tag_name: v${{ env.APP_VERSION }}-build${{ github.run_number }}
          name: FitFlow ${{ env.APP_VERSION }} (build ${{ github.run_number }})
          generate_release_notes: true
          files: |
            android/app/build/outputs/apk/debug/app-debug.apk
            android/app/build/outputs/apk/debug/FitFlow-v${{ env.APP_VERSION }}-debug.apk
